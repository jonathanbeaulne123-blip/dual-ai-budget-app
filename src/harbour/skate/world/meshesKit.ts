/**
 * Tideline Skate Club v2 · world — the painter's kit behind `meshes.ts`.
 * The kit now lives in `src/harbour/art/cardKit.ts`, shared with Hearth Mountain;
 * this path re-exports it unchanged so the park keeps its imports.
 */
export * from '../../art/cardKit.ts';
