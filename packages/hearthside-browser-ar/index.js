// The browser viewer consumes a self-contained GLB, never Hearth's Three objects.
// Keep its supported renderer dependency separate from the authored Studio engine.
export {ModelViewerElement} from '@google/model-viewer';
