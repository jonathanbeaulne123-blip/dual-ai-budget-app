{
  "name": "dual-ai-budget-app",
  "version": "0.0.30",
  "private": true,
  "description": "Local source, checks, and project documentation for the household budgeting Google Sheet and Apps Script.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node scripts/check-syntax.mjs",
    "test": "node scripts/check-syntax.mjs && node scripts/test-maintenance.mjs && node scripts/test-duplicate-flags.mjs && node scripts/test-transaction-validation.mjs && node scripts/test-transaction-atomicity.mjs",
    "clasp:login": "clasp login",
    "clasp:version": "clasp --version",
    "clasp:status": "clasp status"
  },
  "devDependencies": {
    "@google/clasp": "^3.3.0"
  }
}
