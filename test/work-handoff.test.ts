import {describe,it,expect} from 'vitest';import {workHandoffUrl,rememberWorkHandoff,clearWorkHandoff} from '../src/workHandoff.ts';
describe('Ordinary authenticated Work handoff',()=>{
 it('encodes only the clean app origin and allowlisted destination',()=>{expect(workHandoffUrl('https://user:password@example.test/private?invite=secret#access_token=secret')).toBe('https://example.test/?open=shift');for(const url of ['http://localhost:53662/','http://127.0.0.1/','http://[::1]/','file:///tmp/a','javascript:alert(1)'])expect(workHandoffUrl(url)).toBeNull();});
 it('remembers only Shift through an auth callback and clears after deliberate navigation',()=>{const values=new Map<string,string>(),storage={getItem:(k:string)=>values.get(k)??null,setItem:(k:string,v:string)=>{values.set(k,v)},removeItem:(k:string)=>{values.delete(k)}};expect(rememberWorkHandoff(new URL('https://example.test/?open=https://evil.test'),storage)).toBe(false);expect(rememberWorkHandoff(new URL('https://example.test/?open=shift'),storage)).toBe(true);expect(rememberWorkHandoff(new URL('https://example.test/#access_token=private'),storage)).toBe(true);expect([...values.values()]).toEqual(['shift']);clearWorkHandoff(storage);expect(rememberWorkHandoff(new URL('https://example.test/'),storage)).toBe(false);});
});
