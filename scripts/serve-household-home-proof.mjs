/** Actual Household Home / Our Path / Status Centre surfaces with exclusively fictional local books (Vision v2 Horizon A evidence).
    `?composition=queen&state=checking|needs-us|building|reset|grave|empty|win` renders the Still Queen from the same fictional books (`reduced=1` asks for reduced motion through the comfort attribute); `chrome=1` adds STAND-INS for the App's top bar, sync line and collapsed instruments — they are about five times shorter than the App's real shell was, which is how the Queen came to sit in the bottom quarter of the live page. The no-scroll rule on the actual App page is measured by `scripts/serve-queen-world-page-proof.mjs` + `test/queen-world-page-layout.mjs`; this harness is for her states and the studio paths only.
    `charms=none|few|max` dresses her with fictional charms written through `guardQueenDesignSave` (with the acts that earn them seeded first); `charms=max` is the cap.
    `rings=N` closes N fictional Chapters (growth rings); `wheel=1` seeds a thrown form with both fictional turns; `portraits=N` seeds N fictional yearly portraits (2026 on) with `today=YYYY-MM-DD`;
    `clock=HH.H` fixes the living light's hour. */
import { createServer } from 'vite';
import { createServer as createPortProbe } from 'node:net';
import { readFileSync, mkdtempSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { pathToFileURL } from 'node:url';
const css = [...readFileSync('src/main.tsx','utf8').matchAll(/import "\.\/(.*\.css)";/g)].map(([,path])=>`import '/src/${path}';`).join('\n');
const entry = `${css}
import React,{useState,useRef} from 'react';import{createRoot}from'react-dom/client';
import{HouseholdHome}from'/src/HouseholdHome.tsx';import{ChapterRoom}from'/src/ChapterPanel.tsx';import{ComfortControls}from'/src/theme/ComfortControls.tsx';
import{planLifeFixture}from'/test/fixtures/plan-life.ts';import{resolveThemeScene,sceneTokens}from'/src/theme/scenes.ts';
import{openChapter,recordRitualHeld,offerMove,respondToMove,movesForChapter,openChapterFor,closeChapter}from'/src/core/chapters.ts';
import{recordHouseholdFundReconciliation,acknowledgeHouseholdPlan,postEntry,reversePostedMoney,recordWin,addRecurrence,addGoal,foundHouseholdCharter,signHouseholdCharter,fundGoal,purchaseGoal,addPotentialExpense,postDueRecurrences}from'/src/core/index.ts';import{currentPlanVersion}from'/src/core/planSystem.ts';
import{saveKittyNestDesign}from'/src/core/kittyNestDesigns.ts';import{guardQueenDesignSave}from'/src/queen/world/queenAuthoring.ts';import{queenCharmKindsEarned,QUEEN_CHARM_KINDS}from'/src/core/queenCharms.ts';import{QUEEN_CHARM_KEYBOARD_SEATS,queenCharmSettle}from'/src/queen/world/queenCharmSurface.ts';import{queenFormHandles}from'/src/core/queenForm.ts';import{STUDIO_PALETTE}from'/src/kitty/studio/palette.ts';
const q=new URLSearchParams(location.search),theme=q.get('theme')||'classic',surface=q.get('surface')||'home',quiet=q.get('quiet')==='1',composition=q.get('composition')||'panels',state=q.get('state')||'needs-us',chrome=q.get('chrome')==='1',charms=q.get('charms'),rings=Number(q.get('rings')||0),wheelSeed=q.get('wheel')==='1',portraitSeed=Number(q.get('portraits')||0),proofToday=q.get('today')||'2026-09-12',bills=q.get('bills')==='1',loft=q.get('loft')==='1',clock=q.get('clock')===null?undefined:Number(q.get('clock')),motion=q.get('motion')==='1';
const scene=resolveThemeScene(theme,surface==='home'?'home':surface==='path'?'plan':'more','household');Object.assign(document.documentElement.dataset,{theme,scene:scene.id,material:scene.material,sceneLighting:scene.dark?'dark':'light',atmosphere:motion?'playing':'paused',quiet:quiet?'true':'false'});for(const[key,value]of Object.entries(sceneTokens(scene)))document.documentElement.style.setProperty(key,value);
/* Charms are earned by acts: in charms mode a fictional August Chapter was held ten times and closed still-forming at a Sitdown, and the Charter was signed by both. Nothing about money earns anything. */
function earnedActs(h){if(!charms)return h;h=openChapter(h,{memberId:'MEM-001',foundationId:'see-our-shared-life',at:'2026-08-01T12:00:00.000Z'}).household;const ritual=h.rituals[0];for(let day=2;day<=20;day+=2)h=recordRitualHeld(h,{memberId:day%4?'MEM-001':'MEM-002',ritualId:ritual.id,onDate:'2026-08-'+String(day).padStart(2,'0')}).household;h=closeChapter(h,{memberId:'MEM-002',chapterId:h.chapters[0].id,outcome:'still-forming',carryForward:'Fictional: we kept the question list.',sitdownId:'SIT-FICTION-AUG',at:'2026-08-30T12:00:00.000Z'}).household;if(!h.charter){h=foundHouseholdCharter(h,{memberId:'MEM-001',custodianMemberId:h.householdFund?.custodianMemberId??'MEM-001',purpose:'Fictional charter',splitRule:'even',splitNote:'',ceilingKind:'none',cadence:'monthly',date:'2026-08-01'}).household;for(const memberId of['MEM-001','MEM-002'])h=signHouseholdCharter(h,{memberId,at:'2026-08-02T12:00:00.000Z'}).household;}
  const trip=addGoal(h,{name:'Fictional weekend trip',target:'200',shared:true,ownerMemberId:'MEM-002'});h=fundGoal(trip.household,{goalId:trip.postedIds[0],amount:'200',fromAccountId:'ACC-CHEQUING',date:'2026-08-10',createdBy:'MEM-002',visibility:'household'}).household;h=purchaseGoal(h,{goalId:trip.postedIds[0],amount:'200',date:'2026-08-16',createdBy:'MEM-002'}).household;return h;}
/* Growth rings: N fictional Chapters opened and closed in turn, one ring each. Nothing about money. */
/* rings=N is the total: the charm acts already close one fictional Chapter whenever charms is given. */
function ringedActs(h){for(let i=0;i<rings-(charms?1:0);i++){const day=String(1+i*2).padStart(2,'0');h=openChapter(h,{memberId:i%2?'MEM-002':'MEM-001',foundationId:'see-our-shared-life',at:'2026-07-'+day+'T12:00:00.000Z'}).household;h=closeChapter(h,{memberId:'MEM-001',chapterId:h.chapters[h.chapters.length-1].id,outcome:i%3?'established':'still-forming',carryForward:'Fictional.',at:'2026-07-'+day+'T18:00:00.000Z'}).household;}return h;}
function seeded(){let h=planLifeFixture('household');h=earnedActs(h);h=ringedActs(h);h=openChapter(h,{memberId:'MEM-001',foundationId:'make-rent-boring',at:'2026-09-01T12:00:00.000Z'}).household;const chapter=openChapterFor(h);const ritual=h.rituals.find(row=>row.chapterId===chapter.id);h=recordRitualHeld(h,{memberId:'MEM-001',ritualId:ritual.id,onDate:'2026-09-04'}).household;h=offerMove(h,{memberId:'MEM-002',chapterId:chapter.id,text:'Confirm which payday the pre-rent check belongs to',needsAcknowledgment:true}).household;return h;}
/* The Queen's Nest: fictional households shaped so each pulse state has a legible still. Nothing here is real. */
function queenSeeded(){let h=seeded();h={...h,name:'Fictional household'};
  if(state==='checking')return h; // the fixture Fund is not reconciled yet, so the pulse says Checking.
  h=recordHouseholdFundReconciliation(h,{memberId:'MEM-001',date:'2026-09-12',bankTotal:'4000',personalRemainder:'0'}).household;
  /* The cellar's ribbon: six fictional months of the rent recurrence on the card, one of which swelled; and a lidded Build bill for the loft's ledge. */
  const rent=h.recurrences.find(row=>row.note==='Fictional rent');
  for(const [month,amount] of [['03','900'],['04','900'],['05','900'],['06','1380'],['07','900'],['08','900']])h=postEntry(h,{type:'expense',date:'2026-'+month+'-20',amount,accountId:'ACC-VISA',subcategoryId:'SUB-HOUSING-ELECTRIC',note:'Fictional rent',createdBy:'MEM-001',visibility:'household',source:'recurring',sourceId:rent.id,confirmDuplicate:true}).household;
  h=addRecurrence(h,{cadence:'monthly',nextDate:'2026-09-26',type:'expense',amount:'60',accountId:'ACC-VISA',subcategoryId:'SUB-LIFE-FUN',note:'Fictional date night'}).household;
  h=addGoal(h,{name:'Fictional trip to the shore',target:'2000',shared:true,ownerMemberId:'MEM-001'}).household;
  /* The loft's rack (loft=1): two more Build goals so a second shelf has something to take. */
  if(loft){h=addGoal(h,{name:'Fictional porch renovation',target:'900',shared:true,ownerMemberId:'MEM-001'}).household;h=addGoal(h,{name:'Fictional wedding weekend',target:'3200',shared:true,ownerMemberId:'MEM-002'}).household;}
  /* The cellar's bill rail (bills=1): a fictional subscription already paid this month (a shard), a fictional hydro bill filling for the 15th, and a fictional planned expense (a ghost) — beside the rent, which is the hammer on the 20th and a crack once it is overdue. */
  if(bills){h=addRecurrence(h,{cadence:'monthly',nextDate:'2026-09-08',type:'expense',amount:'16',accountId:'ACC-VISA',subcategoryId:'SUB-LIFE-FUN',note:'Fictional streaming',kind:'subscription'}).household;h=postDueRecurrences(h,'2026-09-08',[h.recurrences.find(row=>row.note==='Fictional streaming').id],{createdBy:'MEM-002'}).household;
    h=addRecurrence(h,{cadence:'monthly',nextDate:'2026-09-15',type:'expense',amount:'140',accountId:'ACC-CHEQUING',subcategoryId:'SUB-HOUSING-ELECTRIC',note:'Fictional hydro',kind:'bill'}).household;
    h=addPotentialExpense(h,{date:'2026-09-24',title:'Fictional winter tires',amount:'480',accountId:'ACC-VISA',subcategoryId:'SUB-LIFE-FUN',createdBy:'MEM-001',visibility:'household'}).household;
    /* and one of each other purpose and group, so the rail shows every body, tint and finish: a gym subscription (Health), a transit pass as a recurring payment (Transport), a phone bill (Life), a card payment (Debt). */
    h=addRecurrence(h,{cadence:'monthly',nextDate:'2026-09-10',type:'expense',amount:'45',accountId:'ACC-VISA',subcategoryId:'SUB-HEALTH-CARE',note:'Fictional gym',kind:'subscription'}).household;
    h=addRecurrence(h,{cadence:'monthly',nextDate:'2026-09-18',type:'expense',amount:'128',accountId:'ACC-CHEQUING',subcategoryId:'SUB-TRANSPORT-TRANSIT',note:'Fictional transit pass',kind:'other'}).household;
    h=addRecurrence(h,{cadence:'monthly',nextDate:'2026-09-22',type:'expense',amount:'65',accountId:'ACC-CHEQUING',subcategoryId:'SUB-LIFE-PHONE',note:'Fictional phone',kind:'bill'}).household;
    h=addRecurrence(h,{cadence:'monthly',nextDate:'2026-09-28',type:'expense',amount:'250',accountId:'ACC-CHEQUING',subcategoryId:'SUB-DEBT-VISA',note:'Fictional card payment',kind:'other'}).household;
    /* two more planned expenses in other groups, so the frosted glass reads in olive and slate beside the plum one */
    h=addPotentialExpense(h,{date:'2026-09-13',title:'Fictional big shop',amount:'120',accountId:'ACC-CHEQUING',subcategoryId:'SUB-FOOD-GROCERIES',createdBy:'MEM-002',visibility:'household'}).household;
    h=addPotentialExpense(h,{date:'2026-09-19',title:'Fictional oil change',amount:'90',accountId:'ACC-VISA',subcategoryId:'SUB-TRANSPORT-FUEL',createdBy:'MEM-001',visibility:'household'}).household;}
  const posted=postEntry(h,{type:'expense',date:'2026-09-10',amount:'40',accountId:'ACC-CHEQUING',subcategoryId:'SUB-LIFE-FUN',createdBy:'MEM-002',visibility:'household'});
  h=reversePostedMoney(posted.household,posted.postedIds[0],{createdBy:'MEM-001',reversalDate:'2026-09-11'}).household;
  if(state==='needs-us')return h; // the September Plan is waiting on both people.
  const version=currentPlanVersion(h,'household','2026-09');
  for(const memberId of['MEM-001','MEM-002'])h=acknowledgeHouseholdPlan(h,{planVersionId:version.id,expectedDigest:version.digest,memberId,createdBy:memberId}).household;
  if(state==='reset'||state==='grave'){
    // A large Fund-funded surprise: the accepted Plan's protected promise no longer fits (reset). Without an accepted Plan for the month there is nothing to drift, so the same books read as a top-up the Fund needs before rent (grave).
    h=postEntry(h,{type:'expense',date:'2026-09-11',amount:'3500',accountId:'ACC-VISA',subcategoryId:'SUB-LIFE-FUN',note:'Fictional emergency vet',createdBy:'MEM-001',visibility:'household',funding:{fundId:h.householdFund.id,fundedCents:350000,destinationAccountId:'ACC-VISA'}}).household;
    if(state==='grave')h={...h,planVersions:[]};
    return h;}
  if(state==='empty'){for(const move of movesForChapter(h,openChapterFor(h).id).filter(row=>row.state==='offered'||row.state==='accepted'))h=respondToMove(h,{memberId:'MEM-002',moveId:move.id,response:'decline'}).household;return h;}
  if(state==='win'){h=recordWin(h,{memberId:'MEM-001',level:'shared-win',title:'We chose the rent payday',at:'2026-09-12T10:00:00.000Z'}).household;return h;}
  return h; // building: covered, and the Chapter is open.
}
function Proof(){const[state,setState]=useState(seeded);const ref=useRef(state);ref.current=state;const command=async fn=>{const result=fn(ref.current);ref.current=result.household;setState(result.household);return {...result,ok:true};};
if(surface==='path')return React.createElement('div',{className:'app our-path our-path--household','data-ledger-tab':'plan'},React.createElement(ChapterRoom,{household:state,memberId:'MEM-001',today:'2026-09-12',onCommand:command,busy:false}));
if(surface==='comfort')return React.createElement('div',{className:'app more-surfaces status-centre','data-ledger-tab':'more'},React.createElement(ComfortControls,{environment:'development'}));
return React.createElement('div',{className:'app','data-ledger-tab':'home'},React.createElement(HouseholdHome,{household:state,memberId:'MEM-001',today:proofToday,freshness:'current',busy:false,onCommand:command,onGo:()=>{},onOpenSetup:()=>{}}));}
/* The bin on her: 'few' is three of them, 'max' is the cap (sixteen) over every keyboard seat, every kind she has earned repeating. Every write goes through the guard. */
function dressed(h){if((!charms||charms==='none')&&!wheelSeed&&!portraitSeed)return h;const earned=queenCharmKindsEarned(h);const kinds=QUEEN_CHARM_KINDS.filter(kind=>earned.has(kind));const seats=QUEEN_CHARM_KEYBOARD_SEATS;const gallery=charms==='gallery';const count=!charms||charms==='none'?0:charms==='few'?3:gallery?kinds.length:seats.length;const profile=wheelSeed?[1.08,0.952,1.06,1.06]:[1,1,1,1];const form={handles:queenFormHandles(profile),rings:h.chapters.filter(c=>c.closedAt).length};const rows=[];for(let i=0;i<count;i++){const seat0=seats[i];const seat={part:seat0.part,...(queenCharmSettle(seat0.part,seat0.u,seat0.v,form)||seat0)};rows.push({id:'fiction-'+i,kind:kinds[i%kinds.length],part:seat.part,u:seat.u,v:seat.v,spin:gallery?0:(i*37)%120-60,tilt:gallery?0:(i%3-1)*8,scale:gallery?1.5:[1,1.2,0.8,1.5,0.7][i%5],color:STUDIO_PALETTE[(i*7+5)%STUDIO_PALETTE.length].hex,by:i%2?'MEM-002':'MEM-001'});}
  const design=h.kittyNestDesigns?.find(row=>row.bankKey==='king'&&row.visibility==='household');
  return saveKittyNestDesign(h,guardQueenDesignSave({memberId:'MEM-001',view:'household',bankKey:'king',expectedRevision:design?.revision??0,name:design?.name??'Our Queen',glaze:design?.glaze??'cream',category:null,studio:{version:1,draft:{id:'queen',createdAt:'2026-09-01T12:00:00.000Z',firedAt:null,sculpt:{body:'round',profile,head:'round',ears:'none',eyes:'closed',mouth:'serene',whiskers:'none',tail:'none',nose:'tiny'},paint:{base:design?.glaze??'cream',parts:{},strokes:[],stamps:[]},charms:rows,
    ...(wheelSeed?{wheel:{pull:{by:'MEM-001',at:'2026-01-10T19:00:00.000Z'},rim:{by:'MEM-002',at:'2026-01-10T19:06:00.000Z'}}}:{}),
    ...(portraitSeed?{portraits:Array.from({length:portraitSeed},(_,i)=>({year:2026+i,at:(2027+i)+'-01-03T09:00:00.000Z',by:i%2?'MEM-002':'MEM-001',profile:[1+i*0.02,1-i*0.01,1,1],rings:Math.min(i+1,12),base:['cream','sea-glass','midnight','terracotta'][i%4],parts:{},charms:rows.slice(0,Math.min(rows.length,i+1))}))}:{}),
    },fired:[]}},{earned,rings:h.chapters.filter(c=>c.closedAt).length})).household;}
function QueenProof(){const[household,setHousehold]=useState(()=>{const h=dressed(queenSeeded());if(q.get('easy')==='1')localStorage.setItem('hearth:hercules-easy-read:'+h.environment+':'+h.householdId+':MEM-001','1');return h;});const ref=useRef(household);ref.current=household;const command=async fn=>{const result=fn(ref.current);ref.current=result.household;setHousehold(result.household);return {...result,ok:true};};
const freshness=q.get('freshness')||'current';if(q.get('reduced')==='1')document.documentElement.dataset.motion='reduced';const world=q.get('world')||'auto';
return React.createElement('div',{className:'app','data-ledger-tab':'home'},
  chrome?React.createElement('header',{className:'topbar','data-proof-chrome':'top'},React.createElement('div',{className:'brand'},React.createElement('div',null,React.createElement('h1',null,'Hearth'),React.createElement('p',{className:'brand__identity'},'Alex (fictional) · Fictional household · 12:00')))):null,
  chrome?React.createElement('p',{className:'sync-freshness','data-proof-chrome':'sync'},'Fictional sync line'):null,
  React.createElement(HouseholdHome,{household,memberId:'MEM-001',today:proofToday,freshness,busy:false,onCommand:command,onGo:()=>{},onOpenSetup:()=>{},composition:'queen',world,clock}),
  chrome?React.createElement('details',{className:'home-instruments','data-proof-chrome':'instruments'},React.createElement('summary',null,'The office · instruments and boards')):null);}
createRoot(document.getElementById('root')).render(React.createElement(composition==='queen'?QueenProof:Proof));`;
export async function startHouseholdHomeProof({port=5186}={}) {
if(port===0){const probe=createPortProbe();await new Promise(resolve=>probe.listen(0,'127.0.0.1',resolve));port=probe.address().port;await new Promise(resolve=>probe.close(resolve));}
const cacheDir=mkdtempSync(join(tmpdir(),'hearth-home-'));
const server=await createServer({configFile:false,cacheDir,server:{host:'127.0.0.1',port,strictPort:true},plugins:[{name:'home-proof',resolveId(id){if(id==='/home-proof.js')return'\0home-proof';},load(id){if(id==='\0home-proof')return entry;},configureServer(vite){vite.middlewares.use(async(req,res,next)=>{if(req.method==='POST'){res.statusCode=503;res.end(JSON.stringify({ok:false}));return;}if(req.url?.split('?')[0]!=='/home-proof')return next();res.setHeader('Content-Type','text/html');res.end(await vite.transformIndexHtml('/home-proof','<!doctype html><html lang="en"><head><title>Fictional Hearth Home proof</title><meta name="viewport" content="width=device-width,initial-scale=1"></head><body><div id="root"></div><script type="module" src="/home-proof.js"></script></body></html>'));});}}]});await server.listen();
return {url:`http://127.0.0.1:${server.httpServer.address().port}/home-proof`,async close(){await server.close();rmSync(cacheDir,{recursive:true,force:true});}};
}
if(process.argv[1]&&import.meta.url===pathToFileURL(process.argv[1]).href){const proof=await startHouseholdHomeProof();console.log(`Fictional Home proof: ${proof.url}`);process.once('SIGINT',async()=>{await proof.close();process.exit(0);});}
