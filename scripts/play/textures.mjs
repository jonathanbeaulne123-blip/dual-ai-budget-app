import {chromium} from '@playwright/test';const b=await chromium.launch({headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:5198/?toys');await p.getByRole('button',{name:'Gallery',exact:true}).click();console.log(await p.locator('.play-gallery-thumbs svg').evaluateAll(async els=>Promise.all(els.map(async el=>{const s=new XMLSerializer().serializeToString(el);return await new Promise(resolve=>{const i=new Image();i.onload=()=>resolve({ok:true,name:el.getAttribute('aria-label'),w:i.width});i.onerror=()=>resolve({ok:false,name:el.getAttribute('aria-label'),xml:s.slice(0,600)});i.src='data:image/svg+xml;charset=utf-8,'+encodeURIComponent(s);});}))));await b.close();
