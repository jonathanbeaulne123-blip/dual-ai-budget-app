> **Reference only — not a bible.** Snapshot of the Google Sheets / Apps Script era (`v0.0.31`, git `f2db836`, tag `sheets-v0.0.31`).
>
> Jonathan’s latest instruction, the live files in `docs/` (outside this folder), and the TypeScript app are current. Read this to see how the project moved, not as today’s product law.
>
> Apps Script source is not in the working tree. Recover it with `git show sheets-v0.0.31:Code.gs`.

# v0.0.30 Authoritative CAD Release Candidate

## Status

- Issue: #10, open
- Branch: `issue-10-cad-currency`
- Risk: High — financial-record metadata migration
- Repository candidate: v0.0.30 on draft PR #15
- Development Apps Script: v0.0.30 source-only push completed and independently cloned/verified against commit `edb3d25`; the migration and live behavior verification remain pending
- Production: not linked, targeted, or changed

## Goal

Make CAD the authoritative currency for current household accounts and transaction records. Jonathan confirmed that the existing values labeled `USD` are CAD amounts with an incorrect label. This release changes labels and configuration only: it must not perform exchange-rate lookup, multiplication, rounding, or any other amount conversion.

## Runtime design

`Accounts.Currency` is the source of truth. `getActiveAccount_()` requires exactly one active account with a nonblank stable ID and the supported currency `CAD`. Add Transaction and Add Shift resolve that account before their write path and copy its currency into both record layers:

- `Raw Transactions.Raw_Currency`
- `Transactions.Currency`

The writers refuse a missing account, multiple active accounts, or any unsupported currency. This preserves the current no-selector interface while enforcing D-022: an explicit account selector is required before multiple active accounts can be enabled.

The Data Health Check now detects unsupported account currency, unknown or ambiguous record account links, and any Raw Transactions or Transactions currency that disagrees with its account. Diagnostics also requires all three currency headers.

## Guarded development migration

Two development-only menu actions appear only when the source is exactly v0.0.30 and the spreadsheet title exactly matches `devCopy of Budget_App__v 0.23`:

1. **Preview v0.0.30 CAD Corrections** is read-only. It scans the real live extent of Accounts, Raw Transactions, and Transactions, identifies each row by its stable ID and account link, accepts only the known transition `USD` to `CAD` or an already-correct `CAD` value, and lists every pending cell.
2. **Apply v0.0.30 CAD Corrections…** requires the exact phrase `APPLY V0.0.30 CAD TO DEV`. It rebuilds the plan under a document lock, rejects any plan drift, journals each attempted write, verifies every final currency, and compares protected row fingerprints that exclude only the currency column.

If any write or verification fails, Apply reverses its attempted currency changes and re-reads every target to prove recovery. If recovery cannot be proven, it fails loudly and instructs the user not to retry until the development Sheet is inspected. A second successful run is a verified no-op.

The migration is dynamic rather than tied to the retained snapshot's row count. Based on the last live v0.0.29 evidence, the expected development preview is likely one Accounts cell plus 39 Raw Transactions cells plus 39 Transactions cells, but the preview output—not this estimate—is authoritative.

## Protected data

The migration cannot intentionally change:

- amounts or financial totals
- dates or time-zone values
- categories or subcategories
- ownership/member links
- tips, wages, sales, or tip-out values
- transaction IDs, raw IDs, or batch links
- helper formulas, duplicate keys, or duplicate decisions
- budget, dashboard, or income-history calculations

It does not run the budget summary refresh because currency metadata is not an input to current financial calculations.

## Automated verification

The complete `pnpm test` suite passes and includes:

- pure account/currency health checks for clean, mismatched, unsupported, unknown, and ambiguous cases
- Add Transaction validation and atomic-write tests proving CAD flows into both record layers
- static protection against reintroducing `Currency: 'USD'` or `Raw_Currency: 'USD'` writer literals
- Add Shift ordering proof that account/currency validation precedes the first Tip Tracker write-capable helper
- dynamic migration planning with stable-ID and exact-A1 evidence
- repeat-safe apply and no-op behavior
- write-then-throw rollback and protected-data-drift rollback
- critical recovery-failure escalation
- refusal of unsupported currency, orphan account links, duplicate stable IDs, wrong timezone, and production spreadsheet names

## Independent review result

Gemini independently reviewed the candidate code, mocks, tests, and release plan and returned `APPROVE`. It confirmed:

- both writers derive and propagate the active account's CAD value
- missing, ambiguous, or unsupported account configuration fails before currency-dependent writes
- the migration is limited to the three currency columns
- no amount conversion, calculation refresh, multiplication, rounding, or financial-meaning change exists
- preview, stable-ID discovery, plan drift, locking, rollback, repeat-safe behavior, and Data Health coverage are appropriate

Gemini's only finding was P3 and explicitly non-blocking: the one-time migration writes pending currency cells individually. It recommended no code change because the expected development migration is approximately 79 cells. Its reference to a possible 30-second execution timeout is not applicable to the implementation: `tryLock(30000)` limits only how long the script waits to acquire the lock, not how long the acquired lock remains valid. Individual writes are less scalable than batched ranges, but the current bounded, guarded migration does not justify the extra batching complexity.

Jonathan explicitly waived Claude review for Issue #10 on 2026-08-18. With that project-owner decision recorded, the repository and independent-review gates pass. Jonathan subsequently approved the development-only source push; it completed at 9:13:42 PM and a fresh isolated clone matched `edb3d25` across all seven tracked files with the Toronto manifest unchanged. No migration function ran during the source push.

## Development verification progress

Completed source stages:

1. The latest v0.0.29 live baseline recorded 39 Transactions, 39 Raw Transactions, two Import Batches, manual Record_Count 15, and a clean pre-v0.0.30 Data Health Check. Exact live currency counts remain for the v0.0.30 preview to prove.
2. `.clasp.json` was reconfirmed as the development link, and exactly seven tracked Apps Script files were pushed at 9:13:42 PM.
3. A fresh isolated post-push clone proved that the remote reports v0.0.30, retains `America/Toronto`, and matches approved commit `edb3d25` across all seven files.

Remaining live stages:

1. **Completed:** Development Version & Diagnostics reports v0.0.30, `America/Toronto`, all expected layouts, and the v0.0.30 release entry.
2. **Completed:** The pre-migration health scan reports only the expected one USD account plus 39 USD rows in each ledger layer. Preview validates exactly 79 pending changes (`Accounts!F5`, `Raw Transactions!P5:P43`, and `Transactions!J5:J43`) with stable-ID/account evidence and no unsupported values.
3. **Completed:** Apply changed and verified exactly 79 currency cells: one Accounts, 39 Raw Transactions, and 39 Transactions. Its protected-data verification passed and the built-in Data Health Check returned no issues.
4. **Completed:** A repeat Preview validates all 79 cells as CAD and reports zero pending changes.
5. **Completed:** A controlled `$0.04` Groceries transaction and a controlled shift producing `$4.40` wages plus `$0.01` net tips moved both Transactions and Raw Transactions from 39 to 42 rows. All values in the two authoritative ledger currency columns, including the three new rows, are CAD.
6. **Completed:** Duplicate Review scanned all 42 rows and retained the unchanged five rows across two repeated keys without changing `Duplicate_Key` or `Is_Duplicate`. The final standalone Data Health Check reports no issues.

Every repository, independent-review, development-source, migration, idempotence, controlled-writer, duplicate, and health gate passes. PR #15 remains draft only for Jonathan's explicit merge approval. Production remains untouched.

No production deployment or `main` merge is implied by a successful development verification. Both remain separately gated by Jonathan.
