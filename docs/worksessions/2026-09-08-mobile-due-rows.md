# Mobile C4 — Due occurrence rows

LOCALLY VERIFIED on codex/mobile-c4-due-rows. Exact base/pre-implementation HEAD1aee199d73cea6487d709fb128d44f7e49f921a8(C3,PR394). One writer root. High; Budget(5)+2; Engagement(3)+2.

Claude's row gesture replaces the two-sheet single-occurrence path. The original no-write motion law wins over literal swipe-to-post: reveal then named inline Confirm. Left-side local deferral is explicitly per member/view/occurrence/day/device and never advances the schedule. Preserve bulk review at its existing separate entry. Same original paper/card5px/control3px/44px grammar; vertical scroll and cancellation.

Exact occurrence and source meaning bind local review, queue and authoritative replay. Personal-account or unsupported private-goal recurrences remain unavailable in this Shared posting primitive. Pending/rejected review stays until acceptance. No native/hosted/exhaustive claim; no merge/deploy/schema/Production.


## Implementation and review

`RowReveal` is shared no-write row disclosure with44px native pointer/keyboard controls; horizontal right opens review, left opens explicit local hide, vertical motion scrolls. Cancel/capture loss/second pointer/Escape rolls back and suppresses late click. Snapshot focus continuity moves pending focus to status and restores the row after rejection. Current reviewed amount/type/date drive every label; transfer disclosure uses the actual Joint allocation, not ignored stored transfer splits.

`DuePreviewSheet` is now nonmodal inline content, with a minimal44pxMono arrival link before App page content. Link focuses its heading without opening a second sheet. Home’s existing board is preserved. Scoped local deferral includes environment/household/member/view/recurrence/occurrence/day and never calls skipOccurrence. Bulk posting remains at Calendar’s existing entry; it is absent from this newly reviewed single-occurrence flow.

The optional `postOneRecurrence` reviewed form validates exact occurrence, actor, active Shared CAD accounts, category/parent, goal/Fund lineage, participants and closed period; constant semantic resource hash protects authoritative replay. Existing unreviewed callers remain compatible. UI opening includes date/auth/scope/generation. Source ABA requires explicit review refresh. All settlement/posted callbacks remain scoped through queue and acceptance.

## Proof

- Five pure/actual capture→wire→authority cases: exact overdue date and one advance, concurrent source changes versus unrelated activity, actor/malformed/refused Personal paths, actual Joint transfer and private-goal refusal, per-row local preference isolation. Four mounted UI cases: no-write motion, late cancellation, accepted/rejected focus, source ABA/explicit refresh. Three mounted App cases: accepted75.25date, changedsource and Shared/Personal roundtrip. `/tmp/c4-focused2.log` eight earlier cases4.81s; `/tmp/c4-ui3.log` finalfocus4cases2.57s; `/tmp/c4-app1.log`3cases5.76s; full final gate includes added fifth pure case.
- Ten all-main-CSS browser cases at320/390/720/1100, long200%bodyzoom, empty,busy,rejected,unsupported and long-page arrival link. No horizontal overflow or page errors; all visible buttons≥44px. Two native touch cases preserve reveal/cancel/localhide disclosure/no-post and vertical page scroll0→203px(320)/202px(390). `/tmp/hearth-mobile-c4-evidence`, `/tmp/c4-browser2.log`, `/tmp/c4-gesture2.log`; inspected320render. App DOM tests prove arrival link order/target; browser fixture reproduces long-page layout. This is not an authenticated full-App browser run.
- Independent financial/UX/verifier reviews clear after fixing transfer split labels, refreshed type/amount, removing legacy bulk escape, restoring rejected focus and adding discoverable arrival link. Funded-expense remote changes, closed-period replay and midnight invalidation are source-reviewed but not individually certified by dedicated fixtures.

Final High quick gate passed TypeScript/AI/diff and selected tests in151.667s; fingerprint`b8f972726db3ec421b1347c98c7acb226657edd0d72b7b447674c93d32d57c88`, no five-minute breach. Exact command is first line of `/tmp/c4-gate2.log` with base1aee199 and four focus files named above. Gate1passed103.931s before final focus/arrival refinement, fingerprint`54afd9548cf4b1bf0be950e78014511186a89bfbdafe17545bb6ea02bc7493b0`; no breach.

## Delivery limits

Fictional local component/model/App/authority proof; App transport mocked. No physical, hosted, exhaustive, deployment/schema/Production claim. HEAD is the commit carrying this file. Stacked draft PR follows, next Codex C5 attached claim actions.
