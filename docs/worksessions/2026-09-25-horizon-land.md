# Horizon Pass 1 — land

Status: implementation in progress. Owner: Jonathan. Builder/integrator: Codex, with the three explicitly requested parallel tracks. Risk: High (terrain/collision/camera/saved geography/presence). No deployment or land merge is authorized before the original acceptance gate.

Baseline: immutable `PIN-0` at merged PR #546, `0bb2d6792ef9e64a87ad140d28de5e8361643bb0`. Branch: `codex/horizon-land`; no rebase during the pass. First commit establishes `land/interfaces.ts`. Jonathan's latest instruction is “ok now do pass 1”, after #546 was merged. Prior Claude and physical-device acceptance is not claimed.

Authority: pasted Pass 1 request, confirmed scale 1.0 and seven large reserves (three Terraces plus four Bight Shore; both small reserves retained), canonical manifest v1.6, CONTRACT/LIGHT/STYLE and current cloud continuity. Twelve station pads and enumerated homestead pads are in land scope. Historical deck instructions are reference; latest user instructions govern.

Budget delta (5): no financial semantics, household data, ledger/schema or Confirm change. Geography and presence changes keep the existing Mountain default and isolate the dev-only Horizon world.

Engagement delta (3): continuous asymmetric ground, usable route beds, real supported structures, water/caves, graded hosts/reserves, day/night views and consistent world/Journey terrain.

Track A owns terrain, water, coast/offshore, solar/sky and their probes. Track B owns beds, town, structures, reserves, underground and their probes. Track C owns WorldDefinition v3 assembly, districts, path graph, flight data, named views and probes. Each uses an isolated checkout and commits only its files. The integrator owns shared contracts, runtime mounts/adapters, saved positions, presence, bake orchestration, captures and final evidence.

Acceptance: meaningful focused geometry tests after each section; exact clean final-head quick gate within five minutes and full build; 48 greybox page captures, slope/sun maps, crossing/journey/cable/performance evidence, door walks, independent review. Requested Claude audit, real Mac/iPhone acceptance and Jonathan's visual approval remain explicit gates before land merge. Automated captures do not substitute for physical-device acceptance.

## Integration checkpoint

The three track snapshots are integrated. The development harness mounts the same Horizon runtime as the guarded app seam, without a household or financial reads. Card geometry and body/camera collision use the same solid triangles. Navigation records retain their environment/household/member/scope partition; obsolete Horizon geography migrates to the nearest path node. Browser and Worker share the Horizon world and bounds, while Mountain bounds remain unchanged. Late-packet extrapolation now keeps Horizon positions inside its own extent.

Initial focused integration checks: 25 tests across five files passed in 5.93 seconds (presence, saved position, runtime geometry, browser lane, Harbour source fences). A first combined bake took 251.83 seconds and produced a 2,194,611-byte 2.5/5/20 m asset. Its probes exposed unresolved crossings, fixed-view conflicts, and district budget failures; it is a diagnostic build, not acceptance. The next bake uses 5/10/20 m terrain and exact segment indexing, partitioned structure geometry, and measured crossing repairs. All final evidence must be regenerated after those changes.

An initial headless Chromium view opened successfully. Its frame intervals were about 450 ms; that is a failure of this capture run's performance budget, not a physical Mac/iPhone acceptance result. Renderer backend and subsequent measurements will be retained. No Claude audit, Jonathan visual acceptance, land merge or deployment is claimed.

## Review candidate checkpoint

The integrated candidate uses one 571,191-byte terrain asset (full 5 m, lite 10 m, Journey 20 m), full-resolution collision solids and simplified lite render geometry. Surface beds and foundation bottoms are settled against the decoded terrain. Junction wall openings are regenerated even when a threshold pad already exists. A northwest contour link connects the bank walk to Green Road. It still needs its northern pad edge regraded: the actual runtime walk reaches Home, Bank and Boathouse, but the other four door routes stop there. Graph connectivity is not accepted as physical walking proof.

All fixed design anchors remain unchanged. Cable/flight clearances, several fixed camera views, full-scale journey targets and residual support samples remain open. The delivery is a draft for review, not an accepted land pass. The exact-head evidence and quantified failures live in the Downloads delivery packet; source tests passing cannot waive these failures.

The parallel review service stopped with an authentication error. Jonathan instructed Codex to stop trying the API key, so integration and verification continue locally without retrying that connection. The fresh Claude five-auditor review remains pending. A focused earlier trust review found a stale partner-pose issue; the epoch/opacity repair and real Worker WebSocket tests passed locally. That is not the requested full Claude approval.
