# Hearth worksession — Claude's Cut

- Status: VERIFIED LOCALLY; draft PR preparation
- Owner / decision owner: Jonathan; assignee Codex
- Branch: codex/mobile-b1-cut
- Baseline: cd97dfa201e01f017730e6ff74f1a5216d501891 (A5, PR376)
- Risk: Medium-High (reviewed ownership and rounding)
- Budget(5): +2; Engagement(3): +2
- Environment: local fictional Development only

## Outcome and source contract

Claude's two named readings, divider, detents and explicit filling member replace Add's two percentage fields. Confirm remains separate. The same percentSplits kernel drives displayed cents and posted ownership. Defaults derive from current active IDs; the draft binds environment, household, member, room, generation and ordered roster. Changed scope/roster requires fresh review and refuses again inside the queued callback. Other roster sizes retain all exact fields and invalid totals block Confirm.

Fixed named readings occupy the upper portion of one card; the divider tracks in a reserved44px lower lane, avoiding clipped names/amounts at0/100. This is one instrument, no duplicate reading. The original felt/brass tints,17px Fraunces cents,22×30handle and six detents remain; canonical5px/3pxcorners and44pxtargets supersede prototype values.

## Evidence

Pre-fix mounted AddSlideshow regression failed: one-cent50/50 displayed `$0.01` twice. Canonical percentSplits instead assigns the first member1cent and the final member0. No kernel or equalSplits replacement is authorized.

Final focused proof is 20/20. Eight queued cases change roster order, membership, household, actor, room, generation or environment before a real post; every stale payload refuses without a transaction. A same-scope accepted entry may advance while the reviewed ownership still posts exactly. Three-member tiny-amount negative residuals refuse without changing the canonical kernel. The mounted Add regression exercises held drag → attempted Final Confirm and Add anyway → cancellation → explicit Confirm once. A stationary12.5% handle remains12.5%.

Final Chromium matrix:20 actual AddSlideshow width/state cases (320/390/720/1100, normal/long names/large figures/enlarged text/three members), no content overflow or page errors. Two actual Add/useDialog gesture cases prove0/100 endpoint grabs, preview-only motion, first-Escape cancellation without closing Add, disabled Confirm while held, ordinary detents and one deliberate posting callback. [Matrix and gestures](../evidence/mobile-b1/browser.json), [320px](../evidence/mobile-b1/320-normal.png), [enlarged text](../evidence/mobile-b1/390-large.png). Local fixture and drivers: ignored `artifacts/browser-evidence/mobile-b1`, `/tmp/hearth-mobile-b1-evidence`. No hosted or real household data is used.

Independent UX and money rechecks found no remaining blocker. Both initial interaction findings (grab jump, parent Escape) and receipt/payload mismatch during preview are repaired. New-entry routes initialize fresh scoped drafts; duplicate/rejection retries retain their reviewed shares. The first quick gate caught two test-only type errors (non-async queue callback and a nonexistent Household.updatedAt field); those fixtures now use an async callback and a real accepted entry.

Final Medium-High quick gate passed102 assertions (61fast,41serial), TypeScript, AI and diff checks in161.498 seconds; no five-minute breach. Candidate fingerprint before evidence/documentation closure: `eb5b5760ffbe6002d5288be3ad348997de9de99f1ee8dc84197750910117bab1`. Exact command: `pnpm test -- --risk=medium-high --base=cd97dfa201e01f017730e6ff74f1a5216d501891 --focus=test/split-cut.test.ts --focus=test/add-slideshow-ui.test.ts --focus=test/duplicate-splits.test.ts --focus=test/app-startup-p1.test.ts --focus=test/month-rehearsal-mainline.test.ts --focus-reason="Tie displayed split cents to the existing posted allocation and refuse stale queued ownership, incomplete drag previews and invalid fallback shares"`.

## Acceptance / next owner

Codex prepares the separate stacked draft PR, then continues B2. The focused and browser proofs above are complete. Full-App startup/onboarding canaries ran in jsdom; browser evidence uses actual isolated AddSlideshow/useDialog components. No authenticated full-App browser, exhaustive or physical-device proof. No merge, deployment, hosted schema or physical-device claim.
