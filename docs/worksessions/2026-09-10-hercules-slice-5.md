# Hercules slice 5 — complete wardrobe

Status: LOCAL IMPLEMENTATION VERIFIED. Product acceptance and release remain open. Jonathan requested slice 5. Owner Jonathan; Codex sole writer, bounded read-only asset and persistence reviews. Baseline is the clean reviewed local slice 4, `5a833d5235c9f48dc963cb086417ff52dffba113`, on `codex/hercules-complete-wardrobe`.

Outcome: a complete dress-up wardrobe with 36 original pieces, preserved legacy choices, personal acknowledged Wear/Save and an explicitly shared household gallery. Budget delta (5): cosmetic commands remain nonfinancial and preserve private member scope and exact acknowledgement. Engagement delta (3): collection browsing, fitting, named looks and shared style inspiration across all three authored rooms. Risk High for personal/shared continuity and App integration.

The approved companion plan supplies the collection, control and persistence choices. Slice 4's likeness and physical-phone acceptance remain open; expansion stays behind the off-by-default wardrobe flag. No push/merge/deployment, schema application, secrets, provider calls or real household mutation is authorized.

Plan: audit existing contracts and authority; expand original geometry/catalogue/2D layers; implement compatible nonfinancial look and gallery operations; wire acknowledged controls and normal-app appearance; verify negative disclosure/CAS, all pieces/poses, responsive real-App workflows and scoped High quick gate. Physical/authenticated two-device acceptance is separate from fixtures or browser emulation.


## Implemented scope

- Exactly 36 original pieces across Cozy at home, Tiny office manager, Newfoundland rain, Dressing for applause, Kitchen royalty and Sunday best; 12 preserved legacy wearables; three distinct room Keepsakes; natural winter mane. Colours are variants, not extra pieces. Original geometry and shared SVG recipes are committed with provenance and deterministic hashes.
- Full fitting controls: collection/category/favourites, curated swatches, incompatible-slot replacement explanation, remove/None, undo/redo, reset to worn, shuffle with slot locks, local draft recovery, mirror/camera and garment-dependent poses. Collection shelf props use their real meshes. Normal-app portraits and 2D fallback share item/colour IDs with the 3D look.
- Personal Wear changes the whole look only after acknowledgement. Named presets save independently. Explicit household publication contains only name, item/variant IDs and creator; copy is independent. Rename/remove preserve creator ownership. Active limits are 50 personal and 100 shared; replace/delete are explicit and nothing is silently evicted.
- Existing command authority binds the actor, checks per-resource revisions and keeps deletion tombstones. Profile data remains member-personal; gallery is an explicit shared projection. Version admission blocks incompatible clients. Financial posting, generic Undo, books restoration and activity replacement cannot overwrite wardrobe resources. No money IDs are produced.
- Offline fitting remains usable with cached assets. Wear/Save/Share are disabled until connected; reconnect never starts a fresh mutation. An uncertain request retains the same ID and operation; receipt recovery precedes replay and does not reapply a stale outfit over a newer one. Unknown stored IDs and conflicting legacy source choices are preserved.

## Review and verification evidence

Read-only canon and money-risk reviewers examined the named slice-4 baseline and current diff. No remaining concrete authority, privacy or money blocker was found after repairing receipt recovery, restoration preservation and stale rename fields. Canon review found historical financial-unlock/Health-withdrawal instructions; STRATEGY and ARCHITECTURE now identify the superseding personal-profile and explicit-gallery model. Asset review found back-facing neck cloth, buried accessories, stale display props and cape fur breakthrough; two-sided cloth, front clearance, honest collection retry, distinct patio geometry and rear cape clearance address those findings.

The asset suite checks all 48 nodes, shared live skeleton/inverse binds, gzip/hash identity, extension budgets, distinct SVG recipes, manifest compatibility and sampled finite deformation for all 12 clips. Front neck/charm rays and cape-back rays cover the reproduced clearance failures. These are structural and targeted fit checks, not exhaustive visual clipping or likeness approval.

Actual built-App browser proof uses Chrome 151 on this Mac, synthetic demo data and localhost-only requests. The 36 theme/scope/width combinations (Classic, Taylor, Newfoundland × Shared/Personal × 320/390/720/1100/1440/1920) had no horizontal room overflow and zero automated WCAG A/AA violations. All 48 pieces were individually tried and captured; collection looks, nine selectable poses, back view and 2D comparison were captured. Each close removed the canvas and returned focus to the Drawer. The initial cold run recorded zero pre-open 3D requests and 1,881 ms first preview at 20 Mbps / 50 ms. Browser transfer telemetry reports decoded GLB bytes in this preview server; compressed asset size comes from verified files, not that field.

A separate browser fixture uses the real Room and real captured command authority with two synthetic member scopes. It passed lost-ack Wear → close → reopen → receipt recovery, private preset save, explicit Share, partner-private shelf isolation, independent partner copy surviving source removal, and offline write controls. Save/share receipts had empty posted IDs and gallery accessibility had zero automated violations. This is a two-member local authority fixture, not hosted Auth or two physical devices.

Proof runners are committed under `scripts/wardrobe/proof/`; reproduction commands and local-only fixture boundaries are in `scripts/wardrobe/README.md`. Screenshots/JSON live in ignored `.artifacts/hercules-slice-5/`. Neither real household exports nor provider payloads are included.

## Acceptance and release boundary

Local implementation and scoped verification are distinct from full Slice 5 product acceptance. Owner approval of the existing base likeness, visual inspection of every compatible garment/pose combination, physical midrange-phone sustained 30 fps over 30 seconds, Safari/VoiceOver and authenticated two-device Google/member continuity remain open. No browser emulation or finite-vertex test closes those gates. The wardrobe flag stays off by default. Server-first compatible release, push/merge/deployment, schema application and any real-household mutation require their own authorized work; none occurred here.

## Asset receipt

Base GLB SHA-256: `91e9b04be18df3957c8250dcc90e6432768a15ab7fc05584c3d7c584c2ef6734`; base 5,389,228 decoded bytes / 1,854,980 gzip bytes, unchanged from slice 4. Extension sizes below exclude the base.

| Pack | Gzip bytes | SHA-256 |
|---|---:|---|
| cozy | 62,372 | `98577c22b92c542e46612d3b81e7084983e2b4dd99ce309613779b267f3f59be` |
| office | 112,922 | `533faf8f79402aa61bd94b3fea01e7e5276d214e7176abad64597e6bf073f628` |
| rain | 103,212 | `8b7af95a32c7798a0fa678425985b992656001de240543487a9b10888a18d429` |
| applause | 230,677 | `f88439f843f5e0e116b71312a2c330a7e9ca5cf7fefed24bdbbc8ae2baee3062` |
| kitchen | 124,041 | `87507c7e13d744830cd78557243ecdaa03712f72e33e77b293221ad4065a8727` |
| sunday | 152,633 | `9ad791494e44293009d32c8f93ec7cc0a4bad70a6c4e90947a46b03f63297dc5` |
| legacy | 131,344 | `280df2a5dd8a08389ed9aa5d64bba8bfe05b986af74a9e219b7d78ad8b259d3b` |


## Final recovery and build checks

The final 320px / 200% text check passed with no overflowing elements, usable swatches, 40 keyboard Tab steps contained in the dialog, Escape closure and focus restored to Drawer. The initial enlarged-text failure was corrected by allowing the phone grid and sticky action controls to shrink and wrap. Final recovery passed lazy-room chunk failure/reload, base-asset failure with retained rose selection, base retry, reduced-motion frame suspension, WebGL loss with canvas disposal, 2D fallback, Escape and collection-pack failure/retry. The initial recovery runner counted a parent dialog instead of the fitting dialog; its selector was corrected. Its swatch benchmark now observes a deliberate changed variant and has a bounded timeout rather than waiting on a no-op selection.

Final isolated 3D retry under 20 Mbps / 50 ms was 1,245 ms; twelve cached swatch changes were 1.9–5.6 ms in this desktop browser. The first complete cold-App measurement above remains distinct. The final flagged main Vite build passed in 38.36 seconds and Pro UI build passed. Existing PGlite eval/browser-external and large-chunk warnings remain; no new build error. Scene chunk is 640.56 kB decoded / 165.87 kB gzip and remains lazy.

The final cape screenshot review closed the conspicuous back-fur breakthrough. Small isolated fur tips near the lower edge and one central speck remain visual polish; full combinations and owner likeness approval remain open, as listed above.


Final rebuilt-App repeat: all 36 matrix cases again passed with zero overflow, automated accessibility violations or page errors. Cold initial preview was 2,171 ms with zero pre-open 3D requests. The committed two-member proof runner also passed against the rebuilt components with no page errors. This final repeat includes the enlarged-text wrapping correction; the earlier 1,881 ms result is retained as a separate run rather than presented as the final build's measurement.


## Scoped gate history

The first High quick-gate attempt failed TypeScript in a test-only mock signature (105.980 seconds); the signature was corrected. The second passed TypeScript but exposed an obsolete kitchen test expecting dirty Health to make Hercules hide (184.035 seconds). That assertion now checks continued presence, consistent with the approved plan and existing companion behavior. The next run passed 153 tests but the broad import-parity mutation sweep exceeded its 30-second timeout under four-worker load (186.575 seconds). The unchanged import-parity file was then run alone with `pnpm exec vitest run test/ledger-import-parity.test.ts --maxWorkers=1`: all 5 tests passed in 26.58 seconds, with the sweep itself taking 17.184 seconds. The final quick gate runs the other focused tests; it does not weaken the parity assertion or raise its timeout. These failed attempts did not breach the five-minute gate budget.


## Final quick verification receipt

High quick gate **passed**: 236 tests (149 fast + 87 serial) across 18 files, TypeScript, AI-surface and diff checks. Elapsed **220.715 seconds**, five-minute SLA **not breached**. Separate single-worker import parity: 5/5 passed, giving 241 passing scoped tests across these final runs. No exhaustive/full lane was run or claimed.

Named base and precommit HEAD: `5a833d5235c9f48dc963cb086417ff52dffba113`. Gate change fingerprint: `36579c156ef598b8a4c7e44b3912f8331415fad315b3e473c9963e7301d42b8b`; working tree was dirty during implementation verification. Only evidence/status documentation was finalized afterward; this is not a clean same-SHA release gate.

Exact gate invocation (with the documented runtime PATH from the session):

```sh
pnpm --config.manage-package-manager-versions=false --config.verify-deps-before-run=never check:quick -- --base=5a833d5235c9f48dc963cb086417ff52dffba113 --risk=high --focus=test/hercules-wardrobe.test.ts --focus=test/hercules-wardrobe-ui.test.ts --focus=test/hercules-wardrobe-catalogue.test.ts --focus=test/hercules-wardrobe-continuity.test.ts --focus=test/hercules-companion-continuity.test.ts --focus=test/app-startup-p1.test.ts --focus=test/month-rehearsal-mainline.test.ts --focus=test/hercules.test.ts --focus=test/kitchen.test.ts --focus=test/statements.test.ts --focus=test/ledger-sync-authority.test.ts --focus=test/ledger-confirmation-review.test.ts '--focus-reason=Wardrobe fitting, acknowledgements, private/gallery scope, authority CAS and legacy regressions; broad ledger import parity separately passed all 5 tests with one worker after parallel timeout'
```

Local review is GO for the implemented slice. Default-on activation/hosted release and complete product acceptance are NO-GO until the named device, likeness, full-fit and authenticated continuity gates close. Next owner: Jonathan for local product review; slice 6 handles integrated product acceptance.
