# Hearthside web integration — September 19

Status: web implementation integrated; final verification is recorded by the candidate-specific result files below. Jonathan narrowed the immediate delivery to web integration and asked Codex to finish its remaining work. Native apps, SDKs, native widgets/AR and distribution are deferred. Web-only scope does not authorize deployment, schema application, service/provider activation, real invitations/calendar writes or Production changes.

- Owner: Jonathan. Root writer/integrator: Codex; bounded Sol/Terra agents investigate or implement in isolated worktrees.
- Risk: High (combined App/navigation, creative and financial-authority compatibility).
- Budget delta (5): preserve current-main money model, accepted ledger/Task/Chapter meanings and Final Confirm while integrating connected experiences.
- Engagement delta (3): one coherent Home / Study / Kitchen Table / Together house, shared making, remembered context and complete web exports across all three themes.
- Start: `codex/hearthside-program@d0f0c5f731e1f1a331177480ff609ac936fddc45`.
- Refreshed main: `f62aa94169d3f6e19da0f270b2550336b35caaf2`; 33 commits to integrate. No hosted action.
- Previous local evidence: 1,280 tests/164 files, 284.307s/300s after a recorded 319.061s breach, web build170.459s. This is historical evidence only for the new integration.
- Preserve all current edits/evidence: 26 files copied with SHA-256 into `/tmp/hearthside-web-integration-20260919/original/`; manifest `preservation.json`. Five tracked documentation edits temporarily stashed with message `Hearthside web integration: preserve prior documentation edits`; untracked artifacts remain in place.

## Required web exit

Integrate current main without weakening finance, identity, privacy or exact shared consent; complete the accepted house/navigation and missing web feature seams; prove the actual local App with scoped synthetic clients and relevant recovery; inspect responsive/theme/accessibility flows; run the required change-focused High gate and web build on the final candidate. Keep local browser/authority proof separate from hosted Google, physical browser/device and user product acceptance. Prepare any remaining concrete hosted action for separate authorization only after local work is reviewable.

## Integration decisions and evidence

At recovery, merge conflicts included additive protocol capability fences and canonical Nest design references versus new rack/fund data. Both were preserved. Chapter/month close-and-open preserves paired closure approval. Historical documentation sections and current resume edits were retained.

### Recovered integration

- Merged refreshed `origin/main@f62aa941` in `294656a371c4565660e06a1748befda85edb220c`. All eighteen textual conflicts resolved by preserving both capability/authority lines and canonical Nest references alongside rack/fund fields. This intermediate merge is not a passing candidate.
- Applied the preserved documentation stash; the AI handoff conflict retained both the newer main history and the local corrected evidence paragraph. Stash and backups remain until final preservation verification.
- Current-main nullable mismatch required explicit unavailable category/goal balances through Fund v2, Queen, Loft and Cellar. No allocation proposal or pour is offered over unreadable backing; a known King total remains known.
- Monthly Chapter closing now freezes next month/choice/owner in the same exact paired review. The first agreement keeps the original Chapter open; the second closes and opens the reviewed next Chapter atomically. Stored graph validation protects that successor. Current Queen/Journey task actions must use canonical responsibility/evidence controls.
- Focused financial run: first attempt 24/35 (strict optional-shape decoder and fixture setup defects); corrected rerun **35/35 in 3.34s**. This predates the final living-house and successor-graph checks and is not final-candidate evidence.
- Living-house presentation uses seven/thirty consecutive **dated, recorded Fund deficits** for wilt/wear bands; future obligations, private data, low balance, planned payments that remain covered and successful buffer/goal use do not cause damage. Current reconciliation and readable backing are required; stale/offline/unverified data remains neutral. These are documented presentation defaults for product review, not new money rules or persisted scores.
- Sol house task hit model capacity after leaving partial adapters; a Terra implementation task continues the isolated checkout and owns those edits. Other bounded Terra work implements export profile and shared stage outfit; Sol implements two-context actual-App local authority proof. Heavy verification is serialized.

### Current integration review

- Financial narrow run after successor graph and living selector: **37/37 tests, 5 files, 6.46s**, exit 0. Added a later regression for bank checks older than seven days; final gate must cover it.
- Default-heap TypeScript attempt exhausted its 2GB Node heap after ~339s; no pass claimed. Serialized 6GB retry completed with seven concrete export/stage type errors, all corrected. Final candidate still requires fresh TypeScript/gate/build. This is a machine resource configuration, not a relaxed code check.
- Independent financial review found and corrected consumers of legacy Ritual/Move fields in Queen rewards and Journey; canonical Task projections now supply them. Planner and Journey no longer infer Chapter-task completion merely from a nearby financial receipt. Synthetic histories execute real paired approval, assignment acceptance, dated evidence and completion commands.
- Two actual App browser contexts against real local LedgerRoom/Vault/SQLite/R2: **1/1 passed, 20.345s test work, 22.25s total** on proof head `61da80363e6f14af54d0e3ca2f6e2802c47e9201` (contains root `70ecb24e`). Stable intention and exact Task/Calendar links, unchanged money before Final Confirm, one confirmed transaction/receipt, simultaneous strokes/own undo, private draft/exact publication/recipient read/outsider empty cabinet plus existence-hiding 404/withdrawal passed. Synthetic auth/audience adapters are explicitly scoped to loopback. No legacy REST or provider/hosted request; no claim of real Google/device acceptance. Final integrated gate reruns this proof.
- House review fixed overwritten legacy object/fitting URLs, canonical focused returns, external-return validation, presentation-off leakage, missing Work-centre binding, four phone destinations, unclipped Add actions and Journey/Studio address drift. Actual-page proof is being rerun after these corrections.
- Three scoped export/stage commits were integrated; a later unrelated fixture proposal from the same agent was rejected because it silently skipped Ritual history and used an invalid closure shape. Only corrected real-command fixtures were accepted.
- Preservation check: all **26** originals remain, all **21 original untracked** files are byte-identical, **24/26** total files are byte-identical; the two changed tracked files contain current-main additions. Every nonblank added line from the preservation stash remains. Details: `/tmp/hearthside-web-integration-20260919/preservation-check.json`. The stash and original copies remain retained.

### Independent review disposition

- Financial reviewer used an isolated checkout and reported actionable reader/fixture/completion defects; all were integrated. Final focused reruns: **62/62 in 17.81s**, then **47/47 in 4.00s**. The long story run was **9/10 in 179.67s**: generation, balanced books, health and deterministic replay passed; an expected winter state still assumed fabricated financial Ritual history. Its corrected expectation is committed and awaits the integrated gate. Do not describe that earlier run as green.
- Privacy/authority review at `dee6a10a` found one medium canonical route mismatch: a Together level could name an interior on another floor. Fixed centrally in `ace4150a`; read-only re-review confirmed closure across route, focused-tool and return-context parsers. No other high/medium issue found in shared stage/private clothing, review-bound export notes, or paired next-Chapter capability composition. Local review only.
- Independent reviewer roles used Sol for browser/authority/privacy and Terra for house/export/stage work; the financial review inherited the stronger lead model for money-risk semantics. One writer per checkout; heavyweight checks serialized on this 8GB host.

### Actual App house proof

On root `4e8af5d7`, **2/2 passed in 82.63s** (72.616s main journey + 9.232s flag-off case). Twelve canonical room/level addresses reload, the same single Plan Studio node survives movement between Kitchen levels, internal tent doors update the address, Personal has no shared house shell, and legacy Memory/intention/fitting addresses and exact Task/Calendar return survive. The phone has four rooms, centred Add and Status; all Add actions stay in bounds. Common Room covers all three themes at 320/390/719/720/1100/1440/1920px, Studio at390/1440; three320px200%-text keyboard returns pass. All six390px first-viewports show a complete meaningful object above navigation. Errors and geometry failures are empty; tested source hashes unchanged and the stored financial hash/receipts/shared metadata unchanged.

First actual-page run found missing phone Status and a stale flag-off test label. Second reached all routes and caught duplicate interior headers pushing the first object below the fold; the third passed after compacting that repeated phone header. Earlier failures remain in `house-browser.log`/`house-browser2.log`; final evidence/screenshots are under `/tmp/hearthside-web-integration-20260919/house-browser3/`. Classic390 and1440 screenshots were also inspected directly. No Fund grip was rendered on the measured Together cases; expanded Fund/real keyboard interaction remains an explicit acceptance gap. This is not a complete12routes×3themes×7widths×allstates visual certification.

Remote main was refreshed again immediately before final verification and remained `f62aa94169d3f6e19da0f270b2550336b35caaf2`. Independent findings are closed. Native SDK work and copies are deferred; historical native builds/copies do not certify the new web bundle.

### Final candidate result locations

The verifier records exact HEAD, command, exit code, elapsed wall time, before/after worktree status and the quick gate's complete change fingerprint. The final result is authoritative only when its process has completed; a pending or missing file is not a pass. Read the latest gate result and build result together. The300s budget is unchanged; any breach must be named and an unchanged-candidate rerun retained. No exhaustive lane is authorized.

- `/tmp/hearthside-web-integration-20260919/gate-final.json` and `gate-final.log`
- Intermediate corrected candidate (intentionally stopped for the final guard fix): `/tmp/hearthside-web-integration-20260919/gate-rerun.json` and `gate-rerun.log`
- Final candidate: `/tmp/hearthside-web-integration-20260919/gate-confirm.json` and `gate-confirm.log`
- Unchanged final candidate budget confirmation: `/tmp/hearthside-web-integration-20260919/gate-confirm-unchanged.json` and `gate-confirm-unchanged.log`
- `/tmp/hearthside-web-integration-20260919/build-final.json` and `build-final.log`
- [P0–P15 web handoff and exact remaining actions](../briefs/HEARTHSIDE_WEB_HANDOFF_2026-09-19.md)

The worktree deliberately retains earlier documentation/evidence; implementation sources are committed. No deployment, schema application, service/provider activation, invitations, external calendar writes, native distribution or Production action occurred. Full program acceptance remains open even when these local checks pass.

### Combined-gate corrections

The first integrated High gate at `46263c4faef3dc166dca2e76f2e42bc3ffcc9fb4` failed: 24 fast-lane files failed, 170 passed; 44 tests failed, 1,285 passed and five skipped. TypeScript passed40.513s; total gate372.366s, process373.538s. Serial checks did not run. Evidence remains `gate-final.log/json`; this attempt exceeded the unchanged300s budget.

A new Queen reward projection had introduced a real module initialization cycle through Task commands, sync and sculpture validators. Extracting the unchanged pure Ritual history reader into a type-only module removes the cycle, retaining canonical Task semantics and the existing public re-export. Independent Sol review confirmed the cycle was broken. The shared outfit concurrency fixture now sends the exact companion scope to companion commands and the richer authenticated scope only to ledger authority. Focused cycle/stage/Queen/export verification passed36/36 in10.22s, including a real browser export.

Real browser tests were incorrectly in the four-worker fast lane; contention caused App/PGlite readiness and furniture matrix timeouts. All twenty directly launched browser files now belong to the existing serial lane, guarded by discovery in the lane test. Coverage, test timeouts, required canaries and the300s soft SLA are unchanged. The combined program is large enough that a functional pass may still exceed the SLA; record it as a breach, rerun the unchanged candidate, and never call it release-green solely because tests pass.

The Journey fixture/scene follow-up passed56/56 across both affected files in10.52s. Fixtures now prepare actual dated Ritual Tasks, accept responsibility and obtain each required agreement. Memoizing the projected next Move by household/member stops redundant scene work while the same test confirms a real changed Task still updates the scene. No assertion or consent boundary was relaxed. The lane/policy narrow check passed before final verification. Final preservation audit confirms all26 originals remain, all21 original untracked files are byte-identical, and every nonblank added line from the retained documentation stash survives (`preservation-final.json`).


Independent final review confirmed no dropped coverage, timeout increase or weakened assertion, and found one future guard gap: discovery only recognized `.test.ts`, while the gate supports `.test.tsx`, `.test.js` and `.test.mjs` too. Discovery and both lane-list equality patterns now cover the same extensions. Current coverage was intact. The intermediate `65eeb9a2` run passed TypeScript, all1,412 fast tests, all232 tests in the25-file serial batch, and the first long Demo fixture before it was deliberately stopped937.32s after start to apply this guard correction. It is incomplete, not a gate pass; its budget breach remains recorded.

Final build validation sets every Hearthside web presentation/capability build flag to1 and native AR to0, exercising the included web bundle without deploying it or activating any server. A durable local copy of measured results and the final project-lead summary is written under ignored `artifacts/browser-evidence/hearthside-web-2026-09-19/`. This directory contains synthetic evidence only and does not alter the verified working-change fingerprint.
