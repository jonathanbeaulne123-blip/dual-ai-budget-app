- Status: LOCALLY VERIFIED
- Branch: codex/mobile-b7-punch
- Base: b873cb8afcb3dd96c7dbac2525f2d03c3cda2958 (B6, PR388)

# B7 retained Claude Punch
Exact prior base will be B6 commit. High risk for App queued timeline guards; Budget(5)+1, Engagement(3)+2. Sole writer root.
Original packet15.3 lawone wins over HTML footnote712 drag to start a break. Preserve tall edge handle and downward axis, elapsed/break live reading, existing analog clock. Gesture reveals existing named Paid break/Unpaid break/End break/Clock out actions and never posts or changes time. Release after a completed reveal keeps actions open for a separate deliberate activation; cancellation/Escape/lost capture returns prior disclosure. Ordinary button and keyboard equivalents. Vertical page scrolling remains outside the small handle. No native Live Activity claim.
Phone Timesheet only, below720. Scope key environment/household/member/view/exact open timeline. Conflicts and confirming state retain existing dedicated flows. Existing idle clock and desktop remain. Phone discard remains explicit separate action; never attached to motion.
Capture full shaped member active/conflicting timeline set at App render, recheck inside runKitchen; sameWriteScope already guards environment/member/room/generation before and after queue. Refuse multiple active timelines. Break replacement, new shift and remote conflict refuse stale actions. Do not compare advancing wall-clock ticks. Clock-out remains status confirming and zero money postings. beginSignOut must wait for accepted clock-out and recheck scope before opening review; failure must not create a misleading pay form.
Replace both permanent1s timers with timestamp-derived visibility-aware15s live refresh,60s idleBody, disabled idleGlance; refresh immediately on visibility/pageshow, cleanup hidden/unmount. No accumulated counters.
Proof: pure actual-command stale timeline and nonmoney tests, mounted gesture/cancel/second pointer/scope/busy tests, hidden/resume timers, realTimesheet integration plus queued scope; original style320/390/720/1100,200%, long/empty/disabled/conflict/confirming, native down handle vs page scroll, keyboard44. High quick with startup/rehearsal and independent source/UX/verifier. No hosted/merge/deploy/schema.

## Implemented and independently reviewed

Tall44px-wide edge handle uses Claude's downward motion to reveal the actual elapsed/break reading and named controls. Release leaves those controls available for a separate action; this is the necessary adaptation of the prototype's drag-to-break/release-to-close sentence under packet15.3's no-write-on-motion law. Native page scroll remains outside the handle. Original analog clock remains, phone card5px, controls3px,29px Fraunces and10.5px Plex Mono. Disabling names when controls return. Desktop, idle, conflicts and confirming use the existing flows.

Both Body and Glance now derive time from timestamps, refresh at15-second aligned intervals while live, suspend all timers hidden, refresh immediately on visibility/pageshow and clean up. Idle Body keeps minute refresh; idle Glance has no timer. No local tick counter or native Live Activity.

App captures the complete shaped member timeline set at render and revalidates inside existing environment/household/member/view/generation-scoped runKitchen. Start/end/clock-out require exactly one unchanged open row. Discard explicitly accepts one unchanged open or confirming row. Conflict choice requires the same complete set, at least two candidates and the named surviving ID; an unseen third device cannot be discarded. Other accepted ledger changes do not invalidate a stable timeline. Clock-out remains nonmoney confirming state.

beginSignOut waits for actual accepted clock-out and rechecks scope, returned timeline and current navigation/Add intent before opening pay review. Failed/rejected/navigated actions cannot open a misleading form. Independent review found focus loss during keyed busy/timeline replacement; a stable same-scope boundary now restores the handle, or the confirming status. It never carries focus across rooms or steals focus from another connected control.

## Proof and failures

Initial16 focused assertions passed4.97seconds. Added conflict-set and keyboard-focus coverage brought18 assertions to4.92seconds. Three mounted actual-App clock-out cases (accepted/rejected/navigated) passed4.51seconds. TypeScript caught a broad edit's nullable punch in the separate Add initializer and a mock confirm signature mismatch; both repaired without changing that initializer's previous behavior.

The source verifier reproduced a B7 guard regression: Add→Shift→Never mind for a confirming timeline closed the sheet without clearing the draft. The new actual-App test failed before repair in6.04seconds; a separate reviewed-discard guard restored that supported action while refusing replacements. All four mounted App cases passed5.56seconds after repair. Pure tests also cover confirming discard, replaced breaks/shifts, conflicting device sets, unrelated accepted changes, queued room changes and zero financial postings.

First High gate83/84 fast assertions passed, with the old scheduling test requiring a permanent1-second interval. Updated that expectation to the new visible aligned timeout while preserving idle timer-free and no-interval checks. Gate57.603seconds,fingerprint`e83f82c2df9310cbd6688f9eb4d2f04af3b26ebbfc34cdc8949e5665c59b8a9a`, TypeScript/AI/diff passed, no five-minute breach.

Final High quick gate139/139 plus TypeScript/AI/diff passed103.633seconds; no five-minute breach; fingerprint`3540777daefade19389c21f5602681049080fe423fba622f3bbfa9a4ccd6a7b6`. Exact command: `pnpm test -- --risk=high --base=b873cb8afcb3dd96c7dbac2525f2d03c3cda2958 --focus=test/open-shift-review.test.ts --focus=test/shift-punch-ui.test.ts --focus=test/shift-clock.test.ts --focus=test/scoped-write.test.ts --focus=test/ui-performance-p2.test.ts --focus=test/app-startup-p1.test.ts --focus=test/month-rehearsal-mainline.test.ts --focus-reason="Punch preserves nonmoney timeline actions and scoped queues, permits reviewed confirming discard, gates pay review on accepted clock-out, pauses hidden clocks and restores keyboard focus"`.

Actual Timesheet browser with full main CSS:11 layout/state cases320/390/720/1100,200% body zoom, long name, empty, busy, break, confirming and conflict. Actual Paid break→End break→Clock out via keyboard preserves focus across busy/accepted replacements; transactions and posted shifts remain unchanged. All changed controls44px minimum. Native downward handle opens without commands or page scroll(0→0), touch cancellation restores, outside vertical motion scrolls280px. Errors empty; original5px card and type reviewed visually. Evidence `/tmp/hearth-mobile-b7-evidence/`, ignored harness `artifacts/browser-evidence/mobile-b7/`.

Independent financial, canon/UX and source verifier reviews clear. All fixtures fictional local Development; actual App acceptance transport is mocked. No authenticated physical-device, exhaustive, hosted, native Live Activity, merge, deployment or schema proof. Next retained instrument B8 Prise separates cards to reveal fixed computed evidence and keeps generic duplicate mutations behind scoped Confirm.
