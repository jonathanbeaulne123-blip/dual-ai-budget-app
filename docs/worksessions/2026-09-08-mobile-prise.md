- Status: LOCALLY VERIFIED · separate stacked draft PR
- Branch: codex/mobile-b8-prise
- Exact base:10d0e8d13ee07b5e4498579e71f1813f52cec26e (B7, PR389)
- Sole writer root; High risk; Budget(5)+3; Engagement(3)+2.

# B8 retained Claude Prise — design findings before implementation
Original two cards pulled apart: preserve the metaphor as evidence disclosure. Similarity score and reasons remain computed by unchanged duplicateContrastPairs/confidenceFromScore, never controlled or presented as calibrated probability. One parting handle with ordinary button/keyboard equivalents; drag/cancel never sets duplicate state. Cards show distinct actual IDs/date/account/source facts. Pending previews excluded; scope/private canaries never become comparison input.

Named Include/Exclude opens existing Confirm. Freeze target ID, explicit desired boolean, exact comparison evidence and relevant linked transaction graph. Revalidate on fresh books inside scope-fenced queue. Ledger currently calls markDuplicate(writeHousehold,..) then onChange directly. Books exposes onCommand but actual App supplies run, NOT runKitchen; add dedicated scoped duplicate action prop backed by runKitchen.

markDuplicate changes one flag/time. projectedCountable can consequently remove its transfer partner and reversal descendants. It does not repair Fund immutable events, goalContributions/savedCents, shift receipts/paid counters or claims. For these protected links retain comparison, explain that their source needs review and refuse generic duplicate exclusion pending appropriate correction. No invented auxiliary repair. For ordinary pair/reversal graph explain exact affected visible rows and cases where unflagging one row cannot restore recognition while another blocker remains. Missing or cross-view links must not expose private facts or be silently ignored.

Use displayed/scoped evidence only; the verified Books floor retains original transactions; require an exact eligible accepted source before offering a write. Existing Undo remains the existing accepted-command undo token; do not auto-treat drag-back as Undo. Source freshness/room/member/household/env and queued stale target changes must refuse. Preserve existing reverse action separately.

Proof fixtures: plain duplicate include/exclude; pair/ancestor recognition; protected Fund/goal/work/claim; changed comparison and new incoming linkage while queued; pending/private; pointer cancel/second pointer/keyboard, exact Confirm and no-write before it; scoped writer actualApp integration. Final independent source/UX/verifier and required startup/rehearsal High gate. Separate branch/stacked draft PR after B7. No merge/deploy/schema.

## Implementation and independent repairs

The accepted Books presentation contains original source transactions, verified against `booksPresentationFloor`; the earlier synthetic-row suspicion was not applicable. Pending previews never enter comparison. Original fixed scorer retained. The right card parts horizontally with a distinct accessible 44px handle; both sets of fields remain readable. Similarity sets initial overlap, differences receive paper-2 emphasis only, and named Together/Apart stops preserve one axis. Text-origin drags focus the handle, making Escape cancellation reliable. Busy/cancel/capture loss/second pointer never write. Amount typography is explicitly symmetric.

Include/Exclude now opens Confirm in a body portal with inert background and frozen target/comparison basis. The dedicated Books prop uses App runKitchen, not the old snapshot callback. Accepted pair removal restores focus to the surviving entry control; rejected acceptance remains in the dialog. A closed month, malformed transfer/correction graph or source outside the Books floor refuses without private identifiers. Linked Fund/goal/Work/claim auxiliary facts require their own source correction rather than generic duplicate recognition. Transfer pair structure requires actual reciprocal transfers, equal amount/date/endpoints and existing distinct endpoint accounts; differing duplicate flags remain valid.

Independent financial review identified that local preparation alone did not survive authoritative replay. The existing markDuplicate kind now accepts an optional identifiers-only reviewed request. The B8 path always supplies it; registry checks actor and runtime shape/target/boolean. A constant semantic resource hashes the canonical sorted review basis. Authority recomputes it from current accepted actor books before dispatch; the command repeats readiness. Raw comparison text does not travel in command arguments. Legacy unreviewed primitive callers are deliberately unchanged. The pure reader and apply wrapper are separate modules.

## Verification record in progress

Initial six core tests failed because `isMonthClosed` was imported from kitchen instead of statements; corrected, six passed2.06s. Transfer malformed-pair case added, seven pass. Initial UI six pass4.67s; independent accessibility fixes prompted seven UI tests including readable fields, text-origin Escape, modal inert and accepted-focus restoration. Final focused core/UI/authority17pass3.35s. Three actual capture→wire→authority cases cover unchanged assembly, unrelated changes, actor/malformed arguments and comparison/incoming correction/auxiliary-only claim/closed-month races. No raw comparison text in command payload.

Two actual App writer tests initially waited on the onboarding completion page (63.48s and9.05s failed harness runs); corrected the harness to activate the existing `Open ordinary Books` control. Both accepted and room-changed cases passed2.94s; no App behavior change for that harness issue. Browser harness imports all main CSS. First run had a non-exported helper import, second used an amount rejected by the real entry validator; fixed the import and made the huge display state an explicit fictional fixture. Final browser and High gate results follow.

First High gate156/156 plus TypeScript/AI/diff passed54.541s, no five-minute breach; fingerprint`42fdb08f8220e7974fc3b153180a07c9a920072d53aecb977be6282b609271f5`. Verifier then identified an authority-generation-only open-review gap. A new mounted test reproduced the failure1.90s; App replica generation now threads through Books into the keyed Ledger review/async scope, including auth round trips. Eight UI tests pass2.46s. Native text-origin Escape initially failed because the browser's mouse default removed the pointerdown focus; preventing that default preserves the slider focus while touch-action still permits vertical page scrolling. Retest passed native horizontal parting, cancellation and text Escape with no writes.


## Final evidence and boundary

Final High quick gate157/157 plus TypeScript/AI/diff passed56.709seconds; no five-minute breach. Fingerprint`eea02946a2235ed57cff9eaeb018c8bd74b4c788f4859ce291c0259215154e70`. Exact command: `pnpm test -- --risk=high --base=10d0e8d13ee07b5e4498579e71f1813f52cec26e --focus=test/duplicate-review.test.ts --focus=test/duplicate-review-authority.test.ts --focus=test/duplicate-prise-ui.test.ts --focus=test/ledger.test.ts --focus=test/ledger-sync-authority.test.ts --focus=test/scoped-write.test.ts --focus=test/app-startup-p1.test.ts --focus=test/month-rehearsal-mainline.test.ts --focus-reason="Prise preserves fixed evidence and explicit Confirm, actor-scoped acceptance and authoritative semantic source preconditions, authority-generation review retirement, linked recognition and protected auxiliary records, cancel and focus"`.

Thirteen browser cases passed:320/390/720/1100,200% long text, huge/empty, Personal/protected/rejected/busy and actual Confirm/Cancel/focus/inert. All CSS and actual font readings checked. Native horizontal readhead page scroll0→0, vertical swipe on the card551→804, outside page0→329; cancel and text Escape restore state, zero writes before Confirm. Browser drivers/screenshots/results remain local under `/tmp/hearth-mobile-b8-evidence`; ignored fictional harness`artifacts/browser-evidence/mobile-b8`. Huge state is synthetic display-only. Body200% layout proof does not claim the existing App viewport permits user zoom; integration still owns that restriction.

Independent financial, UX and verifier source reviews clear after the recorded repairs. Sole writer root. Fictional local component/App/command-authority evidence; acceptance transport mocked in App, actual authority replay exercised separately. No authenticated physical two-device, hosted, exhaustive, native application or release proof. No merge, deployment or schema. Implementation HEAD is the commit carrying this record; exact base above. Continue B9 Turn on a separate branch after publishing the draft PR.
