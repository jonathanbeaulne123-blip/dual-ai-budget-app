# Three worlds — Development release

Status: authorized release verification. Owner: Jonathan. Risk: Release; focused high gate, no exhaustive request. Budget (5): make the reviewed visual system available while preserving current financial/Auth authority. Engagement (3): release Classic Hearth, twelve Taylor eras and twelve Newfoundland scenes on the completed mobile UX.

Explicit current instruction: “push merge deploy.” This supersedes earlier local-only/no-release instructions and the release skill’s default refusal to push/merge/deploy. Scope is this candidate and the existing Development hosting pipeline; no schema application, data change or Production continuity activation.

## Exact starting state

- Theme candidate: `c25de234b8f2bcadbbcb55b7dc48e373cf2643d2`, clean `codex/three-visual-worlds`.
- Fresh main: `a17dc35b037f344f44f7a9692d64dffb0150aef7`, PR406 completed mobile release.
- Main advanced beyond the earlier `bf33c87` integration with `dcf7072`: rendered scope guards bind delayed Count writes and accepted receipts to the room that created them. Merge retains this code unchanged. Only conflict is the appended `AI_HANDOFF.md`; both histories are preserved.

## Evidence and release decision

Prior candidate proof:108 tests/TypeScript/AI surface, production build,36 destination compositions ×five widths,60 mobile component cases ×two widths,36 additional geometry checks, all automated Chrome A/AA scans clean. Initial startup timing failure then isolated and complete green reruns are in `MOBILE_WORLDS_HANDOFF.md`.

Release-wide focused gate and CI will cover the integrated source, including incoming Count guards, startup/Bianca rehearsal, theme/account lifecycle and companion boundaries. Current independent release review is bounded to App scope-guard integration and cosmetic account/worker behavior. No private original photograph, workbook, household export, credential or local artifact is included in the diff.

Decision: CONDITIONAL Development release, explicitly authorized by Jonathan with previously disclosed limitations. Real memorabilia is deferred with labelled placeholders. Native Safari/physical phone, authenticated device appearance restoration and comprehensive secondary-flow acceptance remain separate gates; deployment does not certify them. Exhaustive gates are not requested and will not run.

## Deployment and rollback

The existing GitHub main workflow builds with Production continuity disabled and deploys `hearth-books` via Workers + Assets. Verify successful CI and Cloudflare push runs for the exact merge SHA, then inspect fresh live HTML, lazy assets and theme artwork. Do not issue a second manual deployment when the workflow succeeds.

To reverse the theme release, revert the eventual PR merge using its first parent, which is main `a17dc35` (or a subsequently verified main). This preserves the mobile release’s reviewed-command, Personal partition and delayed Count protections. Do not use the pre-mobile Worker as a shortcut. Cosmetic account metadata may remain; reverted code ignores it. No database rollback is required.

The final PR and deployment receipt record actual run IDs, merge SHA, Worker version and live proof after completion.
