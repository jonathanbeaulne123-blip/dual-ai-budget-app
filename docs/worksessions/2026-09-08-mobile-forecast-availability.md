# Hearth worksession — named routes and supported forecast receipt assumptions

- Status: FOCUSED LOCAL CHECKS PASS; QUICK-GATE TIMING FAILURE RETAINED
- Owner / decision owner: Jonathan; assignee Codex
- Branch: codex/mobile-sc4-forecast-availability
- Base:7fba9a88b5767c256eb6739ab3f110279564804c (SC03, draft PR381)
- Origin/main last refreshed:6fb15c7a98f3336862bb743b836aa96a358a35b9
- Risk: High; Budget(5)+3; Engagement(3)+2
- Environment: fictional local command-built coherent Shift Bible fixtures

## Outcome

Claude's Reach retains five stops, but each count selects concrete named dated routes rather than indexing the current global top-four list. Ordinary askRoutes remains unchanged. Stable route identity binds the accepted basis, exact dated shift set, job, role, observation pool and supported receipt policy. Shared candidates across alternatives supply one allowance.

The existing kernel forecasts net tips without a channel split. The narrow positive adapter requires the complete observation pool to resolve one current own tipped job/role with coherent accepted receipt/Bible fields and unchanged supported tip-out policy. Cash-only history supports only an explicit future cash-on-shift-day assumption, with immediate tip-outs already netted. Card-only history supports only an explicit next-recorded-payout-after-shift assumption with withholding-only rules; schedule is not cutoff/payment proof. Mixed/unknown channels, deferred liabilities, missing timing, ambiguous jobs, changed policy and negative bands refuse. No ratio, wages, second tip-out or new earnings model is invented.

Route selection is not receipt-assumption consent or a contribution election. No UI, accepted headline, command, storage or network mutation in this slice. SC05 pairs elected future paths; the Reach consumer and G4 consumer review follow.

## Source review and verification

Independent money review found no remaining source blocker after enforcing coherent permanent duration and an active owned CAD cash payout destination. The full observation pool must tie worked/paid/unpaid minutes to the recorded clock span and posted hours. The ownership test initially used ACC-CASH, which really belongs to MEM-002; corrected fixture creates a MEM-001 account and verifies the destination persists.

High quick gate command: `pnpm test -- --risk=high --base=7fba9a88b5767c256eb6739ab3f110279564804c --focus=test/forecast-availability.test.ts --focus=test/ask-routes.test.ts --focus-reason="Keep net earnings bands unchanged while requiring coherent owned single-channel receipt and timing assumptions, stable named routes for every count and no contribution consent from route selection"`.

Gate failed only the unchanged route timing assertion:2589.814ms against500ms under heavy memory pressure. TypeScript, AI, diff and51/52 fast assertions passed; all14 current forecast cases passed. Total816.334seconds, five-minute breach during TypeScript (678.276seconds). Start fingerprint `dc2011bbffafe995787acf541fd16e6b3e165be1b1784f49db980d7b92858229` preceded the ownership test-only correction; tested corrected file blob `4b38d9fe6a1cc21e251c02e5d948471060000663`. Do not describe this run as a passing quick gate.

Targeted recovery: `pnpm exec vitest run test/ask-routes.test.ts --maxWorkers=1 --testTimeout=30000` passed7/7, including the unchanged500ms assertion, in53.36seconds. The not-yet-run serial canary `pnpm exec vitest run test/proof-matrix.test.ts --maxWorkers=1 --testTimeout=30000` passed7/7 in41.40seconds. All59 selected assertions thus pass across the recorded runs. No repeated TypeScript or exhaustive suite was run solely to erase the load-related evidence.

No UI or browser surface, write, storage, network, accepted headline, hosted, physical-device, merge, deployment or schema change. Mixed-channel or unsupported history remains explicitly unavailable; this is a narrow receipt-assumption adapter, not a guaranteed earnings model. Next owner: Codex creates the separate draft PR and proceeds to SC05 paired composition; G4 final consumer review remains open.
