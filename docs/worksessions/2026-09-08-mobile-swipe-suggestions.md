# Mobile C2 — honest household suggestions

Status LOCALLY VERIFIED; branch codex/mobile-c2-swipe-suggestions; exact base/pre-implementation HEAD0d8f3f2b272606b263233c305225b52e885f64b6 (C1, PR392). Sole writer root. High risk after necessary existing queue/lifetime repair; Budget(5)+2; Engagement(3)+2.

Retain Claude's six-category grid and exact observed selector. Fill remaining slots only from the incoming active household expense children with active group parents, explicitly labelled Household suggestion with no invented counts/dates. No default catalog or partner activity inference. New member can choose existing household categories, not gain custodian authority. More remains non-posting.

Independent money audit found existing Swipe uses unscoped run and stale completion callbacks. Bind rendered environment/household/view/member/generation plus a fresh open/close intent before enqueue and at drain; freeze card/category/Fund/date meaning and refuse replacements. Late callbacks cannot close/reopen a newer sheet. Existing named Post and postEntry remain the writer, including current active account/category/custodian/Fund/period validation. Neutral error copy does not promise nothing posted after uncertain transport.

Proof plan: untouched observed results, genuine first-use suggestions and parent eligibility, exact selected ID/amount, no effect on More, scope replacement/queued card change and delayed callback isolation through mounted App. Browser widths/72pxgrid/labels/keyboard/busy/empty, High focused gate plus App/rehearsal. Separate stacked draft PR; no merge/deploy/schema.

## Implemented boundary and review repairs

The observed reader now checks active parent eligibility before its six-item limit. Otherwise an invalid-parent item could displace a seventh genuine observation and mislabel it a suggestion. Valid existing observations retain their original ranking/count/date. Suggestions carry only a discriminated kind, ID and incoming name. Less than six eligible choices stays less than six, with More available.

A reviewed Post binds the rendered source basis using canonical serialization. The existing postEntry input carries only optional`swipeReviewed:true` alongside its existing IDs/amount/date/funding. This branch validates Shared expense, acting custodian, current resolved CAD card, active category/parent and exact Fund destination/amount. A hashed`swipe-review`semantic resource is captured before the additive-command early return and independently recomputed during authority replay. Remote changed card/category meaning, resolver ambiguity or Fund role refuses; unrelated accepted entries do not. No new command kind, schema, raw private basis in wire or extra account authority. Existing unreviewed postEntry callers remain compatible.

App's optional run/commit presentation-current guard isolates prequeue, queue drain, rejected/accepted/duplicate callbacks, global error and transient command/toast UI. A separate desk-scope guard preserves accepted adoption in the same books while refusing a legacy result that belongs to a departed scope. All Swipe open/close transitions retire intent, including same-room close/reopen; authentication/replica generation is in the component key. Delayed accepted ledger state is not cancelled after the fact. Confirmation references retire only their matching ID. Toast expiry uses exact-ID plus desk-scope cleanup, not the now-retired sheet intent.

## Reproduced and focused evidence

- Ran the five new App interleavings against exact base App0d8f3f2 while retaining the test harness; four failed and ordinary accepted passed5.72s (`/tmp/c2-prefail-app.log`). Restored current App immediately in a finally block. Current final six cases pass6.69s (`/tmp/c2-app5.log`): waiting room/category changes, accepted exact12.34purchase, stale rendered callback, same-room newer sheet, and late rejection checked against global App error.
- The accepted case also proves the eight-second toast expires after successful Swipe closes. The first guard incorrectly suppressed cleanup; this regression failed5.05s (`/tmp/c2-toast-prefail.log`) and passes after exact-ID/desk-scope correction.
-21Swipe/core assertions plus3actual capture→wire→authority cases passed4.52s (`/tmp/c2-authority2.log`). Includes7observed/1invalidparent, canonical property-order equivalence, remote category/card/resolver/Fund changes, unrelated acceptance, malformed marker/funding and spoofed actor. No observed usage fabricated.
- Initial focused failures: parseAmount was imported from money instead of catalog (corrected); an old source-string test omitted A3's FundLedge blocked state (updated to current contract). First mounted App run exposed missing result() on the existing V2 mock and attempted reopening through a busy disabled button; the mock now implements the real typed method and intent interleavings use captured App callbacks. These are local callback/actual-command proofs with mocked transport, not physical UI activation while disabled.
- All-main-CSS browser7cases pass320/390/720/1100 and320long200%body zoom/empty/error. Original72px2×3card grid,5pxborders,Fraunces/Figtree/Plex typography; suggestion labels distinct. Keyboard activation preserves exact amount, More does not post, no horizontal overflow/page errors. `/tmp/hearth-mobile-c2-evidence/verify.mjs`, `/tmp/c2-browser1.log`; inspected390screenshot. Body zoom does not prove native user-zoom permission.
- Independent money/UX/verifier reviews clear after the source and timer repairs.

## Delivery limits

Fictional local component/model/App and actual authority assembly only; mocked App acceptance transport. No authenticated physical, hosted, exhaustive or release proof. Implementation HEAD is the Git commit carrying this file. Separate stacked draft PR follows; no merge, deployment, hosted schema, secrets or Production. Next Codex C3 retains the destructive reveal within Claude's separate named Confirm law.


## Final focused gate

`pnpm test -- --risk=high --base=0d8f3f2b272606b263233c305225b52e885f64b6 --focus=test/swipe.test.ts --focus=test/swipe-review-authority.test.ts --focus=test/app-startup-p1.test.ts --focus=test/month-rehearsal-mainline.test.ts --focus-reason="Household suggestions never invent observations; reviewed Swipe identity survives queue, scope replacement, delayed outcomes and authoritative source changes"` passed136/136plus TypeScript/AI/diff in53.385seconds. Fingerprint`c82ded8ad048f02c4a8a7f14ef127814821739059e58ad42b0424c6110a1fb29`; no five-minute breach (`/tmp/c2-gate3.log`). Documentation closure follows; functional sources unchanged.

Gate1passed136in113.617s before the toast-cleanup regression test and correction, fingerprint`1e64c780a961812d5ec70610539fe9fb90e8874222ce0f56f4073429849a7243`. Gate2failed TypeScript16.013s because the jsdom timeout spy inferred Node's overlapping return signature; explicitly typed the browser mock, no runtime change. The final gate above includes that repair and the actual timer test. No gate exceeded five minutes.
