# Hearth worksession — Startup P1

- **Status:** FINAL REVIEW
- **Opened:** 2026-08-30 (`America/Toronto`)
- **Owner:** Jonathan
- **Assignee or AI:** Codex
- **Repository:** `dual-ai-budget-app`
- **Branch:** `codex/startup-p1`
- **Baseline SHA:** `38e48596016af4111dc3d3038eaa49d3e191afa2`
- **Head SHA:** working tree
- **PR or issue:** none
- **Risk:** High
- **Decision owner:** Jonathan
- **Environment impact:** Development and Production client runtime; no hosted data or schema mutation

## Household outcome

Hearth paints its cached local kitchen immediately and stays responsive while the on-device PGlite journal validates. Cached facts are visibly read-only until validation succeeds. Linked cloud continuity then reconciles in the background through the existing acceptance boundary.

## Budget delta (5)

`+5` — one explicit readiness gate keeps every household mutation, Confirm, authoritative export, and SQL surface fail-closed until the selected local snapshot agrees with PGlite.

## Engagement delta (3)

`+3` — the kitchen shell and bounded Hercules presence appear before books/network startup work; Office and secondary tools load only when needed.

## Verified baseline

- `origin/main` and the clean writer both started at `38e48596016af4111dc3d3038eaa49d3e191afa2`.
- Linked boot currently awaits `reconcileHousehold` and `acceptHouseholdWrite` before `setHousehold`, so network and PGlite acceptance block the cached shell.
- Unlinked boot starts `inspectBrowserBooks` after setting the household, but command entry has no independent books-readiness guard.
- The current built entry is `1,946,108` raw bytes and eagerly imports Office, Books/Batch Import, Calendar, Shift/import tools, Pairing, `jsQR`, and `uqr`. PGlite itself is dynamically imported.
- Installed `@electric-sql/pglite@0.5.5` exports `PGliteWorker`; browser books currently run directly on the UI thread.

## Scope

### In scope

- Cached-first startup state and current-household generation cancellation.
- Worker-backed browser PGlite with the same transactional ingest and audit/hash checks.
- Central UI and command write gate with honest validating/blocked recovery status.
- Background linked reconcile after local validation.
- Route/intent lazy loading for Office and secondary surfaces.
- Privacy-safe startup performance marks, focused tests, build/chunk proof, and responsive smoke evidence.

### Out of scope

- Financial formulas, command meanings, cloud schema, migrations, secrets, Production data, or Auth/RLS policy.
- PGlite incremental projection, normalized hosted journal, new synchronization protocol, or manual chunk partitioning.
- Hercules animation behavior beyond preserving the shipped bounded engine and eager presence.

## Acceptance evidence

- [x] Cached shell and Hercules render before PGlite inspection or cloud reconciliation resolves.
- [x] PGlite browser work runs in a module worker and preserves transaction rollback, audit hash, and environment isolation.
- [x] Every active-household mutation is blocked in UI and at the command boundary until the current generation is ready.
- [x] Repairable schema cases re-ingest then re-inspect; mismatch/interruption/invalid data remain read-only with zero silent ingest.
- [x] Linked reconcile starts after local readiness, is serialized, ignores stale generations, and never displays unaccepted remote money.
- [x] Office, route surfaces, QR/Pairing, Batch Import, Shift import tools, and Realtime load behind explicit boundaries.
- [x] Focused tests, full test/check/build gates, responsive/loading/error proof, independent audits, and release review pass. Native Windows cannot execute the repository's Bash-only sanitizer test; the repository offline suite passed, while a fresh browser network-disconnect toggle was unavailable.

## Plan

- [x] Extract and test startup/readiness orchestration.
- [x] Move browser PGlite behind the installed worker adapter.
- [x] Enforce the readiness gate and wire accessible status/recovery UI.
- [x] Introduce lazy surface boundaries and intent preloading.
- [x] Measure, test, audit, verify, and prepare release handoff.

## Evidence log

- 2026-08-30: clean branch created from `origin/main@38e4859`; no PR, deploy, hosted mutation, or Production action.
- 2026-08-30: focused startup/books gates passed (5 files, 28 tests) and `tsc --noEmit` passed.
- 2026-08-30: exact final source passed all tests except the excluded native-Bash API file: 180 files / 1,192 tests passed, 1 file / 2 tests skipped, in 308 s. A prior run proved 7/8 API tests; its sole failure is the known native-Windows `spawnSync bash ENOENT` sanitizer invocation. The annual stress fixture passes with a 30 s per-test allowance (observed about 15.1 s).
- 2026-08-30: `ai:verify`, TypeScript, Vite production build, Hercules Pro companion build, no-`_redirects`, and `git diff --check` passed. The final production entry is 1,199,309 raw bytes versus the baseline artifact's 1,946,108 bytes (746,799 bytes / 38.37% smaller).
- 2026-08-30: build emitted separate Office, Books, Batch Import, Calendar, Work Shift, Pairing, QR, shift-import, continuity Realtime, soft-presence Realtime, and PGlite worker chunks.
- 2026-08-30: real-browser synthetic smoke proved cached `validating` shell + nav, disabled Post, successful `ready` unlock, mismatch fail-closed UI, clean 390×844 and 1100×800 layouts with no horizontal overflow, zero console warnings/errors on the fresh path, and Calendar absent from Home assets until Calendar intent. QR, Pairing, and Books were absent from Home assets.
- 2026-08-30: final Startup P1 repair gate passed 31 focused startup/readiness/PGlite/Hercules tests plus TypeScript. An unresolved linked pull no longer occupies the write queue; a local Post completes while that pull remains pending. Metadata carry-forward rejects changed canonical financial facts before persistence, and Hercules permission/restore/presence metadata is rebuilt from the serialized current snapshot.
- 2026-08-30: two independent static re-audits reported no remaining P0/P1 blocker after Batch Import rejection containment, Realtime rejection fallback, remote-pull queue separation, and stale metadata-write repairs.

## Decisions

- Read-only cached display before validation is explicitly authorized by Jonathan; accepted write authority remains PGlite.
- Browser PGlite moves as one engine to a worker so inspection and later transactions do not compete for the same IndexedDB database from two runtimes.
- Basic shell and Hercules stay eager. Secondary surfaces use behavior-level boundaries, not speculative vendor chunking.

## Remaining uncertainty

- The combined `pnpm check` script remains non-native on Windows because it invokes Unix `rm`, `test`, and one Bash sanitizer; equivalent Windows build gates passed, but the Bash sanitizer itself was not re-executed here.
- No fresh real-browser network-disconnect toggle was available in the in-app browser; offline continuity remains covered by the repository suite rather than a new viewport smoke.

## Handoff

Implementation, local verification, and independent final audits are complete. Nothing has been pushed, deployed, or mutated outside the clean local worktree. Jonathan remains release decision owner.
