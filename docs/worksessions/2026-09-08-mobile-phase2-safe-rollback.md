# Phase 2 reversible release: compatibility fallback

Release risk; owner Jonathan. Explicit release instruction includes reversibility. Budget (5) +0; Engagement (3) +0. Exact start bf33c87b39f9f594d12d092a3f29d3e48f51e5ba; previous UI source 6fb15c7a98f3336862bb743b836aa96a358a35b9. Separate dormant branch codex/mobile-phase2-safe-rollback. Root sole writer, independent money auditor read-only.

Restore prior src/test/index, retain current src/core and src/ledgerSync, retain Count recovery files and B2 App/WorkShiftPage recovery hunks. No database/schema/Production changes. TypeScript passes. Focused authority/privacy/recovery checks and production build required before this is offered as the release fallback. See docs/MOBILE_PHASE_2_SAFE_ROLLBACK.md.

Compatibility review additionally retained App scope and auth-generation invalidation. Count regression reproduces both old forward failures and passes with the rendered-room guard. Rollback carries a dedicated actual-App acceptance/stale-render/late-room/auth harness. Use the existing configured GitHub workflow_dispatch on this branch for rollback activation. A branch-only Cloudflare build does not prove Google configuration and is not an approved hot-rollback version. An attempted dormant-upload workflow change was refused by the existing OAuth token workflow scope; that unpushed workflow change was removed. No permission expansion is required for the source-based fallback.
