# Hearth worksession — Fund Slice 3 rail and stage

- **Status:** LOCAL SEALED — implementation and independent review complete; not pushed
- **Opened:** 2026-09-03 (`America/Toronto`)
- **Owner:** Jonathan
- **Assignee or AI:** Codex
- **Repository:** `jonathanbeaulne123-blip/dual-ai-budget-app`
- **Branch:** `fund/3-rail`
- **Baseline SHA:** `f31875772a8cb98e3ace01161fc530415a2a5af0`
- **Risk:** High — member-Personal continuity plus visible Shared Fund presentation
- **Decision owner:** Jonathan
- **Environment impact:** local source, tests, and documentation only; no schema, hosted data, secret, Production, push, merge, or deployment

## Household outcome

Each member has one eight-place Fund board. Level remains first and is the calm daily stage. A plate click changes the one existing stage without writing household state; the selection lasts for that member and Toronto civil day in session storage. Arrangement state belongs only to that member's Personal envelope.

## Dual Course deltas

- **Budget delta (5):** `+3` — the board keeps the existing Fund projections intact and puts member-owned presentation behind a fail-closed Personal boundary.
- **Engagement delta (3):** `+3` — the fixed rail and one-click stage make the current eight Fund views explorable without another router, modal, or scrolling shelf.

## Packet reconciliation

The dated packet registers sixteen library ids and later widget work. Current canon at D-161/D-173 has eight implemented, independently reviewed Fund plate models and explicitly leaves later consequence, shape, streams, and drawer slices out. Slice 3 therefore registers the full sixteen-id core contract and role defaults, while the visible rail arranges only truthful plate models already present. Missing later widgets are not synthesized. The current phone office remains structurally unchanged; `phoneRail` seals the first-six projection for the later phone presentation packet.

## Scope

### In scope

- Pure Fund rail constants, defaults, validation, first-six phone projection, Ask scope, and full library inventory.
- Self-owned `setFundRailSlot` and `resetFundRail` commands.
- A dedicated device-local member-Personal persistence lane that bypasses accepted-books, PGlite, receipts, revision changes, and transport.
- Personal split, payload guard, overlay, assembly, and deterministic convergence.
- Current eight Fund plates ordered by the member rail, with Level pinned first.
- One session-only, member-and-Toronto-day stage selection using the existing wide stage.
- Tablist, tab, tabpanel, arrow navigation, Enter/Space staging, focus transfer, and 3px pine inset selection.
- Focused adversarial and continuity tests; decision evidence.

### Out of scope

- Slice 4 drawer UI, drag/drop, a second stage, another router, or modal.
- New consequence, streams, shape, record, minutes, Swipe, contribution, or Ask financial models.
- Phone-office redesign, money arithmetic, posting authority, accepted-books hashes, PGlite schema, hosted rows, Auth/RLS, provider, secret, Production, push, merge, or deployment.

## Acceptance evidence

- [x] Exact clean baseline and branch recorded.
- [x] Full sixteen-id library, role defaults, eight-slot invariant, and first-six phone projection implemented.
- [x] Cross-member changes and custodian Ask refuse with exact packet copy.
- [x] Level is pinned first and duplicates cannot survive a command or envelope guard.
- [x] Shared sync strips the member rail; Personal split/overlay/assembly/merge preserves it.
- [x] Current eight Fund plates stay visible and are ordered by available member preferences.
- [x] The one existing stage changes on plate selection; Level remains the daily resting view.
- [x] Stage selection is session-only and keyed by member plus Toronto civil date.
- [x] Focused core, presentation, visibility, and preference tests pass.
- [x] Independent books/privacy review complete.
- [x] Quick gate, build, and final diff/secret review complete.

## Evidence log

- `pnpm exec vitest run test/fund-rail.test.ts test/fund-plates.test.ts test/desk-plates.test.ts test/surface-preference.test.ts test/visibility.test.ts --maxWorkers=1` → 5 files / 68 tests passed.
- `pnpm exec tsc --noEmit` → passed.
- `git diff --check` → passed (Git emitted only expected LF-to-CRLF notices for the Windows worktree).
- `pnpm test` final quick gate → `quick-gate-passed` in 30.400s; 77 fast tests and 7 serial PGlite proof tests passed, with TypeScript, AI-surface, discovery, and diff checks green. The uncommitted coordinator labeled the change Medium; the worksession's manual risk assignment remains High because member-Personal continuity and visible money presentation changed.
- Windows-native build equivalent (`pnpm exec vite build`, `pnpm build:hercules-pro-ui`, redirect guard) → passed; Vite transformed 421 modules. The POSIX `pnpm build` wrapper itself cannot invoke `rm` in PowerShell, an environment limitation rather than a product failure.
- Local browser at a desktop viewport → eight non-scrolling Fund tabs, pine inset selection, Level resting stage, one-click `Next out`, arrow-right + Enter staging to `This week`, focus on the panel heading, and the borrowed Money-in notebook removing tab/tablist/tabpanel semantics until a plate restores the stage.
- Independent books first review found the generic preference save would still create an accepted-books revision. The final dedicated member-Personal result and exact-diff guarded writer leave `lastCommittedAt`, revision, receipts, and accepted hash unchanged and write only the acting member's Personal key. Final re-review → PASS, no P0–P3; its expanded Fund/preference/runtime/storage/continuity verification passed 69 tests.
- Independent privacy/UI first review found Shared activity leakage, stale tabpanel ownership, and an under-scoped session key; repairs removed Shared activity, gated the composite semantics to the live stage, and keyed by environment + household + member + Toronto date. Re-review then found `display: contents` unsafe for the tablist; the final real grid container passed re-review with no P0–P3.
- Secret scan across new core/test/worksession content found no credential-shaped value. Exact changed-file inventory contains only the 15 Slice 3 source, test, storage, App-routing, and canon files.

## Remaining uncertainty

The packet describes a six-item phone presentation, while current accepted canon explicitly keeps `OfficePhone` structurally unchanged. This slice seals the deterministic first-six selector but does not redesign the phone shell. The sixteen-id library also intentionally exceeds the eight models built today; later slices must add those views before the drawer may place them visibly. The member-Personal writer is intentionally device-local in this packet and does not independently publish rail preferences to hosted continuity. The D-202 quick coordinator cannot infer the manual High assignment from an uncommitted worksession, so its automatic Medium label is not a risk downgrade.

## Handoff

The local candidate is sealed for exact-SHA verification. Jonathan remains the release and push decision owner; this slice is not pushed, merged, deployed, or live.
