# Hearth worksession — actionable Home setup and Kitty Banks

- Status: OPEN
- Opened: 2026-09-12 (America/Toronto)
- Owner / decision owner: Jonathan
- Assignee: Codex; independent read-only route/diff review
- Repository: dual-ai-budget-app
- Branch: codex/feedback-f014-f015
- Baseline / initial HEAD: b159026 (fresh origin/main; #460)
- Risk: Medium — UI navigation and existing gallery entry; no financial writer change
- Environment impact: local synthetic Development verification only

## Household outcome
F-014: the first Chapter on Home leads directly to the Charter and Fund setup it asks for. F-015: shared 2D Kitty Banks open the selected bank in the existing interactive gallery, where existing reviewed funding/movement flows remain authoritative.

## Budget delta (5)
Clearer routes to setup and accepted backing; preserve CAD meaning, private scope, command options, recovery, and Final Confirm.

## Engagement delta (3)
Recognizable customized cats on the household shelf and actionable next steps in Classic, Taylor and Newfoundland.

## Scope
Home Chapter guidance and 2D bank shelf, exact gallery selection and Home return, App callback wiring, focused tests and themed browser evidence. No merge, deploy, schema, real-household writes or new financial APIs.

## Composition
Retain Home's existing scenes. Classic uses a warm wooden shelf and pine labels; Taylor uses pasted paper portraits on the existing Lover spread; Newfoundland uses a painted shelf with brass/nameplate framing. Phone banks form comfortable full-width rows; desktop banks occupy the existing growing panel. Static decoration, existing reduced-motion behavior, no invented memorabilia.

## Acceptance evidence
- [ ] Charter/Fund prompts open the actual existing destinations without completing a Move.
- [ ] Shared visible banks only; selected bank opens; empty state reaches creation.
- [ ] Gallery keeps real command options, recovery, cancel/back and Final Confirm.
- [ ] Phone/desktop Classic, Taylor, Newfoundland visual and keyboard proof.
- [ ] Focused quick gate and production build; independent review.

## Evidence log
Initial checkout created clean from freshly fetched origin/main. Sheet IDs F-014 and F-015 verified live; status remains In progress.

## Remaining uncertainty
Implementation and verification pending; no physical-device, Safari or authenticated continuity claim.

## Handoff
Local branch only. Codex owns implementation and proof; Jonathan owns any later release decision.

### Implementation review
The independent route audit identified the existing Charter modal and Books Fund-pane request as the direct Chapter 1 destinations. Both callbacks reuse `openJourneyDestination`; opening does not complete a Move. Independent diff review found no correctness issue in bank selection, household visibility, scope reset or command option/recovery forwarding.

A real-App browser pass exposed an existing gallery dismissal dependency on global `busy`: background book validation disabled Back despite no bank submission. The room now allows Back/Escape during global validation and still locks while its own `saving` is true; its existing unresolved-receipt recovery remains. Independent review verified that `saving` spans submission/status lookup through `finally`. A focused regression covers this case.

### Verification in progress
- Dependencies: `pnpm install --frozen-lockfile --ignore-scripts` succeeded, lockfile unchanged.
- Initial `pnpm exec tsc --noEmit --pretty false` passed.
- `pnpm exec vitest run test/home-feedback-ui.test.ts test/vision-v2-home-ui.test.ts test/kitty-envelope-ui.test.ts --maxWorkers=1`: 10/10 passed before the added global-busy dismissal regression.
- Initial new-test failure was a nonexistent test selector for the new-bank form, corrected to its actual heading; no product failure was concealed.
- Six baseline component screenshots: Classic/Taylor/Newfoundland at 390 and 1440.
- Real App proof uses only a fictional fixture, fresh browser contexts, local GET requests, and blocked external/provider/POST requests. It exercises the existing 3D room rather than replacing it with a drawing.
- Early real-App proof found the global-busy Back trap above. A later attempt was interrupted by dev-server hot reloads during a click; proof server watching/HMR is now disabled for stable measurement.
- The early quick gate was superseded by the dismissal fix and stopped during TypeScript after crossing the five-minute target. It is not final gate evidence.
- Local browser books initialization hit the existing opening deadline and showed “The local books did not finish opening.” These runs prove UI layout/navigation and locked-state behavior, not successful local ledger initialization, bank posting or hosted continuity. The browser timeout is not diagnosed by this UI slice.
- Independent visual review of Classic 390/720 found the new controls readable and unobstructed. The narrow desktop column wraps longer bank names; the existing Hercules reminder overlaps explanatory text but not the new setup controls.

Artwork provenance: existing `KittyFlat` renders saved Kitty Studio data, with no new raster assets or album artwork. Existing Home scenes and theme tokens remain authoritative; only shelf/setup materials are authored here.

### Verification resource constraint and draft PR
The production-build main TypeScript and separate Workspace Worker TypeScript phases passed. Local Vite bundling then spent several minutes in uninterruptible paging with little CPU progress; the host had roughly 40 MB of free physical pages. The additional browser retry was stopped to reduce pressure, and bundling is being moved to the existing PR CI build. The local build is not claimed complete. Pull-request workflow deployment is explicitly disabled (`github.event_name != 'pull_request'`).

Current real-App evidence: all seven Classic widths plus both setup destinations, and Taylor 320/390/719, passed overflow, changed-control target-size, serious/critical Home axe, selected bank, and return-focus assertions. A later Taylor run timed out during the gallery transition under resource pressure; remaining widths/themes will be measured separately. Reports retain partial successes and failures.

The PR CI now runs the actual-App Home browser matrix only when the Home, Chapter or Kitty room surface/proof files change and retains synthetic screenshots/reports as a seven-day artifact. It uses the CI job's existing pinned Playwright Chromium installation. No deployment permission, secret or additional provider is introduced. This completes verification on the runner without continuing to exhaust local memory.
Independent CI review added `src/App.tsx` to the browser-check selector so App-only navigation changes cannot skip it. The proof server now explicitly ignores the application Vite config and defines only local fixture/static serving (with PGlite dependency exclusion and ES workers), removing all inherited provider proxies.
