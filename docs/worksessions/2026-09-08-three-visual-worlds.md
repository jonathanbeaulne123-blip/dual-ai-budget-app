# Hearth worksession — three complete visual worlds

- **Status:** OPEN — implementation in progress
- **Opened:** 2026-09-08 (America/Toronto)
- **Owner / decision owner:** Jonathan
- **Assignee:** Codex, with Claude assigned production CSS
- **Repository:** jonathanbeaulne123-blip/dual-ai-budget-app
- **Branch:** codex/three-visual-worlds
- **Baseline:** 6fb15c7a98f3336862bb743b836aa96a358a35b9, verified against GitHub main
- **Risk:** Medium-High (app-wide presentation and account-owned cosmetic metadata)
- **Environment impact:** local Development implementation; no deployment or schema application

## Household outcome

Three fully authored worlds: warmer uniform Classic, twelve-era Taylor scrapbook (Shared Home Lover; Personal Home Showgirl), and twelve Newfoundland places. Preserve every existing financial workflow, scope boundary, navigation contract and Confirm semantics.

## Budget delta (5)

0 financial meaning change; improve readability and protect drafts during appearance changes.

## Engagement delta (3)

+3 intended: personal keepsakes, authored places, atmosphere, and Hercules accessories.

## Verified baseline

Current main has 12 natural scope/destination combinations including Shared Till and deep Household table. Three Home paths: OfficePhone, OfficeWide and movable Office. Existing Home theme is local PaperStock tint, not an app theme. Auth sessions omit user metadata; appearance requires authenticated GET user and a namespaced cosmetic update, separate from ledger persistence.

Incoming mobile work is unmerged and dirty in another checkout. Do not copy or overwrite it. Recheck main and route/Shift changes before final integration.

## Scope and acceptance

- [x] Scene registry, root tokens and error/entry/overlay source hooks; draft-preserving switching tests
- [x] Appearance picker, account preference, offline pending state and stale-response guards
- [x] Codex reference designs before Claude production CSS; packet and provenance
- [ ] Classic, twelve Taylor eras, twelve Newfoundland scenes; all secondary surfaces
- [x] Scene animation, accessible pause, reduced motion, Hercules accessories (local/source evidence; device proof remains below)
- [x] Local 320/390/720/1100/1440 destination matrix; focused functional/account tests; quick gate/build
- [ ] Complete expanded workflow/state visual and interaction inventory
- [ ] Actual bracelet/dress/ticket derivatives integrated; no placeholder completion claim
- [ ] Updated-head integration review; authenticated device/Safari proof explicitly distinguished

## Decisions

Jonathan explicitly requested full implementation of the plan in this task. Themes are per account across devices, photos are public bundled derivatives, lighting is authored (no light/dark setting), rich motion has pause/reduced-motion, and existing Hercules identity/wardrobe remain. No financial commands, schema changes, deployment or Production activation authorized by this work.

## Remaining inputs

Actual bracelet, dress, ticket and optional trip photographs have not been supplied. Their final integration remains required. Jonathan selected the installed Claude desktop app. Sent the scoped 2.7 MB CSS packet (source, reference styling, 20 synthetic screenshots, no credentials or household data) in the existing Mobile UX redesign brainstorm conversation. Claude returned worlds.css and CSS_COVERAGE.md through the desktop app; both are integrated with Codex audit corrections. Original downloaded ZIP remains local. Additional hook brief covered offscreen pause, chalk tokens and prepared memorabilia.

## Evidence log

Initial git status clean. GitHub main verified by git ls-remote. Created isolated worktree from origin/main. Dependency directory links to the existing current-main-compatible mobile workspace installation; no package changes.

## Handoff

Codex owns implementation and visual design. Claude owns production CSS unless Jonathan changes that instruction. No merge, deployment, hosted mutation or physical-device proof performed.

### Foundation review and proof

- Appearance tests: 19/19 passed, including real DOM-node/draft/focus preservation under StrictMode; transport identity rejection; namespace and partial metadata contract; offline cache and serial updates.
- Independent read-only review reproduced a loading-race overwrite of untouched metadata. Fixed by retaining allowlisted field-level pending intent and merging it into fresh account metadata before a PUT. Added transport regressions for pause-only and theme-only changes.
- Independent review caught the studio Auth binding. Replaced it with scene-only binding; the studio has no authenticated connection.
- Original SVG scene artwork includes JAG geometric walls/music corner, coastal approach, separate summit, Battery, Quidi Vidi, lighthouse, street scenes and twelve scrapbook materials/motifs. Canonical scene lighting/tokens applied at root. Supporting text colours numerically adjusted to >=4.6 against both scene paper and card.
- Hercules gets fallback bead accessories or a yellow sou’wester/neckerchief through the existing dress overlay; explicit equipment takes precedence. Saved Classic desk finishes remain available under Advanced Desk finish. Chalk repaint uses tokens without changing saved strokes.
- TypeScript passed after initial foundation edits; latest integration check and App regressions running.
- Main rechecked and remains 6fb15c7a98f3336862bb743b836aa96a358a35b9. Incoming mobile work is still separate; integration pending its publication.

### Claude CSS return

Received and integrated `worlds.css` (2410 lines) with original return preserved as `docs/briefs/CLAUDE_THREE_WORLDS_CSS_RETURN.md`. Input/source hash SHA-256 `bef0d7a15966229bc8b7afb1d28c920a5d08621d0b0e0da7bbab7aa11a43eac6`. Claude verified only four static specimens; complete App, responsive, accessibility and performance proof remains Codex-owned. Markup hooks for preview, status, memorabilia kinds and recovery class added.

### Integration audit and current verification

- 22 focused appearance tests passed, including numeric palette contrast, offline/account/environment isolation, serial field intent, stale response rejection and preserved input DOM node/value/focus under StrictMode.
- Codex corrected Claude CSS regressions before approval: fixed camera framing transparency; preserved Charter/onboarding fixed positioning; restored projected chart hatches and aligned legends/marks; reset print tokens; corrected rehearsal warning semantics, dark nested surfaces and calendar category selectors. Calendar heat now uses a theme token without changing its numeric meaning or user-owned identity colours.
- Scene copy and pause now use flow/grid geometry (minimum 200px desktop, 218px phone). Long era titles expand safely. Studio and App use the same scene-header layout. Phone crops anchor the landmark at the right edge. Picker cards now include actual authored Home scene art.
- Preserved Hercules facial ink, coat and spots across dark scenes; only automatic accessories follow the theme. Explicit wardrobe choices still win.
- First 180-scene capture (36 scope/route/theme cases at all five widths) had no horizontal page overflow. This is synthetic component proof, not actual App coverage. A later accessibility pass found seal opacity/ink, warning text and slicker-button contrast defects; Codex corrected these and is rerunning it.
- Quick gate: AI surface and TypeScript passed; 54 fast tests passed; serial lane passed 33/34, with the Bianca UI assertion missing its dialog after a fixed 180ms delay. Replaced that delay with the existing waitForUi helper, preserving both dialog and income-copy assertions. The isolated Bianca test then passed in 3.16s. Whole gate rerun remains required. Initial gate elapsed 443.4s, over its five-minute soft budget.
- Earlier Personal-refusal startup timeout was separately checked: the exact test passed against untouched main (18.92s) and this candidate (14.67s). It was not treated as a financial correctness pass until both reruns completed.
- Local Vite initially returned 403 for PGlite WASM/data because node_modules is shared by symlink. An ignored local config now allows that exact dependency directory. The real demo then reached a local transaction timeout; actual-App browser proof remains incomplete. No hosted ledger was used.
- Main still verified at the baseline SHA; incoming mobile integration remains pending publication. Required personal originals and authenticated Safari/physical-phone proof remain outstanding.

### Final destination pass and expanded proof

- Whole focused quick-gate rerun passed all 88 tests (54 fast plus 34 serial), AI surface and TypeScript. Elapsed 584.333s exceeded the five-minute soft budget; TypeScript 402.472s, serial lane 99.549s.
- Subsequent appearance tests passed 23/23, including Use theme retaining focus after apply and ignoring a repeated unchanged apply.
- Installed Chrome rendered all 36 destination compositions at all five specified widths (180 images), with zero horizontal page overflow, zero page JavaScript errors, and zero automated A/AA violations at 390px. These are local synthetic PGlite fallback results: normal worker startup timed out on this machine.
- Nested audit-office probing established that the demo's Shared Books destination shows its readiness gate. The destination tour must not be represented as complete Books/audit coverage. A separate deterministic completed-onboarding fixture is used for the nested audit proof.
- Production build passed before the separate companion addition. Claude CSS integration corrections include scope/button semantics, visible category labels, calendar heat contrast, scene-muted contrast on tinted surfaces, and legacy window atmosphere pause.
- Read-only review found no new remount/account/financial behavior regression. It explicitly retains all asset, mobile and coverage requirements.
- Main remains 6fb15c7. Incoming mobile reached a13c6c4 with additional uncommitted source-context work. Do not integrate that dirty checkout.
- Added Hercules Pro presentation bridge: fresh allowlisted account metadata on summon, account/environment isolation and safe Classic fallback, scoped Home scene control tokens, transparent canvas and existing rig untouched. Resource version advances to v6. No financial tool writer or permission changes. Existing HTTP tests require an unused LedgerRoom export mock because Node cannot import the Cloudflare platform module introduced on main.

### Companion, audit and motion follow-through

- Hercules Pro focused regressions passed 20/20. The initial two test files failed collection on main's unrelated platform-owned LedgerRoom export; their isolated HTTP harness now mocks only that unused export. Real OAuth, read tools, confirmation and CORS paths remain exercised.
- TypeScript and production build passed with the companion bridge and Books accessibility labels/scrollers. Existing PGlite eval and large-chunk warnings remain.
- Live Chrome atmosphere checks: Classic steam (1 animation), Showgirl (2) and Cape Spear (8) all have zero running decorative animations after pause, focused entry, offscreen and reduced motion. The checker excludes control transitions, which intentionally remain interactive.
- Audit screenshots first ran on a fixture without an accepted-books receipt and correctly showed recovery. The corrected local synthetic fixture is validated with assertAcceptableBooks and financialAuditHash before seeding its isolated cache; no hosted household is touched. The script waits for the real PGlite success note before capturing shared audit panes.
- Visible audit labels and keyboard-scrollable table regions fixed. Print now omits navigation tabs and supplies neutral status tokens. Filled validated data revealed a legacy selected-tab background clash; the explicit card/ink pair is corrected and the final 42-surface pass is running.
- Development preview queued in this task's browser panel. No current-main, mobile checkout, hosted state or deployment changed.

### Checkpoint result

The final completed-onboarding fixture pass has 42/42 audit surfaces, zero automated accessibility violations, zero page overflow and zero JavaScript errors. Six PDF samples pass print-token and hidden-scenery checks. Rendered balance-sheet output is legible and monochrome. The live atmosphere checks also pass.

**No-go for release:** real memorabilia, incoming mobile integration, the rest of the expanded-state inventory, performance against the integrated mobile baseline, Safari, normal-worker and authenticated physical-device proof remain open. No requirement is waived. Main is still 6fb15c7 at checkpoint verification.

### Explicit wardrobe choices

Choosing None originally left the theme's fallback accessory visible. Fixed with independently allowlisted account fields `hideThemeHat` and `hideThemeNeck`, a Wardrobe restore control and matching figure masks. Existing equipped items, outfit commands and rig remain unchanged. Added real SVG DOM and account-loading merge regressions. Final focused follow-up: **45/45 tests passed in 58.92s**, including 25 appearance and 20 companion tests. Native Safari control returned timeoutReached; no Safari proof claimed.

Final production build passed after the wardrobe fix: TypeScript, Vite and companion rig/asset validation. No ThemeStudio or reference stylesheet chunk is emitted in production. Existing large-chunk/dependency warnings remain.
