# Onboarding Slice 24 — Chapter 11 first-plan contract

Chapter 11 presents the deterministic proposal from D-221 and invokes the atomic adoption command from D-223. It does not create a second proposal, approval, or money boundary.

The surface shows both authored estimate sets without totals, ratios, ranks, or member comparisons; exact Shared recurrence anchors with current-month occurrence eligibility, count, input amount, computed floor, and source standing date; run-rate eligibility and its honest first-run absence; the frozen basis and integer-cent result for each category; and the total. `proposal.ts` exposes the already-used month-occurrence breakdown for this presentation, without changing its totals or formula semantics. Because Hearth has no accepted household-capacity fact, the surface names that absence and does not invent one.

Only a member's own estimate submission may be edited. The warning appears before the edit form. Submitting creates a new estimate revision and proposal digest; approvals against older digests remain auditable but do not authorize the current version. Each active member can approve only for themselves. Both current-digest approvals reveal a separate explicit adoption action.

A known pre-acceptance rejection says nothing changed and may offer an ordinary retry. An accepted-but-unsaved command outcome says recovery is required, never claims nothing changed, and withholds ordinary adoption retry so the existing recovery boundary can resolve the ambiguity.

Completion requires an accepted `adoptFirstBudget` receipt for the current civil month whose posted ids are the active budget-plan rows for that month. Chat cannot acknowledge or advance Chapter 11 without that evidence.

The component and projector import nothing from Ask, Ask routes, or Ask presentation, and show no work-time figure. No journal row, transaction, transfer, Fund event, Personal fact, schema, provider, hosted data, or Production setting is changed.
