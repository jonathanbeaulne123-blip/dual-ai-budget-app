# Onboarding Slice 25 — Chapter 12 Ready and unlock contract

Chapter 12 closes the household track on Books and Health. It reuses D-128's isolated correction Practice when the member has no eligible accepted entry; it does not create another ledger, acceptance boundary, or simulated financial record.

An eligible accepted transaction is shown through the existing privacy-safe evidence card. Reversed originals, reversals, opening rows, and duplicate-marked rows do not count. Otherwise the member enters and corrects one fictional `$45` grocery row entirely inside `runMonthRehearsalCorrectionPractice()`. Its typed proof must say it was fictional, balanced, discarded, and persisted no ids. It enters no Household collection, journal, PGlite accepted books, continuity envelope, dashboard/report, streak, Health result, audit hash, or accepted-money progress. Copying the result creates an id-less real draft that still requires ordinary review and Confirm.

The finale shows the current Books equation, Health finding count, and all twelve household chapters from `householdGatesOutstanding()`. Each active member records Chapter 12 proof in their member-Personal progress and appends only their own Shared Ready approval. Both approvals must name the same deterministic completion digest. That digest binds the household/onboarding cohort and the Shared setup facts established by Chapters 3–11, while excluding member-Personal evidence and routine post-setup ledger entries.

`completeHouseholdOnboarding` is the sole unlock command. It is accepted only when every household gate is satisfied and both active members have current Ready approvals for the exact digest. It changes the shared onboarding record from active to complete without posting money. Direct commands and accepted-write replay re-check the transition. A replica that receives the accepted complete record converges forward to unlocked; a complete record cannot coexist semantically with an active Hercules lock.

After unlock, the component names the starting truth that onboarding established, states that routine ledger inputs still remain, offers the optional Personal track, and opens ordinary Books. One approval shows a patient named waiting state. Busy controls are disabled and announced, known failures remain on the page with an alert, and an accepted-but-interrupted completion exposes an explicit Finish setup repair action.

This slice may modify the Ready kernel, onboarding progress/mode/evidence/approval command validation, Shared replay/runtime wiring, Chapter 12 routing, the dedicated Ready surface, onboarding copy/styles, focused tests, and living documents. It does not modify journals, the final financial Confirm boundary, proposal/adoption formulas, schema, providers, hosted data, Auth/RLS, secrets, Production settings, or Personal-track module behavior. No push, merge, or deployment is part of this implementation pass.
