# T4-S2 — Sill Four (Connect Four)

`kitchen.games.four`: 7×6, pine vs copper, turnMemberId, winner/draw, reset. `playFour(household, { memberId, column })`. Empty `postedIds`. Keyboard: Left/Right/Enter.

```text
Implement T4-S2 from docs/briefs/office/T4-S2-sill-four.md.

New kitchen.games.four: 7x6, pine vs copper, turnMemberId, winner/draw, reset command. playFour(household, { memberId, column }). Empty postedIds. shapeGames migration. Widget glance + body. Keyboard: Left/Right/Enter. Tests: win detection, wrong-member no-op, Add inert. Two-phone via existing kitchen sync. pnpm test.
```
