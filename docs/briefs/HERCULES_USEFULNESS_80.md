# Hercules usefulness 80 — remaining packets

**Status:** Cursor packet is on `cursor/hercules-usefulness-80-2a92` (D-112 / D-113 / D-097 first-tap-at-80). This file is the rest of the split.  
**Not permission to merge, deploy, or mutate production.**

80 usefulness means: on a loaded Development demo he scores ≥80 because leftover, claims, jars, presets, and a card statement are real jobs, **and** his mouth cannot swap Mastercard CAD onto a Visa question. It does not mean inventing Health findings.

## Already in this Cursor branch

- Grounded-figure clamp (phone + Worker prompt). Prompt echoes (`GROUNDED JOURNAL`, `FIGURES …`) and Visa-answered-as-Mastercard fall back to grounded speech.
- Usefulness scorer: leftover/claims/jars/presets/card/Health; no grocery nag; `openHelpOnTap` at 80.
- Tray vs statement owed on card questions.
- `Sit-down?` / `sit down` matches leftover.
- `who spent this week` is a shame skip.
- Home chips name the hottest card.
- **First tap at 80 opens How can I help** (`herculesTapIntent`). Yellow/red still beg. Add still loafs (`pointer-events: none`).
- Sit-down / leftover chips expand the postcard **and** leave the leftover sentence.
- Proposal/chat bubble avoids wallet, blotter, and accounts rects.

## GPT / Codex — coordinate and trust-review

1. Independent CAD/privacy review of D-112. Confirm FIGURES shrinking does not re-open D-105’s partner-personal aggregates (those still ride `ledgerLines` for language; they must not become quoteable dollars). Confirm prompt-echo fallback does not hide a real kitchen sentence that happens to mention “figures.”
2. Keep Phase 0 member-scoped disclosure as the next **trust** packet, not as a blocker for this 80. Canary every outbound field when that packet starts.
3. D-097 vs D-113 is reconciled in code: `herculesTapIntent` is the one writer. Do not open a second tap rewrite.
4. Merge order after Claude’s visual packet: this branch → disclosure projection.
5. Do not allocate D-112/D-113 to #63/#61. Those PRs still own reserved D-107–D-109.

**Acceptance:** written review that a Visa grounded line cannot be replaced by briefing `cardsOwedCad`; leftover formula untouched.

## Claude — viewport proof (tap wiring is done)

Cursor now owns first-tap and overlay avoid on this branch. Remaining Claude work is visual proof, not a second tap rewrite.

1. Confirm one-tap How can I help on the seeded demo; yellow/red still beg.
2. Confirm Add loaf + `pointer-events: none`. Proposal stays inert during Add.
3. Confirm the Tim Hortons / chat bubble does not cover blotter/wallet tiles; Not now still persists.
4. Attention ping follows `herculesNeedsCheck` (green desk or hiding/restless) — not missing milk.
5. Sit-down chip: postcard expands **and** leftover sentence stays visible.
6. Visual proof at 320 / 390 / 720 / ~1100, keyboard, reduced-motion, empty catalog (red, no grocery ping).

**Kill criterion unchanged:** if Bianca will not add a grocery because he is in the way, shrink him.

**Acceptance:** one-tap help on the seeded demo; milk still posts without moving him; overlay does not hide the Visa/Mastercard tiles while he talks about them.

## Out of scope for 80

- Auth/RLS, bank feeds, Worker KV bind, two-phone CAS.
- Changing leftover arithmetic (D-083).
- Letting Hercules `postEntry` / `postTransfer`.
- Member-scoped AI projection (Phase 0, Codex+Cursor later).
