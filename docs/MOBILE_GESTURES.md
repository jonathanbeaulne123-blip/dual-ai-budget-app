# Mobile gesture contract — 2026-09-07

Jonathan's phase-2 instruction retains Claude's instruments and exact authored style. Codex additions may support their data/input contracts inside those surfaces; competing layouts are not adopted. This map is G1's prerequisite for the sheet, spread and rails. G2's fortnight build gate was explicitly waived with “just build it”; physical use/tap measurements remain evidence to collect, not fabricated results.

| Surface | Vertical axis | Horizontal axis | Write / focus boundary |
|---|---|---|---|
| Home at rest, Shared or Personal | Ordinary document scroll; Ledge grip alone may capture vertical drag | None in A1–A3 | Grip changes detent only; explicit scope label; live shift > bills > goals |
| Calendar / Plan / More at rest | Document scroll; Ledge grip captures only its own hit region | Existing view controls; no added page-wide rail | Add keeps its current scope; Fund explicitly names Shared |
| Ledge Half | Grip changes height; common stage scrolls vertically | A later Turn/Reach handle owns its own narrow axis, never the whole stage | Slot-one Level is the same component as Full; background is inert |
| Ledge Full | Grip changes height; stage scrolls vertically | Stage handle only, where present | Six fixed slots in 2 columns × 3 rows; Arrange separate; one modal owner |
| Spread (A4) | Each page scrolls vertically | Horizontal page navigation and named index buttons | No page contains an interactive rail; Ledge expansion makes the spread inert |
| Add → Cut | Ordinary form scroll | Divider handle only | Percentage draft, exact setter and detents; separate existing Final Confirm |
| Shift → Count | Ordinary shift page scroll | Three named rails in one authored instrument | Explicit coordinated-three-input exception; exact edits are alternatives in the same reading; existing Confirm posts |
| Level → Turn | Stage scroll outside handle | Date read-head only | Reads one canonical walk; panel labels projection when crossing evidence |
| Level → Reach | Stage scroll outside handle | Integer candidate-count handle only | Named dated routes plus explicitly elected availability scenario; no drag posting |
| Level → Trust | Stage scroll | Three discrete Confirmed/Observed/Estimated buttons | Supported evidence policy only; accepted figure never changes; missing evidence refuses |
| Due → Weight | Ordinary date-list scroll | Due-date read-head only | Reveals existing status and explicit actions; due does not mean posted |
| Goal → Fill | Ordinary goal page scroll | Authored jar amount handle | Draft amount with exact alternative; existing funding Confirm |
| Mid-shift → Punch | Edge reveal only on its grip; document scroll elsewhere | No second competing gesture | Reveals a named clock/break action; crossing/releasing a detent never acts |
| Duplicate review → Prise | Ordinary review scroll | Card separation handle only | Reveals evidence; never changes computed similarity or marks a duplicate |
| Personal Shift / Books | Existing document and reading controls | No new global pager | Existing navigation remains; no inferred Shared funds from Personal totals |
| Onboarding return | Existing page scroll | None | Return instruction and Hercules sit above measured Ledge clearance |
| Add, Swipe, Confirm, guard, command sheet, onboarding invitation, charter takeover, Hercules focus | Existing modal's own scroll | Existing modal's own controls | Ledge is suppressed/collapsed before ownership changes; no two focus owners |

## Required implementation checks

Capture only on a visible grip/handle; release on up, cancel and lost capture. Drag pixels track directly and never drive accepted financial arithmetic. Settle is at most 240ms; reduced motion removes settle, not tracking. Every drag has the packet's named keyboard/tap or exact-input equivalent. Scope changes collapse the Ledge and invalidate its drag state. Enter cycles Rest → Half → Full → Rest; Escape closes. Nothing exists only at Half.

A page-wide horizontal pager containing a horizontal rail fails G1 even if a local unit test passes. Combined proof must drive page scrolling, grip dragging, stage controls, keyboard focus, cancel, scope change and existing overlays at 320×568 and 390px. Browser proof is synthetic; it is not the physical clock-out-to-Confirm measurement required for Count's G3 or the independent scenario trust review required for Reach/Trust's G4.

## Cut implementation proof (B1)

The named readings sit in one card above a reserved 44px divider lane so endpoint movement cannot clip names or cross printed cents. The visible boundary remains exact; only its invisible hit area stays inside the lane at endpoints. Grabbing preserves the starting percentage, including fractional exact input. Intentional movement snaps near the original six stops; arrows change one point and Home/End reach the bounds. Pointer cancellation and first Escape discard the preview; ordinary document scrolling remains outside the handle. Final Confirm and duplicate Add anyway are disabled until the preview ends. Existing percentage allocation drives both displayed and posted cents. [Worksession](worksessions/2026-09-08-mobile-cut.md).


### Count verification boundary (B2)

The three rails are the explicit exception already listed above. Native range interaction, exact typing and ordinary vertical page scroll share the existing Shift flow. Touch targets are44px; one active rail freezes its scale and disables the others and Next. Escape, pointer cancellation and unmount restore the original draft value; exact typing returns focus to its named reading. Envelope duration is locked. Every action edits a draft until the separate Confirm.

Actual component/command tap comparison: blank21→14; populated4→4. This is local synthetic evidence, not physical G3 certification. No under-ten claim for blank entry.
