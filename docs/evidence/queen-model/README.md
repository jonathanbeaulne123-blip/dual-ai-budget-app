# The Mandevilla Queen — browser evidence (D-266)

`HEARTH_CHROMIUM=<chromium> HEARTH_ARTIFACTS_DIR=docs/evidence/queen-model node test/queen-model-layout.mjs`

Fictional books only, reduced motion, SwiftShader WebGL:

- `model-<theme>-<state>-<w>x<h>.png`: the model standing in the Home world. The charms are `few` and the fixture has two rings, so the still words must say the charms are kept for her drawn figure.
- `model-refused-drawn-390x844.png`: requests for `/models/queen/**` are aborted, and her drawn figure stands with no note.
- `queen-model-layout.json`: page overflow, world and model state, frame count and the still words for each capture.
