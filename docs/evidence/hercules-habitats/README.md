# The Hercules habitats — evidence

Chromium (SwiftShader), the **actual App page** (`scripts/serve-queen-world-page-proof.mjs?habitat=well|hard`),
each habitat generated on the device by the same `generateDemoSuite` the Demo Suite panel runs (seed 41, today
= the page's date), then opened as the household. Produced by `test/habitat-layout.mjs`; 12 records in
`records.json`; `overflowX`/`overflowY` 0 in all 12; 0 page errors. Fictional Development books only.

| Set | What it shows |
|---|---|
| `well-home-*` | The Queen reads **building**: leaning in, the vine grown by four acts, one growth ring from the closed Chapter, a Move at her hands, four buds, three dated obligations at her feet. The tabs read *Hercules habitat · doing well* and *Bianca's fictional books*. |
| `well-cellar-*` | Eight bills on the rail; rent, the streaming and the gym already shards on their days; hydro, the phone and the transit pass full and waiting; two planned expenses frosted. The water stays above the mark all month; nothing cracked, no hammer out. |
| `well-loft-*` | Two open-mouthed Build goals on the ledge — *A trip to the shore* (funded) and *Kitchen renovation*. |
| `hard-home-*` | The Queen reads **needs-us** (*not yet covered*): the Fund needs a top-up before its next obligation. Hercules' bubble: *Toronto Hydro needs a payment…* |
| `hard-cellar-*` | Ten bills on the rail — *the cellar runs dry*: the water is copper under the mark from today and dry before the phone bill; hydro overdue with its hammer out, the gym overdue and **cracked**, three crept-in subscriptions as shards, the car-repair estimate frosted. |
| `hard-loft-*` | The same two goals, the shore barely funded. |

Both habitats keep a balanced trial balance and a clean deterministic health check, replay exactly from their seed,
and pass the Demo Suite's books verification (`test/habitat.test.ts`). The sandbox's "Validating the local
journal…" band at the top of every still is this container's PGlite, not the habitat.
