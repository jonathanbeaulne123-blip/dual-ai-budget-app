# The Still Queen — Household Home overhaul evidence

Captured on branch `claude/still-queen` from `origin/main@4b918b0` (#469) with `node test/queens-nest-layout.mjs` (Chromium, `reducedMotion: reduce` for every capture except the two motion checks). Every household is fictional: `planLifeFixture("household")` renamed "Fictional household", members "Alex (fictional)" and "Sam (fictional)", six fictional months of "Fictional rent" on the card (June swelled), a "Fictional date night" bill and a "Fictional trip to the shore" goal. No real books, no hosted rows.

`records.json` holds all 57 records: seven pulse seeds (`checking`, `needs-us`, `building`, `reset`, `grave`, `empty`, `win`) at 320×568, 320×700, 390×844, 720×900 and 1100×800 with the App's top bar, sync line and collapsed instruments stand-ins; stale and offline glaze; the expand at every width (three bank buttons on screen, none overlapping, she stays); the Protect peek and the cellar at every width (panel from the edge opposite the door at ≥720, the sheet below with her dimmed behind it, the same door again going in, the stair returning focus to the door you came through); the loft at 390 (entered by pulling the sheet past the threshold) and 1100 (entered by the same door again); the doors' reveal on a field tap at 320/390/1100 and the Move opening Together with its act; the motion context (doors rest at ≤0.1 opacity, she breathes on a 6s loop, the doors breathe in on a tap and fade again); reduced motion (no animation or transition, doors findable at 0.6); the keyboard walk (Together → her → Move → Status, 3px ring, Enter expands, Tab reaches Build, Enter opens the peek, Escape closes and returns focus, Escape collapses); forced colours; easy read.

Measured everywhere: 0 horizontal overflow, 0 vertical page scroll at every viewport including 320×568 with chrome, 0 targets under 44px, 0 serious/critical axe hits, 0 page errors. One note: at 320×568 with the App's chrome the sheet covers all but the top of her crown (frame 242px tall).

The PNGs here are a curated subset; regenerate the full set with `HEARTH_ARTIFACTS_DIR=.artifacts/still-queen node test/queens-nest-layout.mjs` (set `HEARTH_CHROMIUM` when the Playwright browser cache is older than the package expects).
