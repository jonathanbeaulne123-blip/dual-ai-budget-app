# The Queen's creation and history — evidence

Captured on branch `claude/queen-ceremony` from `origin/main@76ab15b` (#472, the Queen's charms) with `node test/queen-ceremony-layout.mjs` (Chromium 1194 from the container's Playwright cache with `--use-gl=angle --use-angle=swiftshader` for the WebGL sections and `--disable-3d-apis --disable-gpu` for the no-WebGL section; `HEARTH_CHROMIUM` pointed at it). Every household is fictional: `planLifeFixture("household")` renamed "Fictional household", members "Alex (fictional)" and "Sam (fictional)"; the rings are fictional Chapters opened and closed through the ordinary commands, the thrown form and the two turns are seeded through `guardQueenDesignSave`, the ten portraits are fictional stills for 2026–2035 seen on a fictional 2036-01-04. No real books, no hosted rows.

`records.json` holds 36 records. The full load (twelve rings, sixteen charms, thrown, a shelf of ten) at 320×568, 390×844, 720×900 and 1100×800 in the 3D path and the flat path, plus one ring at 390 and 1100: 0 horizontal overflow, 0 vertical page scroll, the ring count on the root, in the drawn figure (one arc per ring), in the sculpture and in the still's words; both wheel turns named in the still; 3–4 frames while nothing changes. The wheel by keyboard alone (3D at 1100 and 320, flat at 390): "Alex (fictional) pulls the form" → Tab reaches the wheel with a ≥3px ring → ArrowRight ×8 changes the belly and leaves the collar → "Hand the wheel to Sam (fictional)" by Enter → "Sam (fictional) opens the rim" → ArrowLeft ×8 changes the collar and keeps the belly → "Keep her" by Enter → "Thrown together", `data-thrown="true"`; no progress bar, no step counter, no skip; 0 serious/critical axe hits. Tipped by ArrowDown on her (3D at 1100/390/720, flat at 390/320): the underside with "A · S" and the date; the flat underside carries no charm; ArrowUp rights her. The shelf in Status (3D at 1100 and 320, flat at 390): ten portraits, ten years, no canvas, and the world's sculpture count is the same with ten portraits as with none (4 = her plus three banks). The light at four points (a February evening at 18:30, July noon, an October dawn at 07:00, December midnight) in 3D at 720 and flat at 390: key lamp 1.178 / 1.855 / 1.261 / 1.178 of a 1.9 maximum, key height 3 / 7.69 / 3.58 / 3, key colour #d8d9df / #fde2ba / #dddadb / #d2d8e3; the still reads "Glazed: the evidence is fresh" at every point; 0 frames while nothing changes. Forced colours (emulated) with easy read on the flat path: twelve rings as lines, the underside a shape, the shelf still reads. A Chromium with no WebGL: `auto` yields silently at every width with twelve rings and sixteen charms drawn, and tips over flat at 390.

Frame timings are from SwiftShader (CPU rendering) in the cloud container, not a phone, and are an upper bound on CPU cost rather than a GPU figure: steady-state frames 1.2–1.8 ms at the full load, first frames 520–580 ms at the full load versus 342–376 ms with one ring and no charms (shader compilation plus the charm kinds). In Node (jsdom, no GPU): sampling the 128-point profile with twelve rings 0.74 ms, rebuilding the lathe plus the three seam tubes on a wheel turn 9.1 ms (6,272 skirt vertices), settling one charm off a ring 0.22 ms, rendering a shelf of ten to markup 11.4 ms, one light reading 0.002 ms.

The PNGs here are a curated subset; regenerate the full set with `HEARTH_ARTIFACTS_DIR=.artifacts/queen-ceremony node test/queen-ceremony-layout.mjs`.
