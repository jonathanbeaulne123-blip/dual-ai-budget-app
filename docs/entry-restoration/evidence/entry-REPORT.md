# Final local entry verification

This is a synthetic React component harness using actual AddSlideshow and the complete main.tsx CSS chain, including Claude-authored entry-restoration-extension.css last. The exact source manifest identifies248inputs. Full-matrix source built2026-09-09T03:14:39.865Z; extension SHA25621aea145f3996de0a580a178ebe400f0ec3688df3657d1e6a68776ccf8a5b135.

## Scope

All four entry modes, three themes and320/390/719/1100/1440 viewport widths. Flow includes real keypad entry, category/account choices, Shift Clock in→Sign out→hours/sales/cash tips/card tips→account→review, More/Back, busy Close, resize/theme preservation and reopening a preserved form. Check both width and height of every visible button/input/select/textarea/summary against the project's44px target; this is stricter than relying on zero axe violations alone.

The harness passes the exact recovery JSX copied from App.tsx through AddSlideshow.recovery, replacing only state flags and Check entry status callback with synthetic values. It captures pending receipt, volatile browser storage and combined notices. Pending state disables entry controls; the receipt action remains clickable, and Close remains usable. `App-recovery-prop-source.txt` preserves the original source expression and was checked unchanged after the build. This verifies presentation and prop-level behavior, not the real App receipt authority or persistence pipeline.

The earlier packet's recovery screenshots meant reopening a retained harness draft. They did not contain the new App receipt notice. The current screenshot names distinguish `reopened-draft-review`, `pending-receipt-notice`, `volatile-storage-notice` and `combined-notices`.

## Opacity correction and current-source delta

The full-matrix bundle records disabled fieldset opacity0.55. This visibly faded review details. Parent corrected selector specificity while that immutable bundle was running. The separate `../entry-restoration-opacity-recheck/source-snapshot.json`, built2026-09-09T03:17:44.103Z, records current source after the correction. Its six theme×390/1440 busy/combined-notice specimens verify disabled fieldset opacity1, receipt clickability, Close and axe. Additional15expense-review stress cases cover all themes×all five widths with combined notices and text doubled.

Computed font size and pixel line-height are doubled; this is text-resizing emulation, not native browser zoom. At320px, the enlarged notice needs ordinary vertical scrolling to reach the receipt action. Automated clicking verifies reachability; no horizontal clipping is accepted. Reduced-motion contexts also record actual running animations.

## Proof limits

Synthetic household with zero transactions; no real money authority invoked. Parent form/clock/callback state is simulated. ShiftPreview is a fixed illustrative fixture, so this is not financial-calculation proof. No authenticated App reload/receipt retry, cloud sync, real devices, OS keyboards, VoiceOver or NVDA was tested. This matrix is local Chromium evidence only.

Claude's final comparison/review remains blocked by his usage quota. Recovered Claude-authored CSS and the local verification must not be represented as his completed final review.

See `measurements.json` for per-stage geometry, both-axis target sizes, tile radii, text, disabled states, fieldset opacity, focus and draft values; `summary.json` is generated when the full matrix finishes. The opacity delta has its own measurements and enlarged-text measurements. Original Claude packets and previous evidence remain unchanged.


## Completed totals

60/60 flows,711 screenshots,0failed flows,0targets below44px on either axis,0horizontal overflow,0runtime errors,0non-font network requests,0posts and0running reduced-motion animations. All60pending-receipt frames contain actually disabled fields; receipt callback and Close pass in all60. Final actual source SHA256 manifest:7db6f2a78fb3a62ae8f44ebedf5b5ef94712579a48b6474a2b93323edb109667.

Current opacity delta:21effective cases (6normal and15enlarged-text across all five widths),57screenshots,0axe violations,21/21receipt action and Close, fieldset opacity1. Two early Taylor wide text200 busy captures were rerun with an explicit await; `text200-verified-measurements.json` is the effective evidence. Current delta manifest SHA256:f04f35af4be94110497ccf89ae335d9732d9b9b312b8c82c082b38d5c1a549f5.

Verifier timing correction: 18 busy-only frames sampled before the requested React busy flag had committed and are excluded as proof of busy-state appearance. `excluded-premature-busy-frames.json` names these frames. The actual disabled pending-receipt frames and the explicit-wait delta provide the stated busy/disabled evidence. This timing issue did not reproduce as a product defect.
