# Synthetic visual evidence

These are rendered application components with synthetic parents, people and inert posting. They do not represent real household records or certify hosted/device behavior. Source manifests distinguish each snapshot.

- `hercules-*.png`: deliberately opened setup in every theme at390/1440. `hercules-summary.json` and report cover the15-width/theme matrix, closed zero footprint, focus containment/return, slot count and separately labeled opening latency/idle probes.
- `drawer-*.png`: repaired Office drawers in every theme at390/1440. `office-summary.json` covers114broad and27post-fix axe scans, layout, pointer collisions and current-breakpoint focus return.
- `entry-recovery-*.png`: exact App pending-receipt/volatile notice markup with synthetic state, and final corrected ancestor opacity. They prove component rendering and reachable controls. Mounted App startup regressions separately prove receipt/reload behavior.

The original Claude packet, source provenance and reported discrepancies are in the parent folder. Actual Claude’s final integrated approval remains open. Viewport/text emulation and axe do not prove an operating-system keyboard, physical rotation or a screen reader.

Final local High gate:701tests/62files passed in208.845seconds with TypeScript/AI/diff. The dirty-tree source fingerprint and exact selected files are in `high-gate.json`; the executed command is in `high-gate-command.txt`. Documentation/evidence packaging and trailing-whitespace cleanup afterward do not change runtime behavior.

`fund-storage-*.png` and `fund-storage-summary.json` cover18actual-component synthetic error cases: three Fund entry points in three themes at390/1440. No command is sent when confirmation storage fails. Keyboard Enter, role=alert,44px targets and zero axe/overflow passed.
