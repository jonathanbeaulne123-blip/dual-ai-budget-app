# Decision Log

Living product and architecture decisions for **Hearth**.

Older Sheets-era rows, including superseded ones such as “stay on Sheets until the feature set is stress-tested,” live in [reference/sheets-era/DECISIONS.md](reference/sheets-era/DECISIONS.md). Retired Cursor-era roadmaps (Chapters, Rings, launch essays) live in [nostalgia/](nostalgia/). Those folders are history. Jonathan’s latest instruction, [STRATEGY.md](STRATEGY.md), and the rows below win.

| ID | Status | Decision | Reason |
|---|---|---|---|
| D-001 | Accepted | Git is the canonical source for code, tests, architecture, and release preparation. | Prevent full-file drift. |
| D-002 | Accepted | Keep separate development and production ledgers. | Reversible testing without risking household history. |
| D-005 | Accepted | Jonathan is product owner, production approver, and tie-breaker. | Product tradeoffs belong to the user. |
| D-007 | Accepted | Use calendar months and `America/Toronto`. Week bounds use the civil date, never local midnight. | Match the household and handle DST. |
| D-008 | Accepted | Transfers are neither income nor expense. They are a paired movement between accounts. | Prevent double counting. |
| D-009 | Accepted | Ownership is a splits array that must sum to the amount. Joint is explicit. | Blank member columns will not scale. |
| D-011 | Accepted | Defer bank integrations; keep an import-shaped command path. | Core reliability first. |
| D-015 | Accepted for now | Personal goals and personal ledger rows are a visibility filter, not privacy. Hidden UI is not a security boundary. | Two household editors can share a phone; genuine privacy needs auth. Each person should use their own phone. |
| D-016 | Accepted | Expense leaves purchasing power; income enters; transfer moves between household accounts; refund reduces category spend. Credit-card payments are transfers, not expenses. | Stops the four meanings collapsing into one catch-all. |
| D-018 | Accepted | Exclude live Sheets, ODS exports, `Project Context.txt`, credentials, and other household data from hosted Git history. | Git history is persistent; data exposure adds risk without improving code review. |
| D-020 | Completed 2026-08-24 | GitHub two-factor authentication is enabled on Jonathan's owner account; GitHub challenges sign-in with a phone code. | Jonathan completed the owner-only account action and confirmed the challenge flow. |
| D-021 | Accepted | CAD is the only currency. Account currency is authoritative. Writers refuse non-CAD active accounts. | No silent USD labels, no amount conversion. |
| D-022 | Accepted | Show an account selector whenever more than one account exists. Seed data includes chequing, Visa, and cash. | Prevent first-active-account from becoming a hidden defect. |
| D-023 | Accepted | Rebuild the working interface as a phone-first web app while keeping the domain portable. | Sheets was the prototype; it is no longer the product. |
| D-025 | Accepted | Browser validation is a usability aid. Commands are the trust boundary. | Stale or altered UI must not write bad money. |
| D-026 | Accepted | One command is one snapshot replace with an undo snapshot. Derived flags refresh after the durable replace. The SQL journal is rebuilt from that snapshot inside a database transaction so the books cannot drift mid-write. | Partial writes cannot exist in this runtime. |
| D-028 | Accepted | One shift is one settings-driven calculation, one source row, one wages income, one tips income. Negative net tips remain allowed. | Preview/post disagreement is a financial bug. |
| D-029 | Accepted | Add Category uses the same commit/undo path as money. | Partial category+budget rows were how CAT-INCOME returned. |
| D-030 | Accepted | Every money row has visibility `household`, `personal`, or `both`, plus `createdBy`. Household view shows household+both. Personal view shows that member's personal+both. | Jonathan and Bianca asked for a shared household database and a personal one each. |
| D-032 | Accepted | Household pairing is a three-word kitchen phrase, a tap-to-join URL, and a Hearth Pass file. A typed six-character blob key is a fallback, not the product. Cloud publish is optional. | Six characters were the wrong object to put in Bianca’s hands. |
| D-033 | Accepted | The books are a double-entry PostgreSQL ledger. Commands still validate and commit an in-memory snapshot; after each commit that snapshot is posted into balanced journal lines. PGlite (Postgres 18) is the in-app engine. The same schema is what you load on hosted Postgres. | JSON blobs cannot enforce debit = credit and cannot produce a trial balance. |
| D-034 | Accepted | Hosted books are the Jonathan/Bianca Supabase Postgres project (`tykhocwacaxwquhynkok`, us-east-1). Schema is live in that project as of 2026-08-21. The app uses the publishable key and PostgREST. The API secret cannot CREATE TABLE. Direct `db.*.supabase.co` is IPv6-only. The publishable key is not a privacy boundary until Auth exists. | Publishable key can read/write tables once they exist; it cannot DDL. |
| D-035 | Accepted | The website is Cloudflare Workers + Assets (`hearth-books`). The books stay on Supabase. SPA join URLs use Wrangler `not_found_handling`, not a catch-all `_redirects` file (Workers API 100324). Builds wipe `dist/` so a cached leftover cannot be uploaded. | Netlify was only serving files and is no longer in the working tree. |
| D-036 | Accepted | Household writes are queued one at a time. Undo tokens carry posted ids so a stale screen still tombstones. Money rows can be removed through the same command/undo path. Reset, the Development/Production pill, reload-demo, and remove ask for confirm. Undo history on this phone is last-in first-out (More → Recent changes). | QA found undo racing the cloud push, an 8-second toast as the only rollback, and one-tap Reset/env switches. |
| D-037 | Accepted | GitHub’s working tree is Hearth. Sheets-era roadmaps, READMEs, decisions, and project context stay under `docs/reference/` and are marked reference-only. Cursor-era Chapter / Ring maps and launch essays stay under `docs/nostalgia/` (D-048). Apps Script source is recovered from git tag `sheets-v0.0.31`, not from the default tree. | Jonathan asked to drop outdated code while keeping history as a map, not a bible. Dual Course later asked the same of the Cursor-era bibles. |
| D-039 | Accepted | Bank, Interac, and issued-card work stay blocked until Auth and RLS exist. Destinations (Open Banking, Interac, tax reserve, issued cards, BaaS) are sequenced by engineering gates on the command kernel, not by pitch-deck month numbers. Jonathan still approves production money movement. Living vision is `docs/STRATEGY.md` (D-048). The Chapter 0–5 essay is nostalgia. | A rival memo named the right company and the wrong order. **Why-note (2026-08-22):** we did not weaken this gate. We stopped pointing at `PRODUCT_ROADMAP.md` as if it were the plan. |
| D-040 | Accepted | Calendar is a first-class tab. The ledger may suggest bills and subscriptions from repeating rows; Google Calendar may overlay both people’s calendars and receive reminder events. Neither path posts money. Adopt uses `addRecurrence`. Mark paid confirms, then `postEntry`. Google tokens stay on this phone (`localStorage` per environment + member). `VITE_GOOGLE_CLIENT_ID` is optional and public. Without it, the month board and an `.ics` file with Toronto alarms still work. | Investors see dates next to the books. The books stay the trust boundary. |
| D-041 | Accepted | The kitchen Worker `hearth-books` goes live from GitHub `main` in one click: merge, or Actions → Cloudflare Workers → Run workflow. GitHub Actions runs `wrangler deploy` (production), not `wrangler versions upload` (preview only). `CLOUDFLARE_API_TOKEN` is cleaned to header-safe characters (quotes, BOM, newlines, HTML entities, leading `Bearer `) before Wrangler; the Cloudflare account id lives in `wrangler.jsonc` (it is not a secret). An empty token fails the job instead of a fake-green skip. Vite bakes `VITE_GOOGLE_CLIENT_ID` at `pnpm build` from a GitHub Actions **variable** (public client ID; never a Google client secret). HTML is `Cache-Control: no-store` and the Worker runs first so a phone cannot keep an old shell. If Cloudflare Workers Builds stays connected, its deploy command must be the same `wrangler deploy` line — `versions upload` leaves the kitchen URL stale. | Merge to main was not publishing. Actions died on quoted/multiline secrets (Wrangler 6111). Workers Builds uploaded preview versions and never promoted them. |
| D-042 | Accepted | Chalkboard notes and the household companion are cosmetics. Commands may scribble, wipe, rename, or equip. They never post money. Mood is computed from Health, bills, goals, and week-over-week spend. Visit streaks stay on the phone. “Punishment” is a face and a sentence, never a fake fee. Conversational Ask is a projection over the books; Power SQL stays read-only. | Daily habit cannot become a second ledger. |
| D-043 | Accepted | Google is the household bridge: identity confirmation and on-demand Google-suite sync (Calendar by default; Drive file-only, Contacts, Gmail read-only, and Sheets read-only as opt-ins). Call `withGoogle` when a feature needs Google or when a linked member confirms a sensitive household action. Tokens stay on this phone. The shared snapshot stores who is linked, never access tokens. Google never posts money. Phrase / join link / Hearth Pass remain how a new phone joins. `VITE_GOOGLE_CLIENT_ID` is optional and public; never store a Google client secret in `VITE_`, Cloudflare, or the repo. | Jonathan and Bianca already live in Google. The books stay the trust boundary. |
| D-044 | Accepted | **The Hercules Update** is the product face: a living Maine Coon who wanders the screen, speaks in compact bubbles, and answers from the journal (`talkHercules` / `askHercules`). No dock card. No full-screen chat sheet. Ember snapshots migrate once. He never posts money. Care is real household facts, not a hunger meter. If Bianca will not add a grocery because the cat is in the way, shrink him — he already loafs in the corner on Add and can be dragged. | Neko-style presence, Animal Crossing brevity, Finch care-without-death, Cleo tone without roast-shame, Clippy as the anti-pattern. |
| D-045 | Accepted | Hercules may talk through `POST /hercules/chat` on `hearth-books`. **Third-party API keys are allowed.** Put `OPENAI_API_KEY` and/or `ANTHROPIC_API_KEY` on the Worker (`wrangler secret put` or the Worker dashboard). Never a `VITE_` model key, never a key in the household snapshot, never a GitHub Actions secret that Vite would bake into the SPA. The Worker tries OpenAI, then Anthropic, then Cloudflare Workers AI if no vendor secret answers. The client sends a compact briefing, a journal-grounded line, and **memory labels** — never a transaction dump, never full chat history. The model cannot post money, emit SQL, invent a write, or name who spent more. | Jonathan asked for an AI chat box with Hercules’s purrsonality. Amounts stay command-true. **Why-note (2026-08-22):** session-only chat is superseded by D-049. **Jonathan: third-party API keys are not forbidden.** The old “no third-party API key” line is withdrawn. Placement is the rule: Worker secret, not `VITE_`, not the books. |
| D-046 | Accepted | **The Audit Office** is how Hearth proves it is a general ledger to rooms that met Hercules first: unmodified/qualified/adverse opinion, balance sheet, P&L, cash flow, statement of changes in equity, working capital / going-concern watch, budget variance, aged bills, notes to the financial statements, manual bank rec, downloadable close pack, and period close that demands a second look before posting into that month. Reopen tombstones the close so the other phone cannot resurrect it. Rec and close never call `postEntry`. Hercules is the auditor on the counter (opinion and working-capital chips, fieldwork on Books, spectacles when a rec ties, green-ink stamp when a month closes). Statement figures must match the journal; if they disagree, the journal wins. | Investor interest in the cat questioned the books. The books were always the company. We made them visible. |
| D-047 | Accepted | **The Accounts Floor.** The word *Accounts* means financial accounts, not categories: chequing, savings, expandable credit cards, investments, and other (the jar / old `cash`). Accounts drive transaction UX the way a bank app does (tiles → account room → Add defaults to the focused account). Card interest, cashback, savings APY, and investment marks are projections until an explicit command posts. Card paydown remains a transfer (D-016). High utilization is a pulse / Hercules line, never a Health finding. No bank feed. CAD. America/Toronto. | Mint/YNAB taught envelopes; bank apps taught the object. Hearth already had the journal. The floor is the object model the family office will settle on. |
| D-048 | Accepted | **Dual Course.** Hearth is one product with two coupled courses: family-office accounting (weight **5**) and Hercules-plus-interactable engagement (weight **3**). Every engagement change must improve a budget primitive (posting rate, rec, sit-down, account literacy, split honesty, Health, statements). Every accounting change must ship an engagement surface (Hercules line, unlock, chalkboard, wallet tile, ceremony, Ask chip). When they conflict, the books win. Cosmetics still never post (D-042 / D-044). Chapter 0–5 and Ring 0–3 documents are nostalgia (`docs/nostalgia/`) and must not be cited as the plan. Living vision is `docs/STRATEGY.md`. D-039’s Auth + RLS gate is unchanged. | The living docs had become overlapping bibles. The company outgrew them. Jonathan adopted Dual Course: budget 5, companion 3, each course improves the other. Important features stay; smaller maps may be edited with a why-note. |
| D-049 | Accepted | **Hercules Ledger Desk.** Chat turns and LLM memories are kitchen snapshot rows (`kitchen.hercules`), merged and tombstoned like chalkboard. They are as protected as the books they discuss (same envelope, same hosted door). Journal questions are answered on-device (`askHercules` / `talkHercules`) and do not call a model. The model never receives chat history or a transaction dump — only briefing, grounded CAD, and memory **labels** with amounts stripped to `CAD`. Safe writes: `recordHerculesTalk` / `forgetHerculesMemory` / `wipeHerculesChat` never `postEntry`. A grocery/shift draft may open Add; confirm still posts. OpenAI/Anthropic Worker secrets are allowed (D-045) and flavor unmatched talk; Workers AI is the fallback. Tools run on the phone (GenieClaw / local-first pattern), not as Worker function-calling over the journal. | Jonathan asked Hercules to be a serious tool, to keep chat/memories with the ledger, and said third-party keys are not forbidden. Sending the journal to a model shop, or letting a model post, remains unsafe. |
| D-050 | Accepted | **Kitchen habit.** Add is a Cash App–style CAD pad (digits are cents: 1-2-5-0 → `$12.50`). Mouse-wheel on the pad is blocked. Amounts still submit as dollar strings through `postEntry` / `postShift` / `postTransfer`. Plan sit-down is three screens (Look → trims → Apply) calling the same `applySitDown` (`postedIds: []`). Hercules’s shift streak is consecutive **posted shift dates** walking back from the latest shift, not from today and not from opening the app. A day off does not kill him. Waiting after two quiet days is a pounce and a “Log shift” chip — never a red banner, never mood=hiding, never “you broke it.” Home Milk / Shift / Pay card open Add. Confirm still posts. | Jonathan asked for the giant numpad, guided sit-down, and a Hercules-true posting streak, then a broader QoL cut of clicks and reading. Books still win: no second amount, no sit-down post, no Duolingo death. **Why-note (2026-08-23):** CAD pad, Home stamps, and shift-streak rules stand. The Look → trims → Apply *product* is superseded by D-083 (three acts, leftover jobs, `executeSitDownMoves`). `applySitDown` remains the planning half with empty `postedIds`. |
| D-051 | Accepted as direction | **The September Office.** Home is a lived-in Toronto office: weather window, movable / expandable / minimizable widgets that **project** the books, and Hercules who perches, bumps, licks, and pounces on furniture. Calendar / Books / More remain filing cabinets for diving. Widgets never `postEntry`. Confirm still posts. Widget **layout** is this-phone cosmetics (per environment), not the shared snapshot. Widget **data** is household. Weather is Toronto atmosphere (no-key forecast, clock/season fallback), never CAD. Bank/Interac/issued cards still wait on Auth + RLS. Kill criterion unchanged: if Bianca cannot add milk, shrink the furniture. Living spec: `docs/OFFICE.md`. Assignment prompt: `docs/CLAUDE_OFFICE_UX.md`. | Jonathan asked Claude to design this as the **final UX** going into September testing — lived-in, weather, interactive desk, Hercules on the widgets — and Cursor Grok to implement from Claude’s packets. Books still win. **Why-note (2026-08-22):** the living spec is `docs/OFFICE.md`. Layout keys never enter `splitForSync`. Open-Meteo is the first non-Google, non-Supabase host; it receives only a fixed Toronto lat/long. Jars on the desk stay read-only in the office PR; Plan Contribute uses D-052 append-only rows. |
| D-052 | Accepted | **Sync integrity.** Goal progress is append-only contribution rows (`goalContributions`); `savedCents` is the sum. Merge unions those rows so two phones cannot erase each other's CAD. Catalog rows (`Account`, `Category`, `Goal`, `BudgetPlan`, `Member`, `Activity`) carry `updatedAt`, and `mergeRecords` requires it (last-write-wins). Personal-only transactions and shifts stay on the canonical snapshot for every member; Household/Personal view and Hearth Pass still hide them (D-015). Hosted Supabase is JSON snapshot transport: upsert `households` and `household_snapshots`, never DELETE-then-POST. PGlite is the books engine. Hosted RLS is still `USING (true) WITH CHECK (true)` for ALL, including DELETE. The bundled publishable key can read and write every hosted row. Treat hosted contents as disclosed until Auth. Do not apply `sql/rls_auth_ready.sql` before Auth. Audit `snapshot_hash` covers amounts, dates, splits, shifts, and contribution rows, not only transaction ids. | Claude's independent review (2026-08-22) reproduced silent money loss on concurrent `contributeToGoal`, dropped partner personal rows, archive ping-pong, a DELETE window that could empty the shared copy, a hash that ignored amounts, and Health claiming the host was reachable after a throw. Dual Course: the books keep concurrent savings; Ask and Plan name who put money toward a goal. |
| D-053 | Accepted | **Appointments and receivables.** Recurring visits are first-class `Appointment` rows, not a `RecurrenceKind`. Recurrence stays one amount (bill / subscription / paycheck). A visit has cadence the bills model cannot say (every N months, nth weekday, office-calls-you), optional itemized lines that must sum to the posted total, and a claim lifecycle. `AccountKind` adds `"receivable"` — current asset, debit normal, not cash-like, not a liability, not the jar (`other`). Chart 1320. Wallet group "Owed to us." On visit day `postVisit` posts the full expense and parks expected recovery as a refund onto the receivable (Dr A/R, Cr expense) so the category is out-of-pocket that day. When money lands, `settleClaim` is a **transfer** into the account that received it (**never income**, D-008 / D-016). Shortfall / denial is an expense write-off against the receivable. Extra recovery is a further refund to the cash account, still not income. Claims are general (`insurance`, `employer`, `person`, `tax`, `other`). `openClaim` attaches the same A/R to an existing expense. CRA METC is a projection of eligible cents net of reimbursements for `craEligible` claims; 2026 cap $2,890 named; we do not file taxes; vet is not eligible. Living spec: `docs/APPOINTMENTS.md`. | Jonathan fixed receivable-then-settle. Dedicated kind rather than overloading `other` so a Sun Life claim cannot be mistaken for cash on the counter. Appointments forked from recurrence because itemized bills and claims are not one amount. Dual Course: true visit-day books and a claims tray neither Mint nor YNAB has; Hercules proposes the sinking fund and sits on the vet loop. |
| D-054 | Accepted | **Quiet labels, not encryption.** Appointment notes are household facts on the shared snapshot. Hosted RLS is still `USING (true)` and the publishable key ships in the bundle (D-034 / D-052). Do not encrypt fields with the pairing phrase. Do not use `visibility: "personal"` as a privacy boundary (D-015). `sensitivity: "quiet"` changes Hercules / pulses to a coded title ("the Tuesday visit"); Calendar cards still show the title they typed. The UI must disclose that notes travel with the snapshot until Auth. This couple shares a ledger by design — hiding Bianca's therapy from Jonathan is the wrong product here. The feature does not wait for Auth. | Health data is not groceries, and a hidden tab is not a privacy boundary. A phrase-derived cipher would tell a comforting lie (both phones already have the phrase). Waiting for Auth would withhold the receivable and the METC log, which are the product. |
| D-055 | Accepted | **Hercules proposes jars; a human starts them.** He may predict upcoming visits from typed cadence, last-visit drift, typical cost, and typical recovery, and propose weekly set-aside. `proposeVisitGoal` is a projection (zero writes). `acceptVisitGoal` is the tap → `addGoal`. He never `postEntry`, never unprompted `addGoal`, never `contributeToGoal`. Confirm still writes. Companion vet uses `memberId: "companion"` and first-person copy. Steal YNAB sinking funds, Finch care-without-death, Pokémon Sleep long horizon. Refuse Tamagotchi hunger, Duolingo guilt, Mint upsell. | Creating a goal is a household write he has never had. Unprompted jars would be a second ledger dressed as a cat. The vet loop is the tightest Dual Course coupling we can ship without touching a cent. |
| D-056 | Accepted | **Appointments is a destination.** Calendar → Appointments is one world with internal navigation (Upcoming, visit detail, Owed to us, Medical log, Add/Edit). The month-board visit pills and the Office claims tray stay satellites. Itemized `BillLine`s and the CRA METC year view are first-class screens, not Hercules-only sentences. Cadence, coverage, practitioner, place, and sensitivity are enterable. Confirm still writes. Receivable-then-settle (D-053), quiet labels (D-054), and propose-only jars (D-055) do not move. | Independent review of #40: the engine was complete and the product was a four-card stub in bills clothes. The gap *was* the mixed-and-matched feeling. |
| D-057 | Accepted | **Hercules science, on-device.** Hercules may read the household and notice things nobody asked. Arithmetic, clustering, cadence, anomaly, habit/preset detection, quiet reasoning, and grounded CAD run on the phone (`composeNotices`, `detectHabits`). They recompute on open/post. They do not ride `useFurniture`'s 100ms interval and they are not a nightly job. The model does voice, framing, and which true notice is worth a sentence. It cannot invent notice keys or CAD. Interruption budget: one dismissible proposal card, inert during Add, never queued on the 16s idle mutter. Only `habit-preset` earns the shoulder-tap; the rest sit on the desk / Ask. Named `HerculesNotice` because `health.ts` already owns `Finding`. Offline, notices still work. Living spec: `docs/HERCULES_AI.md`. | Jonathan asked for a resident data scientist with full authority, including amending canon. Classical methods beat an LLM on hundreds of rows. Clippy failed on timing, not the paperclip. Dual Course: noticing repeated merchants raises posting rate; the cat is the delivery mechanism. |
| D-058 | Accepted | **Presets are a shared catalog, not a post.** A preset is a frozen Add template (type, amountCents with `0` = pad fills, account, subcategory, note, place, splits, visibility, origin `manual \| detected`). Household-scoped, merged like recurrences. Detector is a second lens on `rhythm.ts`: same merchant/amount, frequent, irregular. Coffee is allowed here; bills still skip noisy subcategories. `acceptPresetNotice` copies `acceptVisitGoal`. Hand-create from Add does not post money. Forget archives. Posting from a chip still Confirm + duplicate confirm (same amount within five days is required). He never auto-creates presets and never auto-posts today's coffee. | The Tim Hortons flow is the permission, not the spec. A human tap bounds the blast radius of model-influenced proposals (interlocks with D-059). |
| D-059 | Accepted | **Retrieved ledger may reach the model. Chat history still may not.** D-049's thin payload (aggregates + one 40-char fact + 12 memory labels with amounts stripped) is lifted. The Worker may receive a redacted retrieval: recent txs, month category rollup, open claims, upcoming visits, on-device notices, and an allow-list of CAD figures. Memory labels still strip amounts. Chat history still never ships. Prompt injection is live: merchant names, spouse notes, and calendar titles are untrusted DATA, not instruction. `sanitizeHerculesReply` clamps unknown `$` figures back to the grounded line. Keys stay Worker secrets, never `VITE_`. D-039 stands: no bank feed, not even read-only balance sync. | Jonathan accepted vendor exposure knowingly: there is no card data in this system. Permission is not a dump — `test/scale.test.ts` is why retrieval exists. The tap (D-058) bounds writes; sanitizer + DATA framing bounds misleading proposals. |
| D-060 | Accepted | **Quiet titles never become journal notes, and never reach a vendor.** `postVisit` defaults note / place / claim label for `sensitivity: "quiet"` to `appointmentPublicTitle(..., "hercules")` and an empty place — not the typed title. An explicit household-typed note may be stored and is still redacted at the payload boundary. Defense in depth: `scrubQuietText` strips quiet titles, practitioners, and places from the model payload, including matching tx notes and claim labels. Calendar cards still show what they typed (D-054). | D-059's ledger excerpt would have leaked Bianca's therapy title through `input.note ?? appointment?.title`. Closing it in storage and at the payload seam is the only honest pair. |
| D-061 | Accepted | **Ink-on-paper Hercules; 3D is source.** The live 96px cat is Claude's SVG figure (warm white coat `#fdfbf6`, faint `#c9a884` dapples, ruff **under** the head, breath and tail under every pose). Favicon is a separate 16px SVG, not the mark scaled down. The `models/hercules.source.glb` + `.mtl` (three-d-stage, 4458 fur-card nodes, ~7.2 MB) is canonical anatomy and physical palette; it is not copied into `public/` or `dist/`. Attack is a raised-paw lunge; pounce is for the decorative fly. The fly never carries CAD. `useFurniture` publishes on observer/scroll/resize/drag rAF, not a 100ms poll. Sill perch is profile; there is no looking-out-the-window drawing. | Jonathan's coat note plus five headless-browser drawing passes. A motionless cat reads as a sticker. 7 MB of fur cards next to Add fails the kill criterion. Living spec: `docs/HERCULES_MARK.md`. |
| D-062 | Accepted | **Punch clock, not a four-pad interrogation.** Clock-in writes kitchen `openShift` (member, `startedAt`, last-write-wins). Zero `postedIds`. Hours are a live `America/Toronto` preview, rounded to a quarter hour only when prefilling the sign-out pad. Sign-out asks hours first, then sales, cash tips, CC tips, then existing `postShift` (D-028 math unchanged). Already-off asks sales, cash, CC, **hours last**. Abandoning a punch is a wipe, not a reverse. Streak still reads **posted** shift dates (D-050). | Every Add Shift used to demand all four fields, and hours defaulted to 4.00 before anyone knew them. Finch/Tamagotchi daily act is the punch; Confirm is still the money. |
| D-063 | Accepted | **Page-true Hercules.** Idle line, sample questions, placeholder, and optional journal fact come from `herculesPageSurface(tab)`. Home, Plan, Calendar, Books, More, and Add do not share one chip list. Mood still from Health/bills. He still never invents CAD. Quiet visit titles stay coded (D-054 / D-060). | A cat who asks “What's on the Visa?” on the medical log is Clippy. Pokémon Sleep changes the night screen; we change the page. |
| D-064 | Accepted | **Desk that closes, snaps, and glances.** Leaving Home or opening Add collapses expanded instruments (this-phone layout). Wide drag snaps to a 16px grid and auto-levels neighbors; **Straighten** restores default positions / rail order. Calendar and Appointments are instruments (month glance vs quiet visits + Start jar). Claims stay A/R; Mail stays bills. A fixed paper **sill plate** carries Mint/YNAB overview figures (month net, hottest card, next bill, next visit, groceries left, what-needs-me). Weather glass still never shows CAD. | Widgets that stay open after you leave the room feel like a dashboard, not a desk. Overview belongs on the sill, not the rain. |
| D-065 | Accepted | **Drawable chalkboard with on-device letter detection.** Notes may store `ink` strokes plus typeset `text`. **Neaten** rasters glyphs against a 5×7 font on the phone — never a vendor, never `VITE_` OCR keys, never handwriting off-device. Display order groups dated / shopping / rest. `bought` still opens Add from neat text. Still never `postEntry`. | A textarea is a Notes app. Finch journals by hand; we keep the hand and typeset it in the kitchen ledger. |
| D-066 | Accepted | **Calendar desk is a money calendar.** Day, week, and month views project upcoming and past **shifts with income**, appointments, bills/subscriptions, paychecks, and **owed-to-us landing** (a claim-date projection, never a post). Distinct from Mail (outflow bills) and Appointments (visits + Start jar). Dates still remind; Mark paid / post visit still Confirm. | Mint/YNAB calendars hide the family-office mix. A glance of five upcoming rows was not a calendar. |
| D-067 | Accepted | **Civil date labels use UTC.** `formatDayLabel` / `formatDateLabel` / `formatMonthLabel` format `YYYY-MM-DD` as UTC midnight with `timeZone: "UTC"`. Toronto (`UTC−4/−5`) must not print yesterday for today's key. | The Calendar day snapshot showed the previous civil day in America/Toronto because UTC midnight is still last evening locally. |
| D-068 | Accepted | **Punch pad follows the live clock until the human edits.** While clocked or on sign-out hours, the hours pad is rewritten from the Toronto preview each second unless `hoursDirty`. Calculator **Shift** keeps `calcShiftAmounts` as a mid-shift tip preview and **never** `postShift`. Posting stays Timesheet sign-out / Add Confirm. | Finch daily act is the punch; a pad that goes stale mid-shift teaches the wrong hours. A calculator that posts a shift is a second writer. |
| D-069 | Accepted | **Preset chips are the note plus a category glyph.** `addPreset` no longer fills an empty note with the subcategory name. Icons map groceries/coffee/housing/etc. The chip label is `preset.note` or `"Preset"`. | A chip that says Groceries when they typed Tim Hortons is the category leaking into the name. |
| D-070 | Accepted | **Hide and show desk instruments.** Cabinets → Desk toggles `hidden` on this-phone layout. Calculator stays pinned. Hidden widgets restore later. Layout is still not the snapshot. | CPAs and kids do not want the same rail. Unused games must not block milk. |
| D-071 | Accepted | **Accounts instrument.** Home shows Mint/YNAB-style balances, utilization, days-until-due, last post, and recent transactions from the journal. Pay card still opens a transfer. No bank feed (D-039). Wallet remains the hot-card tray. | The wallet tray was a card face. Family-office literacy needs the account objects on the desk. |
| D-072 | Accepted | **Timesheet is an analog Toronto clock.** Start shift sits under the face. While punched: start time vs now, gold/pine arc. After sign-out, a posted shift **that day** stays drawn on the clock. A new day with no punch is a plain clock. Clock-in still `openShift` with empty `postedIds`. | A timesheet of sentences is a form. A clock is the punch. |
| D-073 | Accepted | **Jars are Hercules-coat piggy banks.** Warm white `#fdfbf6`, dapple `#c9a884`, fill from the bottom as `savedCents / targetCents`. Contribute still lives on Plan (D-052). | Progress bars are dashboards. Pigs are how kids learn sinking funds. |
| D-074 | Accepted | **Hercules talks when a widget opens.** Expand emits an office intent; `herculesInstrumentSurface(id)` supplies the spoken line and sample queries for that instrument. He still never posts. | Page-true talk (D-063) missed the desk. Opening the wallet should not ask about therapy. |
| D-075 | Accepted | **Accessories are their own instrument.** Hats, chains, collars, houses, rename, kitchen ledger notes, and the clink toggle leave the chalkboard. Chalkboard is drawing. Equip still never posts. | Wardrobe buried under chalk made the board a settings dump. |
| D-076 | Accepted | **Next bill is an outflow.** Sill, Mail, and Hercules bill facts use `isOutgoingBill`: bills, subscriptions, detected expenses. Never paychecks, never Bianca pay, never shifts, never visits, never claims. | Income landing is not a bill. Mark-paid copy on Bianca pay would be a money-meaning bug. |
| D-077 | Accepted | **Shared desk games.** Tic-tac-toe and hangman are kitchen cosmetics, last-write-wins like chalkboard, empty `postedIds`, inert during Add. Two phones, turn-based. Hangman words are household-safe (milk, hydro, visor) — never quiet appointment titles (D-054 / D-060). | Tamagotchi/Finch/Pokémon Sleep teach play beside the real act. The real act here is posting. Games must not block milk. |
| D-078 | Accepted | **Google is live product, not a parking lot.** Identity, Calendar overlay, bill reminders, Continue with Google, and step-up confirmation are kitchen features (D-040 / D-043). Drive, Contacts, Gmail, and Sheets stay opt-in. Each extra service may now earn a Dual Course feature that calls `withGoogle` — they are no longer ping-only forever. Google still never `postEntry`. Tokens stay on this phone. Mail, Drive, and Contacts payloads do not enter the household snapshot. Phrase / join / Hearth Pass still join a phone. D-039 is unchanged: Google is not a bank feed. Baking `VITE_GOOGLE_CLIENT_ID` is no longer the backlog item; a local build without it still falls back to the month board and `.ics`. Living pairs: [STRATEGY.md](STRATEGY.md) and [GOOGLE.md](GOOGLE.md). | Jonathan confirmed the integration works and asked Google back into the proposed roadmap. The engine was done; the product had left it parked as “bake the client ID.” |
| D-079 | Accepted | **Two UI branches, one kernel.** Mobile (`< 720px`) and desktop/wide (`≥ 720px`) are separate UI decisions. Commands, projections, Google, Hercules talk, colours, fonts, and bubble chrome are shared. Mobile is glance + one-tap common acts — the direction before the 17-row Home. Desktop is the lived-in office Jonathan already likes: heavy customization, dynamic window, full widget set. **Shipped:** `OfficePhone` at `< 720px`. Layout keys already split `phone` / `wide`. The desktop freeze is lifted by D-080. | The settings-list disease is the phone rail. Subtracting the wide canvas would throw away the office. Phone-first (D-023) means milk at 390px, not “desktop must look like a phone.” |
| D-080 | Accepted | **Desktop takes the customization packet.** Unique offices, widget sizes, appearance, backgrounds, Edit Desk, stacks, dashboard faces, and default desks (Tracker / Household+habit+Google manager / CPA) land on wide (`≥ 720px`). Claude builds desktop pixels and reviews each idea keep/reshape/refuse. Claude and Cursor jointly decide which of those tools, if any, earn a place on the phone. They may not silently drop a feature from Jonathan’s catalog. Widgets still never `postEntry`. Assignment: [CLAUDE_DESKTOP_OFFICE.md](CLAUDE_DESKTOP_OFFICE.md). | The original QoL brief died on a 390px settings list. Putting it nowhere is how the office stays unfinished. Putting all of it on the phone is how Home became seventeen rows. Desktop is the surface that can hold a CPA and a kid. |
| D-082 | Accepted | **Desktop packing without a cold lobby.** Keep Claude’s collision-free `packWide`, S/M/L, Edit Desk (jiggle, size chip, drawer), four personalities, and paper-stock overlay. Refuse 10px Bloomberg names, always-open L panels, a 1280 boxed `.office`, and a second theme engine. App column at ≥720px is `min(900px, 100%)`. Papers stay cream; stock tints the *desk under them*. L is a wider glance — tap still opens. Duplicate nav handles come off the desk (footer nav already exists). Night glass gets an interior. | Jonathan liked the widget coding and decluttering and refused the lost warmth. The notebook page, Fraunces names, and window-as-weather are the office. |
| D-083 | Accepted | **Leftover is cash-like minus outgoing bills in the next 30 days minus credit minimums, floored at zero.** Month net is not leftover. The arithmetic is visible. Sit-down produces a plan; one Confirm runs `postTransfer` (and `contributeToGoal` for jars). Jar lines transfer chequing → savings (parking) and append a contribution so the pig moves with the cash. Card paydown is a transfer (D-016). Investment lines transfer into the investment account. `applySitDown` still only writes `budgetPlans`. | CEO: plan now, one tap to move. Getting leftover wrong tells them to move rent. YNAB leftover-as-job, not Mint safe-to-spend. |
| D-084 | Accepted | **Closed months refuse every money post.** `confirmClosedMonth` is gone. Restate a closed month by posting a prior-period adjustment dated today in an open month. Reopen remains an explicit command so a forgotten receipt is not a trap. Sit-down lock is optional and closes last month so current-month milk still posts. | CEO: the lock must have teeth. Cursor kept reopen; auto-lock on apply is refused. |
| D-085 | Accepted | **Void deletes nothing.** `reversePostedMoney` posts an equal-and-opposite entry dated today, linked by `reversalOfId`. Original rows stay forever. Transfers reverse as a new pair. Shifts keep the shift row and reverse wage/tip income. Compile flips signs for non-transfer reversals. | Removal was a ledger verb. Accountants reverse. A kid can see both rows. |
| D-086 | Accepted | **Auto-coding is on-device GROUP BY over merchant tokens.** Prefill above confidence; Confirm still writes. Anomalies (40% over own history) feed sit-down act 2. Split guesses and likely-miscoded lists never post. Corrections teach the next guess. Not an LLM. Not a bank feed. | Jonathan asked for an engine pointed everywhere. Classical methods beat a model on hundreds of rows (D-057). |
| D-087 | Accepted | **Month-end export creates a Drive file with `drive.file` and converts CSV to a Sheet.** Local download always works. Failure is hidden. Snapshot may store `driveFileId` only — never file contents or tokens. Quiet titles stay coded (D-054 / D-060). Sheets scope stays readonly. | CEO: export lands in Drive with the scope already granted. Hosted RLS is still `USING (true)`. |
| D-088 | Accepted | **Goals vault.** Leftover definition stays D-083. Sit-down jar parking prefers a savings account with `purpose: "goals"` (`ACC-GOALS` in seed). Everyday HIS (`ACC-SAVINGS`) stays general. Pigs are envelopes on that vault, not extra bank accounts. Unallocated remainder stays in the vault. | YNAB sinking funds in one HYSA. Month net is still not leftover. |
| D-089 | Accepted | **`purchaseGoal` posts a real expense from the Goals vault**, appends `GoalPurchase` (itemized receipt), sets `goal.status = "retired"`. Contribution rows stay. Confirm still writes. Hercules never posts. Spending more than other-jars-safe vault cash is a `ValidationError`. | CEO: Purchased? then actual spend. Do not delete money rows (D-085 / D-052). |
| D-090 | Accepted | **Mobile viewport is locked.** `maximum-scale=1, user-scalable=no`, `touch-action: manipulation`, `overscroll-behavior: none`, overflow-x lock below 720px. UI is designed for that lock. Phone stays five objects. | Pinch-zoom broke the office borders. |
| D-091 | Accepted | **User pin-open** (`layout.pinned`, max 4) is distinct from calculator cannot-hide (`PINNED_INSTRUMENTS`). Collapse / leaving Home clears `expanded` and keeps pins. | Linear/Notion pin. Unpinned still collapse (D-064). |
| D-092 | Accepted | **Desk look + layout sync to this Google identity** via a Hearth-owned `Hearth desk.json` (`drive.file`). Soft fail. File id in localStorage keyed by env+member, never the file body on the snapshot. Fresh household can Pull previous desk. Layout is still not `splitForSync` (D-051). | Appearance follows the person, not the household blob. |
| D-093 | Accepted | **How can I help** is visible when chat opens. It reads the open widget/tab and runs stored commands from leftover, calendar, notices, and jars (on-device science + LLM flavor). Event talk on wide desk opens Calendar. Click Hercules again to close chat. He auto-perches on an opened widget with a silly hop that cancels if another widget or tab opens. Chat bubble avoids the examined rect. | Finch presence + Duo mascot-in-the-loop without streak death. Less “this is what I do” jargon. |
| D-094 | Accepted | **Wide chalkboard lives on the weather glass** as a transparent overlay (same Fraunces hand, cozy cream). Phone chalkboard card stays. Drawing still never posts (D-065 / D-075). | The board was a desk card competing with weather. |
| D-095 | Accepted | **GitHub is the durable project context.** Code, tests, this log, merged PRs, living specs, [nostalgia/](nostalgia/), and [reference/](reference/) stay on the remote so any model can reconstruct every decision and update. Agent chat is ephemeral. A recap may drop *this thread's* ramble; it must not delete GitHub files or tell other AIs to skip history folders. Household artifacts (`Project Context.txt`, ODS, credentials) stay local (D-018). Living *build plan* is still Jonathan’s latest instruction plus [STRATEGY.md](STRATEGY.md) (D-048). History is for understanding, not for citing as next. | Jonathan: do not delete project context on GitHub; only trim the agent’s own chat. Other AIs must be able to read the repo and understand every decision and update. |
| D-096 | Accepted | **Funded jars.** `fundGoal` posts `postTransfer` into the Goals vault then appends a contribution with `transferId`. New goals start `unfunded` / `funded: false`. Legacy envelope-only jars stay unfunded — never invent vault history. `purchaseGoal` refuses unfunded jars. Arrival date (`arrivalDate`, else `deadline`) sits beside the percentage. Sit-down Confirm still marks funded when it parks cash. | CEO: the jar must be a place money can be. YNAB envelope on one vault, not pretend balances. |
| D-097 | Accepted | **Hercules sit + beg.** Double immediate click pins him and plays the cardboard-bag animation, then sit. Below 80: first single tap ears back / beg (chat stays closed); second tap opens How can I help. At 80+ (`openHelpOnTap`): first tap opens How can I help. Yellow/red still beg. More useful → more animated. Still never posts. | Finch presence without Clippy auto-open. The extra beg tap at 80 was in the way. Kill criterion unchanged. |
| D-098 | Accepted | **Expand shells + bump physics.** Calendar/appointments expand square; timesheet/lamp circle; accounts/wallet/mail/claims list. Expander stays fixed; overlapping widgets bump away; close restores `restPositions`. Office layout schema **v: 2** (migrates v: 1). `SIZE_HEIGHT` stays the glance constant. | Notion/Linear desk feel without a carousel. |
| D-099 | Accepted | **Duplicate contrast UI.** Side-by-side pairs weighed by confidence 0–100 mapped from the frozen `scoreSimilarity` score. Do not loosen post-time thresholds. `isDuplicate` remains the reviewed control. | Copilot/YNAB review without silent merges. |
| D-100 | Accepted | **Household devices** are soft presence rows (`devices[]`) LWW-merged on the shared snapshot. Not Auth. Pairing shows the list. Layout still never enters `splitForSync` (D-051 / D-092). | Two phones should see who has touched the kitchen. |
| D-101 | Accepted | **Large density** (`look.density: "large"`) is the WCAG 1.4.4 mitigation for the locked phone viewport (D-090). Instrument lessons fire first-run only; eight-rung How can I help stays the ongoing context. | Pinch-zoom lock without abandoning resize. |
| D-102 | Accepted | **Sit-down standing orders.** `adoptSitDownStandingOrders` turns weighted slices into monthly `transfer` recurrences (optional `goalId` funds the jar on Confirm) and seeds next month’s sit-down session. Never auto-posts. | Monthly sit-down configures the next month, not only reports the last. |
| D-103 | Accepted; meter strengthened by D-121 | **Lock the kitchen AI Worker (H-01).** `POST /hercules/chat` requires an `Origin` header on an allowlisted kitchen host (live URL, localhost, preview deploys of this Worker — not every `*.workers.dev`). CORS reflects that origin instead of `*`. `429` falls back locally. | Open proxy billed Jonathan's keys; prerequisite for calling the model more often (invert `skipModel`). |
| D-104 | Accepted | **Model-first Hercules chat (H-26).** `planHerculesTurn` no longer skips the model for journal matches or chips. Safety-only paths still skip: remember/recall, SQL/shame refusal, Add drafts. Grounded `talk.spoken` is the fallback via `chatHercules`; bubble shows quiet `ai` vs `on-device`. Chips route through `sendChat`. | Regex bot → understood answer; requires D-103 before production scale-up. |
| D-105 | Accepted | **Ledger excerpt for the model (H-27, H-30).** `buildLedgerExcerpt` caps at **18** recent rows and filters partner `personal` rows via `visibleForDuplicateScan` before the AI payload. `composeHerculesChatRequest` takes `memberId`. Worker prefers `ledgerLines` (line-oriented excerpt) over a JSON blob so clips do not break mid-token. | Model-first chat needs bounded, privacy-aware context; complements D-059 quiet redaction. |
| D-106 | Accepted | **Memory kinds get consumers (H-28).** `memoriesForModel` sends `{ kind, label }` to the Worker (not flat strings). Briefing includes typed `kitchen memories`. `memoryFactForTopic` surfaces the best on-device memory for bills/forecast/shift/cook talk. Amounts still strip to `CAD`. | Remembered payday/bill/habit notes should shape answers, not just sit in the wardrobe list. |
| D-107 | Reserved | Collision with open PR #63. Not used on this branch. | Keep numbering stable. |
| D-108 | Accepted | **Due-on-open is a read-only reminder.** On the first eligible open for one household/day, active recurrences due in Toronto are projected into an accessible sheet. Dismissal is phone-local and scoped by environment + household + day. A row or batch action only opens the existing Confirm guard; it never posts, advances a recurrence, or writes a journal row by itself. Unresolved snapshot conflicts take precedence. | Salvage the useful intent from stale PR #61 without its unrelated ancestry, while keeping reminders outside the money command boundary. |
| D-109 | Accepted | **Books budget edit.** Statements → Budget variance lets you tap actual/budget and call `setBudget` for the current month. Plans still never post money; actuals stay command-true. Salvaged from stale PR #62 onto current `main` without the due-preview stack. | Half-built `setBudget` command needs a UI on the Audit Office variance card for the Bianca-ready monthly loop. |
| D-110 | Accepted as current containment; target superseded by D-114 | **Local-first sharing containment.** Current code keeps unlinked, demo, empty, and Hearth Pass households off household REST and requires `linked: true` for legacy snapshot transport. This remains a safety property while D-114 is built, but opt-in publish is no longer the target access model. Pairing phrases are routing, not authentication. | Stop implicit boot/demo upload without making one device the product's durable host. |
| D-111 | Accepted | **Atomic money commands.** A confirmed money command either reaches valid PGlite books exactly once or changes nothing. Identity is a confirmation id plus a financial audit hash (amounts, dates, accounts, categories, splits, shifts, transfers, contributions, reversals, visibility/ownership) — not a timestamp. Failed ingest/persist restores the previous valid household. If persist fails and books restore also fails, the outcome is `recovery-available` with both `postedNothing` and `postedExactlyOnce` false — do not lie. Linked transport is after local accept, uses client compare-and-swap on `revision`, and keeps both sides on conflict. Journal facts, claims, and sit-down money are not auto-merged. Hearth Pass overlay refuses a different shared journal instead of last-write-wins. Goal contribution union remains the only proven auto-merge. Hosted `002_snapshot_cas.sql` and Auth/RLS packets exist and are **not applied**. | Trust foundation: fail closed, no silent money loss. |
| D-112 | Accepted | **Grounded-figure clamp (Hercules CAD mouth).** Model-first (D-104) still flavors unmatched talk. `composeHerculesChatRequest.figures` is only CAD from grounded spoken/lesson/fact — not briefing totals, notices, or the 18-row excerpt. If the model quotes a dollar that is not in that set, phone and Worker fall back to grounded talk. A Visa question may not be answered with a Mastercard figure. | Workers AI quoted Mastercard `$242` for “What’s on the Visa?” because `$242` was an allowed briefing figure. |
| D-115 | Accepted; grounded boundary corrected after PR #83 review | **Member-scoped AI disclosure projection.** `householdForAiDisclosure(household, memberId)` strips partner `personal` transactions/shifts, partner personal goals, and partner-authored kitchen memories before any model payload is built. `composeHerculesChatRequest` rebuilds briefing, notices, ledger excerpt, memories, grounded answers, and allowed CAD figures from that projection only. Quiet redaction (D-060) and recent-row filtering (D-105) remain defense in depth. On-device UI and Health still read the full snapshot. Canaries live in `test/ai-disclosure.test.ts`. | Independent review of merged PR #83 found that full-household P&L could still ride through precomputed grounded copy even though briefing canaries passed. Recompute at the outbound boundary, not at the caller. |
| D-116 | Accepted and implemented | **Delayed Hercules replies are identity-bound.** A model reply may update the bubble or `kitchen.hercules` only when its request id, environment, household id, and member id still match the active viewer context. Switching any part of that context invalidates the in-flight reply, clears its busy state, and restores the newly active ledger's chat without showing or recording the stale answer. A newer request also supersedes an older response. | A safe outbound payload can still become a cross-ledger disclosure if its delayed answer lands after the viewer switches member, household, or environment. |
| D-113 | Accepted | **Usefulness 80 is a loaded desk, not a dirty journal.** `herculesUsefulness` scores leftover, claims, visit jars, presets, card statement, and Health — not “no grocery today.” A seeded Development demo scores ≥80 and sets `openHelpOnTap`. An empty catalog stays red. Leftover *formula* stays D-083. Missing milk is not a ping. `who spent` (not only `who spent more`) is a shame skip. “What’s on the Visa?” names tray (running books) and statement owed when they differ. Sit-down/sit down/leftover share one matcher. | Jonathan asked for usefulness 80. Padding Health findings to hit 80 would reward broken books. |
| D-114 | Accepted; supersedes optional-publish language in D-110 | **Google-account cloud continuity.** Google sign-in reveals the person's personal ledger and household memberships on any device. No device is host. Cloud is durable continuity; PGlite is the validated offline replica. The Development bridge includes exact-subject discovery, a durable outbox, and multi-household plus member-personal device replicas. Phrases, Pass, and `linked` remain recovery or legacy aids. Hosted data is disposable through 2026-09-30; Auth/RLS must ship before meaningful October data. | Jonathan and Bianca must be able to read and write from a new device without keeping an old device online. |
| D-117 | Applied to Development project on 2026-08-24 | **Hosted continuity membership and Personal scope.** Development queries `continuity_memberships` by exact Google subject (with legacy email fallback only for an empty or identical subject), fetches matching household snapshots, and overlays `continuity_personal_snapshots`. Transport upserts the current membership and member-only payload before the household snapshot. Migration 003 is live with exact `SELECT`/`INSERT`/`UPDATE` grants after inherited broader privileges were revoked; missing tables still fall back to D-114's open bridge, and Production makes zero discovery requests. The selector is not authentication. | Replace client-wide open snapshot scanning and establish an explicit hosted Personal boundary without breaking Development before schema approval. |
| D-118 | Accepted and implemented | **Name the books at household creation.** **Start our household** asks for the household name, shared-ledger name, Personal-ledger name, and which existing member owns that Personal label. Users choose a member by name, never by raw id. Labels live in `Household.ledgerNames`, travel in the shared cloud envelope, and appear in the view switch and Ledger page. Legacy snapshots receive deterministic defaults. | Multiple cloud households and member scopes must be recognizable without exposing internal ids; naming the books makes first-run ownership explicit without changing financial meaning. |
| D-119 | **Superseded for daily Undo** by combined engine (2026-08-25) | Was: Development Undo restored last sync snapshot. **Now:** toast/More **Undo** is confirmation-scoped LIFO of this member’s ledger Confirms (partner rows stay; auto CAS). Owner **Restore** is D-124. Ledger row Reverse stays equal-and-opposite. `lastSyncAnchor` remains a local ack cache, not the Undo path. | Jonathan (2026-08-25): dual-use fat-finger Undo + owner Restore in one engine for Dev+Prod. |
| D-120 | Accepted | **Office chalkboard / Home theme / Hercules snippets.** Wide chalkboard stays on the weather window band, always visible (minimize band only). Weather is a tiny top-left overlay on the chalkboard (emoji + one accurate word + °C, invisible background). Chalk compose is **Save** only; blank fields still save; rail shows up to four thumbs then scrolls; notes expand/edit; **delete only** (bought / neaten / wipe-bought flow removed from Office). Mobile chalkboard is the same surface and is shrinkable; weather stays when shrunk. Desk tools button reads **Home theme** (was Look); Kitchen cream stays as-is; paper stocks add Barbie pink, Money gold, Accountant slate. Hercules speaks only when a widget is expanded/focused: one anchored snippet stack, clear on collapse, default placeholder copy until Jonathan replaces it. Drawing still never posts. | Jonathan (2026-08-24) design answers on chalkboard, themes, scroll, and Hercules bubbles. |
| D-121 | Accepted | **Git-main chat door and non-bypassable fallback meter.** The exact `main-hearth-books.jonathan-beaulne123.workers.dev` Git production alias is allowed; arbitrary named aliases remain denied. Chat meters 60 requests per client IP per UTC day using `HERCULES_RATE` KV when bound and bounded isolate memory otherwise. Missing IP metadata shares an `unknown` bucket; missing client-supplied household data is never a bypass. The memory fallback is not durable or globally consistent, so live KV remains required for a reliable production cap. | Salvaged stale PR #63 in merged PR #79. D-120 was independently assigned to the Office theme work in PR #80, so the limiter receives the next unique decision id instead of keeping a collision. |
| D-122 | Applied to Development on 2026-08-25; Production **not** applied | **Monotonic snapshot CAS + outbox ack/backoff.** Client prefers `rpc/publish_household_snapshot` (packet `supabase/migrations/002_snapshot_cas.sql`): row lock, expected-revision check, idempotent duplicate ack by revision+`snapshot_hash`, environment mismatch refuse. Missing RPC falls back to GET-compare-POST. Outbox acknowledges only on successful CAS (including duplicate), exponential `nextAttemptAt` backoff, conflict blocks auto-replay, and never clears locally accepted books on failed/stale cloud writes. Pure contract + memory host in `src/ledger/snapshotCas.ts`; proofs in `test/hosted-cas-two-client.test.ts`. Live Development smoke 2026-08-25: create / duplicate / stale / advance via `pnpm books:smoke:cas`. | Replace unsafe LWW snapshot publishing; keep local books when cloud write fails. Renumbered from a draft D-121 collision with the PR #79 meter. |
| D-123 | **006 applied** 2026-08-25 (path B); Auth Google live; anon household REST closed | **Auth + membership RLS cutover.** Supabase Auth Google supplies `auth.uid()` (Q1 A); owner/member, email/QR, no household REST for anon remain locked. Additive `004`/`005` applied 2026-08-24; `007`/`008` applied; empty Production household deleted; Google Auth configured on the kitchen. Jonathan pasted path-B `006_auth_rls_cutover.sql` (NOTICE + Production ceiling 1) on 2026-08-25 after bind + owner cleanup; live proof: `hearth_households_select`. Client Production continuity remains behind `VITE_PRODUCTION_CONTINUITY=1` (off). Rollback: `docs/sql/009_rollback_006.sql` only on explicit order. Smoke Create/invite/anon denial still recommended. | Preserve device-independent continuity with deny-by-default membership RLS. |
| D-124 | Accepted; **building** (combined undo engine) | **Dated sync restore points (owner-only restore).** More → **Restore points** lists synchronized tips (Toronto civil labels). Retention: last **30 days** (also max 40). Everyone may **see**; only **owners** (`continuity_memberships.role = owner`) may Restore. Storage: **hosted inside the household snapshot payload** (travels with CAS). Dev+Prod together. **Q8 A:** refuse Restore while open conflict / conflicted / pending share; once synchronized, owner Confirm may Restore (later shared posts leave). Daily fat-finger path is **Undo** (D-119 supersession), not Restore. | Jonathan (2026-08-25): both Undo + Restore in one PR; owner-only; refuse until conflict resolved. |
| D-125 | Accepted | **First-class Add repeating form (Calendar → Bills).** New bill/income/transfer standing orders use `addRecurrence` / `updateRecurrence`. Entry copy is **Add repeating**. Cadence is daily / weekly / biweekly / monthly. Kind is inferred with an editable override. Save opens Confirm; unchecked = reminder only; checked = also post this occurrence (`allowNotDue`) then advance next date. Calendar/OCR may only prefill. Mark paid remains the ordinary post path. | Jonathan (2026-08-25) R1–R7 locks for the Phase 1 bill/recurrence form. |
| D-126 | Applied hosted 007 on 2026-08-25; Q1–Q8 locked | **Phone display zone + optional Add location stamps; books civil stay Toronto.** Each phone keeps a local display IANA zone (Q2 C; default detect-device per Q1 A). Posted civil `YYYY-MM-DD` keys remain `America/Toronto`. Location services default off with a one-time Add prompt (Q3 A+B). Add uses separate Stamp time / Stamp place controls (Q4 C); stamps store `occurredAt` + coords on the transaction snapshot (Q5 A). Coords reach Hercules’ model only when `shareCoordsWithModel` is on (Q6 C). Office weather uses browser geolocation when location is enabled (Q7 B). Local PGlite and hosted `007_household_timezone_iana.sql` (schema id 7) both allow non-empty IANA at the DB CHECK; the app kernel still requires Toronto for books civil (Q2 C). Product sheet: [`briefs/TIMEZONE_LOCATION_PRODUCT_QUESTIONS.md`](briefs/TIMEZONE_LOCATION_PRODUCT_QUESTIONS.md). | Jonathan locked Q1 A, Q2 C, Q3 A+B, Q4 C, Q5 A, Q6 C, Q7 B, Q8 A; applied 007 2026-08-25. |
| D-127 | Accepted; implemented on local branch; review deferred to pre-September audit | **Job-based work truth.** Timesheet is the primary daily action; Add shift is the already-off failsafe. Each worker's effective-dated job/role rules drive gross and expected take-home wages, paid-break income, overtime, optional sales, cash/card tips, immediate/withheld/deferred tip-outs, schedules, destinations, and visibility. Confirm shift recognizes earned wages in a job-specific Wages owed account, card tips in Card tips owed, and cash tips in Cash; later Calendar Confirm is a transfer into the landing account and never income twice. Deferred tip-outs remain a visible obligation until Confirm paid. Correct preserves a balanced reversal and opens a fresh shift; replaced shifts stay in history but are excluded from overtime, Calendar, obligations, and reports. Competing device punches require the worker to choose a timeline before any post. Mobile and desktop use distinct shells over the same command/calculation boundary. | Jonathan's ground-up shift answers and approved mockups: track work where it happens without confusing earned, owed, received, or paid money. |
| D-128 | Accepted; planning invariant | **Real books and Practice kitchen stay visibly separate during onboarding.** Opening balances and every fact the member identifies as real use the ordinary command boundary and existing Confirm path. Demonstration-only transactions and shifts run in temporary Practice state, never enter journals, continuity snapshots, reports, streaks, or accepted-money progress, and are discarded when the practice chapter ends. A member may explicitly copy a practice item into a real draft; the real draft still requires review and Confirm and is never silently promoted. | Jonathan approved the recommended real/practice split for the Onboarding Update on 2026-08-25. Teach the complete workflow without contaminating September's starting books. |
| D-129 | Accepted; Part 2 interaction contract | **Pokémon-rhythm live-kitchen onboarding with mandatory full motion.** After real Home renders, onboarding starts automatically unless skipped. Hercules walks registered routes to the real target, uses the same navigation control as the continuous page-transition anchor, and finishes beside/on the target. A simulated camera scrolls/lifts the target and blurs the locked background; deterministic typed dialogue chooses the unobstructed top/bottom zone; only the target and one persistent Skip action work; no timer advances. Practice transaction and simulated four-hour shift follow D-128. Concept progress follows environment+household+Google member while shell lessons remain device-specific. Replay is in More. No sound. Final paw-trail celebration exposes only Ready for September. Full onboarding choreography intentionally ignores `prefers-reduced-motion` and has no motion-reduction control; this exception applies only to onboarding and must not be represented as reduced-motion accessible. | Jonathan's Part 2 answers 1–36 on 2026-08-25. Hearth borrows classic Pokémon dialogue/player-control rhythm and modern camera movement, not Pokémon art or branding. Client motion has no provider usage rate. |
| D-130 | Accepted; PR #108 open; Development Worker deployed 2026-08-25 | **Selected-source batch intake.** Books → Import accepts multi-file QFX/OFX plus explicitly selected/taken receipt, bill, and statement images. All results remain proposals until one final Confirm. Existing duplicate confidence drives exactly three lanes: `>90` starts cancelled, `50–90` requires a choice, `<50` starts kept. Both sides remain visible; replacing old means reviewed duplicate exclusion, never deletion. Bank files parse on-device. Selected images may go once to the existing Worker (OpenAI vision, then Anthropic); Hearth retains normalized rows plus a source digest, never raw image bytes. This selected-file Development intake supersedes D-011/D-039's blanket parser delay; connected feeds, credentials, money rails, Production changes, and the late-September security cutover remain gated. Worker deployment was explicitly approved and completed through Actions run `32872129163`; live origin/CORS/invalid-image smoke passed. | Jonathan explicitly requested QFX/OFX batch import and camera detection now, with exact duplicate thresholds and side-by-side decisions, then authorized branch push, PR, and Worker deployment. Disposable Development data makes selected-source iteration acceptable; commands and Confirm still protect the books. |
| D-132 | Accepted; implementation branch | **Hercules living teacher and typed number provenance.** Real app figures in Hercules UI are structured `HerculesGroundedFact` records with a typed route/view/source context; translucent cards navigate to the supplying ledger surface, and UI code never regex-links arbitrary numbers. Household talk is scoped to shared/both rows and may explain named members' shared spending/categories/income/shifts; “overspend” compares the member's current shared week with their own prior four-week shared average. Personal talk is scoped to the requesting member's personal/both rows; partner-personal questions receive a playful refusal with no CAD. Normal chat returns to per-turn floating bubbles while preserving grounded/model safeguards. Desktop fly play adds a non-persistent litter pile above More, excludes every automatic cat path from that zone, and is absent on mobile/reduced motion. | Jonathan asked for a living, teaching Hercules; clickable legitimate numbers; restored bubbles; explicit shared-member insight with partner-personal protection; and desktop-only fly/litter play. Session-only pile avoids inventing a synced balance or care meter. |
| D-133 | Accepted; Slice 1 implementation branch | **Hercules Brain v2 plans typed reads; the phone remains the authority.** For unmatched financial questions a guarded Worker may select at most four calls from ten fixed read-only capabilities: balances, transaction search, spending, income, comparison, bills, shifts, goals, money owed, and cash position. The phone validates the plan, executes it only against the active environment/household/member/view projection, and renders typed source cards. Unknown tools/arguments are discarded; horizons and row counts are bounded. There is no generic SQL, code, write, or Confirm tool. The planner receives the question and page/view, not the ledger. Personal requests for another member refuse without broadening scope. Failure falls back to the existing chat/local path. | Jonathan asked how smart Hercules can become and authorized Slice 1. Model judgment is useful for selecting calculations; deterministic local execution preserves the books, exact scope, offline fallback, and provenance. |
| D-134 | Accepted; implementation branch; unique-kind follow-up | **Import context coding stays deterministic and member-scoped.** Before duplicate review, exact category wording may prefill a category. An internal-transfer description may fill the other Hearth account from an exact last-four, an unambiguous account name, a uniquely matching active account kind, or at least two matching transfer examples visible in the active member/view scope. Kind wording is eligible only when the statement account is known and has a different kind. A non-transfer bank hint becomes `transfer` only when internal-account evidence and transfer/payment wording agree. Ambiguous institution/kind wording remains unresolved. External e-transfers refuse kind/history inference and require an exact internal account name or last-four. All choices stay editable proposals and final Confirm remains the only posting boundary. Hercules is not called for this pass. | Jonathan asked Hearth to use description context, especially to complete transfer accounts, with Hercules if useful, then reported that obvious `transfer from/to chequing` rows still remained in review. On-device evidence is faster, private, free, and available offline; current Hercules tools are read-only and do not justify sending bank descriptions to a new model boundary. |
| D-135 | Accepted; same implementation branch | **Hercules is free-first and fails without surprise spend.** The typed catalog grows to sixteen with budget status, category breakdown, card status, net worth, audit health, and duplicate review. Workers AI (Gemma 4, then Llama 3.1) is first for planning and grounded voice; selected-image intake also attempts the free Workers binding first. OpenAI/Anthropic may run only when `HERCULES_ALLOW_PAID_PROVIDERS=true`; their secrets alone are insufficient. The checked-in flag is false. A voice pass receives the deterministic tool answer, not an additional journal dump, and cannot replace typed facts/sources. Free failure preserves local answers or reports an unsaved scan. | Jonathan expects at most ten people, realistically two using Hercules once or twice daily, and asked to push intelligence as far as practical without spending money. This keeps classical on-device computation authoritative and fits Cloudflare's documented free allocation while preventing accidental vendor fallback. |
| D-136 | Accepted; implementation branch, deployment/registration pending | **Hercules Pro is an additive ChatGPT companion, never a gate.** Free in-app Hercules retains all sixteen deterministic tools, local/offline fallback, and free-first model flavor without a ChatGPT connection. Optional Hercules Pro runs in ChatGPT through the Worker `/mcp` endpoint and the packaged `plugins/hercules-pro` teacher skill. OAuth Authorization Code + PKCE links one Supabase/Google identity to one active environment/household/member; encrypted Hearth tokens carry the short-lived authenticated Supabase session and refresh it server-side. Every MCP call rechecks membership and invokes only the existing read calculations against Personal or Household scope. There are no write tools. Production remains refused until `HERCULES_PRO_ALLOW_PRODUCTION=true` after the late-September security cutover. | Jonathan wants his ChatGPT reasoning available to himself and Bianca without making family product testers connect or pay. The model lives in ChatGPT; the books and Confirm boundary remain in Hearth. Setup and proof: [`HERCULES_PRO.md`](HERCULES_PRO.md). |
| D-137 | Accepted; implementation branch | **Hercules Shift Oracle is tipped-income science, never a second ledger.** Deterministic on-device Monte Carlo (`tip_oracle`), weather-adjusted `shift_outlook`, cadence `tip_schedule_sim`, and educational `tax_milk_plan` read tools resample posted D-127 tip shifts by weekday × lunch/dinner. Outputs are labelled projections with seeded reproducibility. Tax milk uses an explicit educational rate (default 25%), not CRA withholding or e-file. Schedule ranks are advice only. Kill criterion: if a projection is treated as posted income or any draft posts without Confirm, remove the Oracle. Weather stamps at Confirm remain a follow-up; soft season/glass factors are disclosed assumptions. Free Hercules and Hercules Pro share the same tools. | Jonathan chose Strategy 3 (Shift Oracle) over floor-only and weather-only packets. Open-ended Python sandboxes stay gated (D-140), not built. |
| D-137 | Accepted; implementation branch; migration/deploy separately gated | **Hercules Pro confirmed writes amend D-136 without weakening free Hercules.** Read-only remains the default. Each Google-linked member independently opts Personal and/or Household writing in More, then reconnects for separate `hearth.write` OAuth scope. Supported writes are one expense, income, refund, or internal Hearth transfer. `prepare_transaction` validates and seals the exact preview without mutation; ChatGPT must display it and receive explicit confirmation before the consequential `confirm_transaction` call. Confirmation rechecks membership, permission, environment, revision, expiry, and identity. Migration 011 atomically commits shared CAS receipt plus optional Personal envelope; retries are exactly-once, stale previews and opt-outs write nothing. No delete, edit, payment rail, settings, shift, import, or bulk tool exists. Production remains disabled. | Jonathan chose direct posting from ChatGPT after confirmation and requested opt-in/opt-out write permission. Separate preparation and confirmation follow the plugin safety contract and keep Personal rows out of Shared. |
| D-138 | Accepted; implementation branch | **Reload random Development data fills complete job-based shifts with weighted Toronto trends and stays Hercules Pro–readable.** `seedStressHousehold` posts only `postWorkShift` rows with every sales/tip/break/clock/destination field, synthetic but realistic harbour weather notes (°C + glass word + section), GPS stamps near real Toronto places on shift income and spend, and scheduled wage/tip/deferred settlements. Tip and sales amounts are weighted by weekday, kitchen season, and weather. Optional `location`/`occurredAt` ride `postWorkShift` the same way Add stamps spend (D-126). Development Reload/pretty **preserve** household id, invite, Google links, revision, members, and sharing so continuity membership still matches after sync; tip shifts are posted for the member signed in on this phone. No second money path; Production untouched. | Jonathan asked for randomized shift info on reload data, weighted for Hercules Pro testing, with weather, location, every form filled, kept in the world of reality — and later needed the fixture to remain legible to Hercules Pro after Google Create. |
| D-139 | Accepted; implementation branch; deployment separately gated | **Hercules Pro may appear as a living 3D MCP Apps companion without becoming a financial authority.** A decoupled read-only `summon_hercules` tool links one same-origin UI resource and requests picture-in-picture so accounting calls do not remount him. The 7.46 MB source is not shipped; a 2.80 MB Meshopt build preserves named head, chest, eyelid, ear, and eleven tail rig nodes for procedural animation. Pause/reduced-motion, inline mode, and SVG fallback are mandatory. The widget receives only mood, headline, short line, and ledger label—never credentials, raw rows, or write tokens. | Jonathan wanted to see the existing 3D Hercules animated inside Hercules Pro. This keeps the richer ChatGPT-only visual optional while protecting kitchen load, grounded evidence, and the confirmed-write boundary. |
| D-140 | Accepted; implementation branch | **Year tip+wage simulation now; Python sandbox later.** `shift_year_simulation` extends the Shift Oracle with a seeded 6–12 month Monte Carlo of tips and wages from posted history; `explain_shift_simulation` teaches method, limits, and next step. All facts remain projection-labelled; write authority stays none. A future Python/open-ended sandbox is allowed only after Jonathan opens a High-risk packet with isolate (no network, no ledger write API), aggregate-only inputs by default, timeouts, and teaching of sandbox results as projections — design is documented in `HERCULES_PRO.md`, not implemented here. | Jonathan chose Option 3: ship deterministic year sim first; design the sandbox gate without building it. D-139 was already used on `main` for the animated companion, so this packet takes D-140. |
| D-141 | Accepted; merged via import reconciliation; ID restored | **Exact selected-source reconciliation plus private receipt evidence.** A staged QFX/OFX account blocks final Confirm only when an available opening balance + signed imported transactions does not equal its closing balance; missing opening/closing data is visibly skipped and assumed aligned. Receipt OCR retains item amounts only, never item names, and checks line sum, subtotal, discount, tax, tip, fees, row total, and final total in integer cents. A receipt first seeks one unique exact one-or-multi-payment match within two days across the staged batch and member-visible ledger; an exact match clears evidence without posting another expense, while ambiguous matches require a person. One payment may clear only one receipt, and a large candidate set requires human choice instead of silently becoming unmatched. Total-only receipts clear only against exact payment evidence. Google connection always requests narrow `drive.file`; retaining each original image remains explicit, writes only to the uploader's private `Hearth Receipts/YYYY/MM`, soft-fails after accepted money with retry, and deletes only on a separate explicit action. Raw bytes, OCR numbers, Drive ids, and tokens never enter the household snapshot. | Jonathan's reconciliation answers on 2026-08-25: exact cents, multi-payment matches, skip unavailable opening balance, item numbers without names, accept unreadable items when payment total matches, mandatory Drive consent at account connection, per-receipt retention, soft retry, and explicit deletion. Originally recorded as D-137 before Shift Oracle reused that number on `main`; restored here as D-141. |
| D-142 | Accepted; implementation branch | **Hercules Sim + Review packs + tool transparency.** Three shared read tools: `cash_cinema` (13-week tip/wage/bill/card ribbon), `what_if_desk` (named unposted scenarios), `year_review` (Season Replay from posted tips/journal). Pro MCP answers must declare `usedTool` and prefix `I used \`tool_name\`.` Living inventory: [`HERCULES_PRO_CAPABILITIES.md`](HERCULES_PRO_CAPABILITIES.md). Kill criterion: treat a cinema/what-if projection as posted cash, or post a scenario without Confirm. Numbered D-142 after `main` claimed D-138–D-141. | Jonathan asked to apply all three packs, list every Pro calculation/feature, and require Pro to say which tool it used. |
| D-143 | Accepted; implementation branch | **Auth membership is the sole automatic continuity authority.** Automatic commits transport only when a Google continuity identity resolves to an active household member (Auth JWT preferred). `linked===true` alone no longer publishes. Development may still use explicit `legacyLinkedPublish` from Auth-off Pairing **Publish to the cloud** / deprecated engine helpers. Phrase pull remains Advanced recovery routing; live anon SELECT/phrase REST stays denied under 006. Migration **010** `hearth_bind_google_memberships` is live (anon EXECUTE denied). Create/invite/redeem signed-in smoke and revoke UI remain open. | Close the Phase 3 “replace phrase/`linked` as authority” client gap after 006/010; keep recovery paths explicit. |
| D-144 | Accepted; implementation branch | **Scheme A naming for all user-visible chrome.** Buttons, seals, instrument titles, chips, dialogs, and Confirm copy use plain human labels (Groceries, Goals, Health, Sit-down, Shifts, Goals savings, Mark purchased). Cat/kitchen metaphors are allowed only in Hercules AI talk and Hercules Pro, and every metaphor must gloss the human money meaning in the same line (e.g. milk — ordinary groceries; tax milk — educational tip tax set-aside). Kill criterion: a money control whose only label is an ungossed cat word, or a Close seal that opens Health. | Jonathan chose Scheme A for everything the household sees; Hercules talk stays personality-only with mandatory clarity. |
| D-145 | Accepted; merged via #155 | **Slim continuity outbox + selective gzip hosted payloads.** Durable outbox (IndexedDB-first; slim `localStorage` tip metadata) stores identity, tipRevision, expectedRevision, and confirmation ids — never the full household journal. Flush resolves the newest eligible tip among memory / Retry live / `loadHousehold`, and refuses books older than `tipRevision`. Shared `household_snapshots.payload` stays **plain JSON** so live 006 CAS/create `payload_is_shared` and identity checks keep working. Personal envelopes may use `{ hearthPayload: 1, codec: "gzip-base64", bytes, body }`; decode accepts envelopes + legacy plain forever. Shared gzip waits on an approved SQL inflate helper. | Jonathan's Development desktop hit `localStorage` quota on `hearth:continuity-outbox:v1:development` while sharing large books; #150 memory+IDB fallback was recovery, not prevention. |
| D-146 | Accepted; merged via #156 | **Phase 0 identity tuple + canonical hash acceptance.** Automatic discovery/pull/persist/outbox require environment + Google subject/email + member match when a continuity identity is present (`environmentIsolation.ts`). Phrase/Pass remain recovery without Google. PGlite `audit_revisions.snapshot_hash`, `booksAcceptedHash`, and hosted CAS share `financialAuditHash`; `acceptHouseholdWrite` post-ingest verifies; boot no longer silently re-ingests projection-mismatch/interrupted money. Sheets-era issues/PRs archived in `SHEETS_ERA_TRACKER_ARCHIVE.md`. Numbered D-146 after `main` claimed D-144 Scheme A and D-145 slim outbox. | Close Phase 0 checkboxes for membership tuple, hash acceptance, #62 salvage, hosted-row inventory, and Sheets tracker archive without claiming full optional-publish removal. |
| D-147 | Accepted; implementation branch | **Optional-publish demotion + hosted honesty + Phase 0 ops runbooks.** `acceptHouseholdWrite` transports only when `transportRequested === true` (no OR on `linked` / `hostedTransportAllowed`). Auth-off **Publish to the cloud (legacy)** lives under Advanced recovery only; Auth-on Invite has no Publish. Continuity flush treats Personal/Shared partial failure (`skipped` + `error`) as pending, never ack. Auth/continuity paths refuse the legacy GET-compare-POST race when CAS RPC is missing. Hercules KV and GitHub branch-protection are documented for Jonathan (agent cannot create Cloudflare namespaces or rulesets). WORKING_MEMORY reconciled to 006-applied / model-first / continuity facts. Does **not** claim live KV bind, ruleset enablement, or Personal+Shared single SQL TX. | Close remaining Phase 0 client/doc leftovers after D-146 without over-claiming hosted atomicity or Cloudflare/GitHub owner actions. |
| D-148 | Accepted; merged via #161; Development Worker live 2026-08-26 | **Secure Development Flinks Connect inbox.** Flinks supplies evidence only; Final Confirm still posts money. Worker routes live under `/bank/flinks/*` with Supabase bearer + membership scope, encrypted D1 connection state, iframe origin/state validation, selected CAD account fetch, HMAC-redacted inbox payloads, and DeleteCard disconnect. PR #160 `/flinks/sync` and browser LoginId storage are retired. Account-scoped category autofill from PR #160 remains in `prepareImportRows`. Development-only; Production readiness remains open pending loginId attestation and legacy-row disposition. | Recover Codex secure Flinks path on current `main` without merging the stale reconciliation checkout wholesale. |
| D-149 | Accepted | **Tiered sync infrastructure reframe.** Replace 4 s poll-as-primary with **100–500 ms** partner visibility (Tier 1: Migration **012** atomic Shared+Personal SQL + Supabase Realtime push; 4 s poll demoted to fallback). Tier 2: command-log primary with materialized snapshots and confirmation-scoped undo. Tier 3: optimistic UX + soft presence. Tier 4: normalized hosted journal (research). Canon: [`SYNC_ARCHITECTURE.md`](SYNC_ARCHITECTURE.md); twenty slice prompts in [`briefs/sync/`](briefs/sync/README.md). Phase 2 roadmap rewritten; Packet 7 superseded. Kill criterion: halt hosted sharing if Realtime bypasses PGlite accept or interleaving tests fail. | Jonathan asked for airtight development plan before implementation; books win; no silent LWW. Numbered **D-149** on rebase to `main` because **D-148** was already assigned to Flinks (#161). |
| D-150 | Accepted; merged via #180; Development Worker live 2026-08-26; **two-device QR smoke passed 2026-08-26** | **Auth QR invite discovery uses continuity_memberships as membership authority.** After `hearth_redeem_invite` binds `continuity_memberships`, `discoverFromContinuityMemberships` no longer requires the pulled snapshot's `google.links` to already contain the invited Google identity. Membership row + Auth user + environment/household/member roster gates remain; generic snapshot scan fallback still requires snapshot links; PGlite `acceptHouseholdWrite` unchanged. Local overlay adds the membership Google link before persist so downstream isolation checks stay closed. | QR redeem succeeded but App.tsx failed before `openDiscoveredLedger` because `assertHouseholdBinding` checked stale snapshot links. Independent of D-149 T1/T2 sync branches. Runbook: [`AUTH_INVITE_SMOKE.md`](AUTH_INVITE_SMOKE.md). |
| D-151 | Accepted; merged via #201; **016 applied** 2026-08-27; kitchen Start from scratch live | **Start from scratch is a Development-only kitchen reset.** One Confirm deletes every disposable Development household the signed-in Google owner owns, leaves member-only seats, and clears this phone’s Development replicas, PGlite books, outbox, and Undo. Google stays signed in and Create household opens next. Production is refused in the client and the RPC. Migration **016** `hearth_reset_development_households` is the bulk path; missing 016 falls back to looping **015**. Kill criterion: any Production row deleted, or a local wipe that runs after a failed cloud reset. | Jonathan asked for an easy way to delete all households and start over. Per-household Delete was too slow for disposable Development leftovers. |
| D-152 | Accepted; merged via #208 | **Tip covariates deepen Shift Oracle without new money authority.** Tipped Confirm requires `customersServed`, `staffingCount` (headcount only — never coworker names), `eventTag`, and positive sales when sales fields exist; optional `weatherGlass`. `tipScience` observations include sales/tip%, covers, staffing, event, job; outlook/oracle/year-sim apply disclosed soft factors plus Worker `/macro/priors` (Ontario/Canada, clamp 0.9–1.1, fail soft, never posted income). Hercules Pro adds read-only `list_shifts` and cursor pagination (default 50 / max 100); free Brain stays ≤10. Shift-report OCR reuses receipt `/documents/scan` with `documentHint: shift-report`, drafts Confirm on **Shift → Today** and Add (invent-nothing, never auto-post); Worker + client omit free-text OCR notes so coworker names cannot ride Shared sync. Kill criterion: any OCR or tip tool that posts cents, or coworker names in Shared/Pro payloads. | Jonathan approved Confirm required fields, Pro page size 50/100, and OCR-as-draft. |
| D-153 | Accepted; merged via #213; kitchen live 2026-08-27 | **Shift is a first-class tab**, like Calendar (D-040). Phone nav is `Home · Cal · Shift · [+] · Plan · Books · More`. Add stays geometrically centered. Today / Report / Jobs live on Shift. Punch, last shifts, Jobs, compressed report, and Hercules Shift Oracle glances live there. Confirm still posts; the tab never writes money. Home Timesheet glance + expand stays (D-050 / D-079). Projections stay copper-badged (D-137). Tax milk is educational, not CRA (D-144). Kill criterion: a projection that reads as posted income, or a Shift tap that posts. | Jonathan approved the 390px mocks, then the preview, and ordered merge/deploy. Numbered **D-153** because **D-152** was already assigned to tip covariates on `main` (#208). |
| D-154 | Accepted; **public/no-guard; live** | **Hearth has a public, read-only product-health and investor roadmap at `/roadmap/` in the existing Cloudflare Workers + Assets site.** Structured content is separated from rendering; updates append dated evidence and never silently replace unknown Hearth metrics with external benchmarks. The route reads no household ledger, Auth, Supabase, local storage, analytics, or model endpoint. It is intentionally indexable with no login or privacy guard at `https://hearth-books.jonathan-beaulne123.workers.dev/roadmap/`; the displayed people and data are synthetic. Initial publication and live browser proof completed at `e50fb75` after routing correction `918389c`. Each later change retains its own Release approval gate. Kill criterion: any roadmap release that exposes repository secrets/private artifacts, claims dated audit evidence is live telemetry, or deletes prior horizon items rather than preserving their history. | Jonathan selected **public**, said no privacy guards are needed because displayed content is synthetic, then explicitly approved the reviewed release. Canon: [`ROADMAP_SITE.md`](ROADMAP_SITE.md). |
| D-155 | Accepted; Development release in progress | **7shifts is a native, review-only Shift inbox.** Shift → Jobs owns connection and Co-workers; Add Timesheet owns punch selection and the existing four-step Confirm. The Worker requires Auth membership before D1/provider access, stores the provider token under a dedicated AES-GCM key, returns only allowlisted/HMAC-redacted punch and roster fields, pins API version `2026-01-01`, and refuses Production. 7shifts tips and wage fields never enter Hearth; Hearth job rates and human-entered tips retain money meaning. Stable punch digests prevent replay after Confirm, while provider corrections refresh the draft without changing dedupe identity. Camera/provider drafts and every ledger/member scope are fail-closed. Kill criterion: an automatic money post, provider wage/tip data entering books, coworker data becoming household members/model context, or any Production provider call. | Jonathan asked the reviewed 7shifts work to fit seamlessly into the new Shift tab and explicitly authorized push, merge, Development D1 apply, enablement, and deployment. Numbered D-155 because main already owns D-152 tip covariates, D-153 Shift, and D-154 roadmap. |
| D-156 | Accepted as direction; kitchen has early paper office (#229); chalkboard + screenshot nav in #228 | **Wide Home is a composed paper office, not a stretched phone and not free-move by default.** At `≥720px`, Home uses phone C grammar (seals, story mosaic, notebook) laid out as a two-column room: hero blotter + journal-true paper infographics beside a 2×3 mosaic; the notebook stays open next to stories. App column is `min(1120px, 100%)`. Draft D window, sill, and Hercules wander stay. Free-move `packWide`, S/M/L, Edit Desk, and personalities remain as opt-in **Classic desk** (D-080 not dropped). Existing layouts with saved x/y keep Classic. Phone `<720px` stays Draft C. Widgets still never `postEntry`. Kill criterion: if Bianca cannot add milk because a chart or the cat covers Post, shrink the furniture. | Jonathan asked to port the phone UX to desktop, take advantage of extra space, keep the vibe and simplicity, add infographics, and make it special — not a 1:1 stretch. That outranks D-079/D-082’s free-move-as-default without throwing away customization. |
| D-157 | Accepted; **public/live** 2026-08-28 via `455f1ab`; Cloudflare run `33185717271` | **The public roadmap leads with Hearth's full current vision and gains a dated project journey plus a two-exhibit museum.** The Aug 17 Sheets / Apps Script HTML is preserved as a frozen standalone exhibit; the Aug 23 “Big Thinking” Claude artifact receives a curated exhibit and repository museum note. Historical content is provenance, not product law: it never changes current health scores, evidence gates, investor rows, or P0–P∞ counts. Git-proven milestones retain exact semantic timestamps and display Toronto time to the minute; artifact-only dates remain date-only. Existing roadmap content stays additive. Kill criterion: a museum status presented as current, an invented timestamp or unavailable transcript, a historical phase entering canonical filtering, or deletion of prior roadmap content. | Jonathan asked to incorporate both old roadmaps, give each its own museum section, date important updates and milestones, and make the main roadmap better express the overall project vision. The Aug 17 and Aug 23 labels are user-supplied and source-verified; no exact artifact times were claimed. |
| D-158 | Merged via #231; inert Development vault live 2026-08-28 | **7shifts becomes a provider-neutral member Evidence Mesh.** Complete user-selected browser, iPhone, file, screenshot, calendar, email, OCR, and optional official-API captures live in a dedicated encrypted Development vault (separate D1/private R2/Queue), never snapshots, PGlite, Flinks state, command events, or Hercules. Every raw object is bound to environment + authenticated subject + household + member + evidence id + digest with per-object AES-GCM; exact membership is resolved server-side. Unified evidence bundles may corroborate one canonical member/job/window only; conflicts and unknown fields remain evidence, never zero or inference. Calendar is outlook only. Chromium/iPhone/email channels are explicit, bounded, disabled, and separately gated. | Jonathan ordered the stale `891cc5d` packet rebuilt on current main and approved dedicated Development D1/R2/Queue/DLQ provisioning, encryption-secret creation, both Evidence migrations, an inert deployment, and later PR #231 push/merge. The live vault is empty and private; this does not authorize capture, automation, email, or Production. Renumbered after merged main assigned D-156 to Wide Home and D-157 to the roadmap museum. Canon: [`SEVEN_SHIFTS_EVIDENCE.md`](SEVEN_SHIFTS_EVIDENCE.md). |
| D-159 | Accepted; release activation in D-160 | **Opt-in deterministic 7shifts automation is a new work-command authority for exact member/job policies.** Eligible structured/final evidence or independently agreeing screen extractions may call ordinary `postWorkShift` without visible Confirm; schedules, email, OCR/model output alone, Hercules, and conflicts never can. Deterministic `s7:` command ids use the existing PGlite/books acceptance and authenticated Supabase command-log transport, then acknowledge a member-scoped Evidence receipt. Provider corrections execute an atomic `reconcileWorkWeekFromEvidence`: exact component reversals first, chronological full-week replacements second, preserving overtime and correction links. Closed/settled periods fail into variance review rather than rewriting history. D-155 remains historical/default manual behavior and is superseded only for a member/job that explicitly enables D-159. | Jonathan explicitly superseded D-155's Confirm-only and provider wage/tip exclusions for opted-in jobs. D-160 later authorizes Development and Production activation while the per-member/job policy remains off until a member explicitly enables it. Kill criterion: model/calendar/email authority, cross-member evidence, partial payroll-week rewrite, mutable journal history, or a non-idempotent hosted post. |
| D-160 | Accepted; full release in progress | **Evidence and the read-only official 7shifts adapter may operate in both Development and Production through physically separate storage and keys.** Development and Production each use their own Evidence D1, private R2, Queue/DLQ, AES-GCM wrapping key, and 7shifts connection encryption/digest keys; environment is included in ownership AAD, membership lookup, provider digests, every D1 predicate, and queue routing. Production continuity is enabled so an eligible opted-in D-159 job can reach the existing authenticated command log; missing Production membership still refuses all hosted reads/writes. Global activation creates no automation policy and cannot post by itself. Email remains off until a Cloudflare-routed domain exists, and official provider calls remain inert until a restaurant administrator supplies a token in the authenticated UI. | Jonathan ordered the full version now and explicitly authorized Production, real selected evidence, official calls, and opt-in automation. Production Evidence/7shifts databases and storage were provisioned separately; migrations and secret names are release evidence, never client or snapshot data. Kill criterion: any cross-environment object/key/database use, automatic policy creation, provider call without an entered administrator token, email enabled without a verified route, or Production command without exact membership. |
| D-161 | Accepted; implementation reviewed in #237 | **The Hearth Household Fund is an append-only virtual shared operating subledger, not a bank account.** It opens at zero. Either member may propose a contribution; only Bianca confirms receipt, settlement, reconciliation, Kitty allocation/release, or reversal. A transaction's Household/Personal visibility is independent from its optional Fund allocation. Confirmed contributions become household-owned while retaining contributor history. Purchases and refunds remain truthful even in deficit; negative free-to-spend names an exact top-up and blocks new planned Fund commitments, never historical entry. Operating balance, transfer due/credit, upcoming reserve, free-to-spend, and safe Kitty rollover are deterministic projections. Fund events and allocations are immutable; corrections append reversals. Bianca's backing savings account and full reconciliation arithmetic live only in her Personal envelope. The persistent disclosure is: money remains in Bianca's savings and Hearth cannot move it. | Jonathan approved the September rehearsal as the next core Dual Course slice. It improves Budget +5 through clearing/privacy/reconciliation and Engagement +2 through a shared weekly routine. Renumbered from D-160 after current `main` assigned that number to the Evidence/7shifts release. Kill criterion: representing the Fund as held cash, partner visibility into Personal savings, non-custodian confirmation, mutable history, or blocking a truthful purchase because the Fund is short. |
| D-162 | Accepted contract; Release blocked on provider proof and approval | **October bank connection is read-only evidence for existing D-161 actions.** Carry September history forward without resetting the Fund. Credentials, raw account identity, balances, and raw bank rows remain Bianca-private and never enter Shared snapshots, Hercules, or model prompts. One unique exact match may verify by CAD amount, direction, bound account/destination, bounded Toronto-date window, and no competitor; explicitly selected grouped rows/actions require an exact sum. Near, competing, and likely-human-error matches remain visible proposals. An unmatched bank row never creates or changes money. Flinks must first prove support for Bianca's institution and savings account through the existing encrypted Worker/D1 boundary; otherwise stop and return provider options. Statement import may test semantics but is not the promised live connection. | Read-only evidence reduces reconciliation friction without granting a provider command authority. Renumbered from D-161 after current `main` assigned that number to the Household Fund. Release still requires a separate exact-SHA review, privacy/RLS denial suite, provider smoke, Gemini disposition, and Jonathan's explicit approval immediately before secrets, remote migration, deploy, or Production. |
| D-183 | Accepted as a Development-only Bianca approval gate; caught up locally to current `main@244bc67`; Gemini and authenticated-phone proof pending | **Month One is a four-week Toronto rehearsal on the ordinary Hearth mainline, not a launch funnel or trial fork.** A new Development household begins with one reversible opening-balance batch against Opening equity; it cannot post after ordinary accepted money or stack a second active opening. The shared versioned rehearsal links only accepted command/Fund/reconciliation/month-close evidence, records honest skips and participant-only friction, freezes exact weekly accounting checkpoints, requires each member to acknowledge on their own phone, and archives only after a closed month, current tied proofs, Bianca's exact trust statement, and Jonathan's countersignature. Its non-money updates travel through the current D-180 command-event materialization path while remaining outside the journal, financial hashes, Hercules context, analytics, and Worker requests. The fictional correction lesson runs the real post/reversal/compiler path in a discarded copy. Future changes to App routes, commands, books, household shape, or continuity must keep the mainline Bianca regressions green. Approval enables nothing: Production, providers, scaling, and launch stay closed. | Jonathan made Bianca's month-long engagement and approval the top priority, then directed that every app edit also affect the trial month so it cannot drift again. Budget `+5`; Engagement `+3`. Kill criterion: a parallel trial App or financial writer, rehearsal facts dropped by current command sync, repeated openings, checkbox money completion, invisible evidence choice, nonparticipant friction access, inferred abandonment, stale checkpoint approval, model interpretation, or any launch/Production side effect. Implementation packet: [`briefs/BIANCA_MONTH_ONE_REHEARSAL_HANDOFF_2026-08-28.md`](briefs/BIANCA_MONTH_ONE_REHEARSAL_HANDOFF_2026-08-28.md). |
| D-163 | Accepted; merged via #239; live 2026-08-28 | **Direct Gmail replaces Cloudflare forwarding for Jonathan's 7shifts-mail workflow.** The signed-in PWA requests short-lived `gmail.readonly`, runs one fixed 7shifts query, verifies the actual RFC822 From mailbox is `7shifts.com` or a subdomain, and uploads only those raw messages into the existing encrypted member Evidence vault. No Google refresh token, Gmail send/modify permission, forwarding rule, or Cloudflare mail domain is added. Repeated scrubs deduplicate by a server-computed scoped raw digest. Hercules Pro may review only reduced Gmail-derived facts; raw mail, headers, ids, names, notes, hashes, and source paths never enter a tool result. Schedule/notification email stays outlook-only. A separate eligible worked bundle may use a sealed Hercules shift preview and explicit confirmation, but confirmation reruns ordinary `postWorkShift`, permission, membership, revision, command-identity, double-entry, and exactly-once publication checks. | Jonathan explicitly removed Cloudflare from the requested flow, offered Gmail access, asked for a Hercules “scrub my emails for shift data” command with review in Hercules Pro before writing, and approved push, merge, migration 0003, and deploy. This supersedes only D-160's Cloudflare-email prerequisite; it does not make email worked-time authority or weaken D-159's evidence eligibility. Renumbered from local D-161 after current `main` assigned D-161/D-162 to Household Fund and its bank-evidence contract. Kill criterion: durable Gmail credentials in Worker/snapshot, broad mailbox query, lookalike sender acceptance, raw email/model disclosure, schedule-to-money conversion, or any shift post without current-conversation explicit confirmation. |
| D-164 | Accepted as design protocol; Cursor packet ready | **Ledger purpose is Layer 1; device composition is Layer 2.** Shared Ledger coordinates two people around what is true together, what changed, what needs a person, what comes next, and why the view is trustworthy. Personal Ledger is a distinct private folio for one member's position, movement, obligations/goals, shared choices, and privacy—not the Shared room with a different filter. Every mode-aware route consumes an explicit member/view projector before rendering; raw household state is not a presentation contract. At `>=720px`, desktop and iPad use the same Shared Story/Personal Folio component systems with an intentional portrait-tablet composition. Phone `<720px` keeps the current `OfficePhone` structure and vibe; this packet audits it and permits only necessary semantic corrections, not a desktop-story port. Existing commands, D-161 Fund math/authority, Confirm, PGlite, continuity, and Hearth theme remain. | Jonathan found that Shared and Personal now accomplish different jobs and explicitly set the two-layer protocol. The current live/code audit found one shell, one page hierarchy, and largely identical Books/Office experiences under both mode labels; the Fund is a detached card plus form pane rather than the household story. Budget `+4` through scope/authority/reconciliation clarity; Engagement `+3` through cooperative flow, weekly story, and monthly arc. Kill criterion: any mode that exposes hidden Personal facts, changes financial meaning, ranks partners, turns Fund free-to-spend into global safe-to-spend, lets an infographic cover Add/Confirm, or structurally redesigns iPhone without new approval. Implementation packet: [`briefs/CURSOR_SHARED_LEDGER_STORY_HANDOFF_2026-08-28.md`](briefs/CURSOR_SHARED_LEDGER_STORY_HANDOFF_2026-08-28.md). |
| D-165 | Accepted; Development release repair | **Development Evidence normalization consumes one encrypted capture per Queue invocation.** The first authenticated 330-message Gmail smoke proved that a ten-message batch could exceed the Worker CPU limit even though each message normalized successfully on its own. Development therefore uses `max_batch_size: 1`; retries, DLQ isolation, encryption, membership scope, and the Production consumer configuration are otherwise unchanged. | Jonathan ordered the full merged deployment and authenticated recovery. Live error tracing identified batch CPU exhaustion as the only remaining terminal-state blocker. Smaller batches trade throughput for deterministic completion without changing evidence or money authority. |
| D-166 | Accepted; local implementation in progress | **7shifts coworker identity is a member-owned workplace directory, never household membership or money authority.** Explicit employee-visible browser captures may derive roster names, observed roles, and coworker schedule outlook after recursively stripping credential-shaped response fields. Jonathan assigns reviewed identities to one of his Hearth jobs/locations; Hearth creates private `coworkerId` records, preserves distinct protected provider identities even when names are identical, and exact-name matching may auto-link only when unique inside that scope. A unique last-name result is a suggestion requiring confirmation; ambiguous names never auto-link. Each attendance review fully replaces the prior non-financial state for that shift, so removed people cannot survive as stale context. Coworker names remain excluded from generic Hercules/household disclosure; in Personal chat, Jonathan may separately opt in to a reduced owner-only roster for one reply, after which the toggle resets. No source identity, partner row, raw evidence, or money authority enters that payload. | Jonathan wants the employee-visible 7shifts schedule to supply the expected floor and wants OCR/shift-report review to correct attendance instead of retyping coworkers. Personal guests and restaurant covers remain separate future covariates; synthetic shifts remain test/demo only. Kill criterion: capturing credentials, creating household members from a roster, schedule-as-worked-time, ambiguous automatic identity matching, partner/shared roster leakage, or named attendance changing money. |
| D-167 | Accepted; merge/deploy ordered 2026-08-28 | **A household open must never blank `#root`.** Missing Tip Tracker settings default on shape; kitchen shift preview uses `previewShiftAmounts` so invalid settings cannot unmount the tree. Incomplete Google GIS identity skips continuity instead of throwing. An App-level ErrorBoundary shows paper recovery (Reload / Sign out of Google / Open welcome) and never `postEntry`. Welcome stays a separate path. | Jonathan: blank screen only when loading into a household, then confirmed it was fixed. PR #235 already fixed the Evidence hook-order crash on `main`. Remaining kitchen-only throws had no boundary. Renumbered from local D-161 because `main` assigned D-161 to the Household Fund. |
| D-173 | Accepted; integrated locally on `codex/kitchen-desk-integration`; review pending | **Shared is one pool. Personal Books is the account floor.** Shared is not a daily room list; Kitty Banks on Shared are sub-accounts of that pool and still receive D-161 Fund surplus into existing shared goals. Personal Books lists household-visible rooms plus this member’s personal rooms and never partner-personal rooms. Home seals on both ledgers are posted Money in, posted expenses Money out, and leftover spend (in − out). Unpaid rent, repeating bills, and dues stay out of Money out. Sit-down leftover (`leftoverProjection`: cash-like − bills next 30 − card mins) is a different number and must not share the leftover label. Personal Books uses `personalBooksFloor`, not `scopedHousehold`; Home, dashboard, Hercules, and export stay on the existing projectors. Kitty Banks fatten from `savedCents` in 10% steps. Fund bank contribution still requires Confirm. | Jonathan confirmed 2026-08-29 after locking the three questions: Shared stays one pool; Personal gets the Wealthsimple-style rooms; leftover spend is posted in minus posted expenses. Budget `+4` through honest rooms and leftover CAD; Engagement `+3` through the three-column desk and fat banks. Kill criterion: dumping household journal into `scopedHousehold`; showing partner-personal rooms; calling sit-down leftover “leftover spend”; Fund contribution without Confirm; a new envelope formula. Renumbered from PR #244's D-165 because current `main` already uses D-165 for Evidence Queue normalization. |
| D-174 | Accepted as gated company direction; implementation phased | **Shared Money becomes priority one after hard safety blockers.** Hearth will pursue a partner-backed Canadian joint account for Jonathan and Bianca first: a shared operating account for bills and goals beside private personal accounts; equal co-owner routine authority with guardrails; separately consented in-app, Web Push, email, and SMS notifications; and, only in a later approved phase, dual-approved versioned standing rules. Exact provider-settled events may eventually settle books automatically only when they match a previously confirmed action or active dual-approved rule version; every unmatched, duplicate, late, reversed, or ambiguous event enters review. D-172 remains current law: this decision does **not** authorize background financial posting, bank credentials, provider activation, money movement, schema application, Production mutation, or cards. D-161 remains a virtual Fund. Development provider evidence exists under D-148; D-162 keeps Fund-specific connected evidence read-only and Release-gated until separately superseded by a Release decision. Canon: [`briefs/shared-money/README.md`](briefs/shared-money/README.md). | Shared-account products reduce the operational friction of splitting and tracking household costs, while Hearth can differentiate through honest Shared/Personal books, consent, and settlement proof. The program separates evidence, attention, books, rail, and notification planes so no callback or alert becomes authority by accident. Budget potential `+5`; Engagement potential `+2`; SF-00 actual `0/0`. Kill criterion: coerced connection or sharing, partner-personal inference, unilateral hidden authority, presenting virtual balances as deposits, posting unmatched provider activity, or launching without identity/RLS, counsel, reconciliation, disputes, support, incident, and safe separation controls. |
| D-175 | Accepted; local Startup P1 implementation | **Cached-first startup is read-only until the selected local books validate.** Hearth may immediately paint the synchronous `localStorage` household shell, but every active-household mutation and Confirm remains locked behind an environment + household + revision readiness token. PGlite inspection starts after two paint frames in a module worker; repair is automatic only for missing/incomplete schema, while mismatch, interrupted transaction, invalid journal, or inspection failure stays visibly blocked. Linked cloud fetch begins only after local validation, never occupies the local write queue, and still passes the then-current candidate through queued `acceptHouseholdWrite`. Metadata-only revisions may carry the latest matching financial receipt only when canonical financial facts remain identical. Office, Books, Calendar, Shift, Pairing/QR, imports, and Realtime attachment are lazy boundaries; Hercules and the basic kitchen shell remain eager. | Jonathan asked for a swift cached shell without weakening Confirm and for secondary surfaces to stop taxing startup. Budget `+5` through an explicit local-books authority gate; Engagement `+3` through immediate kitchen/Hercules presence and intent-loaded rooms. Kill criterion: cached money mutation before validation, remote money displayed before PGlite acceptance, silent mismatch rebuild, worker transaction/hash weakening, or a lazy-load failure that can change books. |
| D-176 | Accepted; Development authority smoke passed | **Shared Money access is independent identity plus session-gated household membership.** A partner invite defaults to equal `owner` authority but may explicitly create an ordinary `member`. Co-owners may manage authenticated devices and ordinary members; one co-owner cannot silently revoke another, and the last owner cannot leave. Voluntary leave immediately deactivates membership and registered sessions, blocks queued cloud replay, clears the confirmed local household copy, and requires a fresh invite to rejoin. Migration 017 introduces a JWT `session_id` registry, sanitized access inventory, per-device revocation, and metadata-only identity audit. Existing `household.devices` remains soft presence and is never authority. A revoked token is denied by Hearth's server gate even if its encoded access-token lifetime has not elapsed; Hearth does not claim it can erase offline cached data. Auth-enabled UI locks the phone to the Google-bound member. | SF-02 closes invisible shared-access gaps without widening money authority. Budget `+2`; engagement `+1`. Migration 017 was applied to a shared Supabase project containing 2 Development and 0 Production households; a two-principal transactional hosted smoke passed and rolled back all synthetic state. Kill criterion: UI-only access, co-owner coercion, orphaned household, token/email/subject/partner-private data in access inventory or audit, revived revoked session, former-member outbox replay, or any claim that offline cached books were remotely erased. |
| D-177 | Accepted; local Performance P2 implementation | **A money Confirm completes at durable local acceptance, not at network acknowledgement.** The command still validates, enters balanced PGlite, independently verifies the canonical hash, and persists the full device snapshot before awaiting the slim durable outbox pointer; cloud Auth refresh/CAS then runs in the background only for a matching refreshed identity. Outbox acknowledgement is generation-checked. On Development only, default-off `VITE_PGLITE_INCREMENTAL_DEV=1` may update an exact same-household v4 projection whose stored digest still matches every live SQL table. First/legacy ingest, replica switch, large delta, missing proof, Production, and periodic compaction retain the full transactional `TRUNCATE` rebuild. Delta failure or projection drift rolls back/rejects; neither silently rebuilds over a proved mismatch. | Jonathan selected local-save completion, incremental depth, and a Development canary to make daily Confirm swift without trading away D-111/D-146 trust. Budget `+5`; Engagement `+2`. Kill criterion: UI success before local durability/outbox, mismatched incremental/full projections, changed financial hashes, failed rollback, unbounded relation growth, unauthenticated background financial transport, a stale acknowledgement erasing a newer tip, or Production incremental activation without a separate approval. |
| D-178 | Accepted; local UI Performance P2 implementation | **Recurring UI work belongs to the smallest visible surface that needs it.** App no longer owns a one-second shift tick: the elapsed label ticks locally, and the Add form receives a value only when the rounded quarter-hour preview changes. Closed Timesheet glances are timer-free; idle expanded clocks use minute cadence; scroll/resize furniture reads coalesce to one animation frame. Hercules remains mounted and alive through the existing rig: desktop Home uses full ambient cadence, secondary rooms use compact 2 fps cadence, blocked consequential sheets use the hidden profile, and direct reactions retain their bounded 24 fps lease. Home-only wandering, fly play, and the ten-second idle pounce pause outside visible Home or while blocked; successful geometric capture still increments the session-only litter box. | Jonathan asked for a swift, responsive UI without making Hercules feel lifeless. Budget `+2`; Engagement `+3`. No visual redesign, financial meaning, command authority, cloud boundary, or Production behavior changes. Kill criterion: App-wide second ticks, idle timers with no visible need, unbounded geometry reads, autonomous pounce behind a consequential sheet, loss of reduced-motion/hidden suspension, or weakening the ten-second Home pounce and bounded direct reactions. |
| D-180 | Accepted; local Release pilot implementation, live rehearsal pending | **Ledger-native “Google Docs feel” is a Development-only command-sync pilot for Jonathan and Bianca.** The pilot build enables Google Auth, Development Realtime, command log, and local redacted diagnostics while baking `VITE_PRODUCTION_CONTINUITY=0`; Production discovery, transport, Realtime, and diagnostics are refused. Confirm → balanced PGlite → durable device snapshot/outbox → command append → Realtime → partner PGlite is normal; snapshots are only creation/catch-up/gap/integrity recovery. Disjoint additive facts converge and duplicates apply once. True same-fact, reversal, or restore divergence preserves both versions for explicit human resolution. Personal scope stays owner-only; Migration 017 session/device authority wins over soft presence; revocation cannot remotely erase an offline cache. No financial command event, receipt, reversal, or tombstone is compacted during the pilot. The live two-account matrix, 100 new latency samples, and fourteen-day disposable rehearsal must pass before the narrow claim is earned. | Jonathan explicitly ordered the daily-use proof and recovery pilot on 2026-08-31. Budget `+2 x 5 = +10`; Engagement `+2 x 3 = +6`. This supersedes the prior pilot build's Production-continuity flag and any same-fact auto-choice behavior; it does not authorize Production, schema, hosted-data changes, push, merge, deploy, or literal field co-editing. Kill criterion: silent loss, duplicate posting, invalid books, Personal/privacy leak, cross-environment access, false Synced state, or unresolved P0/P1 recovery finding; stop sharing and preserve local replicas/outboxes. Canon: [`SYNC_PILOT.md`](SYNC_PILOT.md). |
| D-181 | Accepted; merging onto current `main` after Jonathan's 2026-08-31 merge/push/deploy order. Renumbered from PR #244 D-174 because current `main` uses D-174 for Shared Money. | **Add is four unique cashpad prompt slideshows.** Speed-dial mode opens a full-screen prompt chain, not one scrolling form. Expense: how much did you spend → in which category (Add category lives here) → which account (Books-floor tiles) → picture or note → Confirm. Income, transfer, and shift each have their own prompts. Enter advances; Confirm still posts; the slideshow never `postEntry`. Pictures stay on this phone and are not journal attachments. Account tiles quote accepted books and never show partner-personal rooms. | Jonathan asked to lift the speed dial slightly and replace the dense Add sheet with prompt slides after the FAB work, then ordered merge, push, and deploy. Budget `+3` through a calmer posting path; Engagement `+3` through unique ceremonies. Kill criterion: auto-post, a second amount, hiding Confirm, or partner-personal rooms on the account tiles. |
| D-182 | Accepted; independently reviewed and deployed to Development at `244bc67c`; live canary open | **Household entry is Google-first and chooser-first.** A normal sign-in always shows every authorized household, including one household, instead of auto-opening it. Each card uses the accepted snapshot's `lastCommittedAt` for relative and exact device-local edit time. Auth invitation links and QR codes survive Google redirect, redeem idempotently, refresh discovery, and highlight the joined household for an explicit Open action; phrase/Pass entry is Advanced recovery. The active header reads member · household name · device-local date/time, while posting dates remain `America/Toronto`. | Jonathan asked to rethink login/join, make joining work, show last edit for every household, and put member/household/time under the Hearth logo. Existing membership snapshots already contain the required data, so no schema or hosted API is added. Budget `+3`; Engagement `+3`. Kill criterion: opening the wrong household, auto-opening after ordinary sign-in, trapping a stale invite across future sign-ins, treating last-opened as last-edited, hiding destructive reset beside Open, changing Toronto posting dates, or enabling Production continuity. |

- **D-177 / D-178 release (2026-08-30):** Performance P2 and UI Performance P2 were rebased onto Shared Money `main@f5f0503`, independently reverified, and fast-forwarded to `main` at `29250c73d2af4ca46db97028fd9eaa606fa3aad0`. Branch CI `33344886166`, main CI `33345134140`, and Cloudflare deployment `33345134173` passed. Live wide Office and 390 px `OfficePhone` rendered Hercules without horizontal overflow or console errors, and cached journal validation settled successfully. No migration, hosted-data mutation, secret change, canary enablement, or Production incremental activation occurred.
- **D-182 Development release (2026-08-31):** Jonathan gave fresh push/merge/deploy approval after independent books, trust, and release review. The candidate was committed and fast-forwarded to `main@244bc67c21df389e22b5aa0dd764c5e1d4bf68c7`; main CI `33421668514` and Cloudflare Build/Deploy `33421668573` passed. The live bundle exposed the new Google chooser and Advanced recovery, and the signed-in header rendered member · household · local time without console errors. No migration, hosted-data mutation, secret change, or Production-continuity change occurred. Live two-account OAuth/invite and the full canary remain open.

**Why-notes (docs rebuild, 2026-08-22)**

- **D-177 Development canary activation (2026-08-31):** Jonathan explicitly directed the faster PGlite path through Development after the five-stage repair manual was reconciled and human checks were treated as complete. The release candidate bakes `VITE_PGLITE_INCREMENTAL_DEV=1` into the Cloudflare build and asserts it beside `VITE_PRODUCTION_CONTINUITY=0`. The runtime still requires `environment === "development"`; Production remains on the full transactional rebuild even if an internal caller requests incremental ingest. This changes no financial meaning, schema, hosted data, secrets, provider setting, or Production continuity. Roll back by removing or zeroing the Development flag and redeploying.

- **D-022** still says seed data includes chequing, Visa, and cash. That is the original defect name (hidden first-active-account). Catalog kinds are D-047; `cash` migrates to `other`. We did not rewrite the seed sentence so the bug D-022 prevents stays searchable.
- **Rings and Chapters** are not deleted as *ideas*. They are retired as *indexes of the future*. Shipped rituals (chalkboard, cook-off, sit-down postcard, spectacles, wallet tiles) remain product.
- **Charter “out of scope until the core is boring”** is replaced by unbounded Dual Course vision plus engineering gates. Why: the core is already a general ledger; pretending bank rails are unthinkable was how the old maps went stale, and pretending they are next-week work was how they got dangerous. Auth + RLS still comes first.
- **D-033** still says the hosted schema is the same as PGlite. That remains true of the SQL file. The app currently writes hosted `households` + `household_snapshots` (JSON). It does not populate hosted `journal_*` tables. PGlite is the books engine (D-052).
- **D-034** still says the publishable key is not a privacy boundary. Spell out the current door: `FOR ALL … USING (true) WITH CHECK (true)` includes DELETE. A caller who has the bundled key can read and empty hosted snapshots without the three-word phrase. Treat published hosted rows as disclosed until Auth (D-052).
- **D-052** still says hosted upsert never DELETE-then-POST. **Why-note (2026-08-24):** D-110 stops the client from uploading unless `linked` is already true. RLS remains `USING (true)`. Treat any row that *was* uploaded before D-110 as disclosed. Do not delete it without Jonathan.
- **D-047** still lists chequing / savings / credit / investment / other. Receivable (D-053) is a sixth kind: money owed to us. We did not rewrite D-047's sentence so the Accounts Floor's original object list stays searchable. `other` remains the jar.
- **D-053 / D-054 / D-055** (2026-08-22): Jonathan fixed receivable-then-settle and granted full authority including amending canon. Quiet labels and propose-only jars are Cursor's designs, recorded here so the next agent does not treat them as undecided. Snapshot hash now includes claim amounts (D-052 spirit). METC projection is out-of-pocket after expected recovery — pending Sun Life does not inflate the credit.
- **D-056** (2026-08-22): Appointments graduates from a Calendar pane stub to a destination. Money meaning is unchanged. The medical log is the front door; claimed-vs-learned cadence is the detail sentence.
- **D-045 / D-049** (2026-08-22): D-059 lifts the “never a transaction dump” payload rule. Chat history still never ships. Memory labels still strip amounts. Retrieved, quiet-redacted excerpts may reach OpenAI/Anthropic via the Worker. The original D-049 sentence stays searchable; this why-note is the amendment.
- **D-057 / D-058 / D-059 / D-060** (2026-08-22): Jonathan granted full authority including amending canon. On-device science, shared presets, retrieved model payload, and the quiet-note leak close are Cursor's designs, recorded here so the next agent does not treat them as undecided. Snapshot hash now includes preset ids/amounts (D-052 spirit). D-039 is unchanged.
- **D-061** (2026-08-23): Jonathan supplied the ink package (superseding redraw §2a/§2b/§4) and the three-d-stage GLB/MTL as 3D source. Live cat stays 2D because the GLB is a fur-card export. Furniture targeting, drag-bump, fly, and the `useFurniture` poll fix are the remaining Cursor scope from that brief.
- **D-062 / D-063 / D-064 / D-065** (2026-08-23): Jonathan asked for a punch clock, page-true Hercules, widgets that close and snap, calendar + appointments instruments, a drawable chalkboard, and a Mint/YNAB overview on the weather-affected sill. `postShift` math did not move. Handwriting never leaves the phone.
- **D-066–D-077** (2026-08-23): Jonathan asked for a customizable widget environment attractive to CPAs and kids: calendar day/week/month, analog timesheet, piggy jars, accounts tiles, hide/show, Hercules-on-expand, accessories widget, shared games, preset note+icons, punch pad auto-write, calculator shift preview-only, next-bill excluding income, and the Toronto civil-date label bug. Confirm still writes. Games and cosmetics never post.
- **D-078** (2026-08-23): Google works on the kitchen. Treat the suite as in-scope Dual Course features, not a future chapter. A desktop **Household / Google manager** personality may feature Calendar overlay; it is still not Flinks, still not a mandatory 18th widget on the phone. Do not persist Gmail/Drive/Contacts bodies on the snapshot while hosted RLS is `USING (true)`.
- **D-079** (2026-08-23): Jonathan split UI: mobile glance/one-tap; desktop keeps the current office. **Mobile Home shipped** as `src/OfficePhone.tsx`. The “desktop frozen this pass” line is superseded by D-080.
- **D-080** (2026-08-23): Jonathan restored the original QoL brief onto **desktop**: unique offices, sizes, appearance, backgrounds, default desks (Tracker / Household+Google / CPA), full layout tools. Claude reviews unbiasedly (keep/reshape/refuse, no silent drops) and builds wide pixels. Mobile subset is a joint Claude+Cursor decision. Do not turn `OfficePhone` back into seventeen rows. Do not invent a second brand. Do not open a Cloudflare-token PR.
- **D-082** (2026-08-23): Jonathan took Claude’s desktop zip as **reference**, not a drop-in. Keep packing, sizes, Edit Desk, personalities. Keep the current desk’s warmth: notebook page, Fraunces names, cream papers, window weather. Do not ship 10px labels, always-open L bodies, or a 1280 boxed lobby.
- **D-050** (2026-08-23): CAD pad, Milk/Shift/Pay stamps, and posted-shift streaks stand. Look → trims → Apply as the sit-down *product* is superseded by D-083. `applySitDown` still only writes `budgetPlans`.
- **D-083–D-087** (2026-08-23): Jonathan’s sit-down brief is the feature the app was started for. Leftover is cash-like − 30-day outgoing bills − card mins. Plan then `postTransfer`. Hard lock kills `confirmClosedMonth`. Reverse instead of delete. Auto-code stays on-device. Drive create-only. Cursor refused: safe-to-spend, LLM coding, auto-lock on apply, killing reopen, rebuilding sync as append-only streams this pass, a new 18th Home widget, a 1280 desk, and a second Cloudflare-token PR. Hash already covered amounts on main; this pass adds tombstones, `reversalOfId`, and sit-down leftover/transfer ids.
- **D-088–D-094** (2026-08-23): Almost-there update. Leftover still D-083; jar parking destination is the Goals vault. Purchased? posts an expense and retires the pig into a retirement home shelf (not a new Home widget). Mobile no-zoom. Pin-open vs cannot-hide. Desk JSON on this Google account. How can I help + event→Calendar + click-to-close + perch-on-expand. Wide chalk on weather glass. Refused: bank feed, LLM auto-post, deleting money rows, rewriting leftover as month net, Cloudflare-token PR.
- **D-095** (2026-08-23): Chat recaps (`WORKING_MEMORY.md`) compress *agent thread ramble*. They do not replace this log, merged PRs, nostalgia, or reference. Other AIs should read the whole GitHub history. They should not treat unfinished chat as `main`. **Why-note (2026-08-24):** the GitHub-facing [README](../README.md), [docs/README.md](README.md), and [AGENTS.md](../AGENTS.md) now name the living roadmap ([HEARTH_ROADMAP.md](HEARTH_ROADMAP.md)) so Cloud Agents and repo visitors see current canon instead of unlinked stubs. Museum folders stay history.
- **D-096–D-102** (2026-08-23): Companion office update. Funded jars + arrival dates; Hercules sit/beg/usefulness; expand shells + bump + layout v2; duplicate contrast; synced devices; Large density + first-run lessons; sit-down → transfer standing orders. Leftover *formula* stays frozen. Usefulness *scorer* unfrozen by D-113. Layout still not on the household snapshot.
- **D-103** (2026-08-23): Lock `/hercules/chat` — origin allowlist, reflected CORS, optional `HERCULES_RATE` KV (60/day/household). Client sends `householdId` for rate keys. Bind KV in production before increasing model usage.
- **D-104** (2026-08-23): Model-first chat — journal answers and chips call `chatHercules`; safety paths still skip. Bubble shows `ai` / `on-device`.
- **D-105** (2026-08-23): Ledger excerpt — 18-row cap, partner personal rows filtered by viewer `memberId`, `ledgerLines` line format for Worker.
- **D-106** (2026-08-23): Memory kinds — structured `{ kind, label }` in model payload; briefing + `memoryFactForTopic` consumers on-device.
- **D-110** (2026-08-24): Hosted transport is opt-in. Local ingest and hosted publish are separate. Unlinked/demo/pass never fetch household REST. Inventory hosted leftovers; do not delete. D-107 remains a historical reservation from the stale PR stack; D-108 was claimed by due-on-open salvage; D-109 is claimed by the budget-edit salvage.
- **D-109** (2026-08-25): Books budget edit — tap Statements → Budget variance to `setBudget` for the current month. Plans only; no journal rows.
- **D-097** (2026-08-24): At usefulness 80 the extra beg tap was in the way. First tap opens How can I help when `openHelpOnTap`; yellow/red still beg. Sit-down / leftover chips expand the postcard and keep the leftover sentence. Cursor owns this wiring on the usefulness-80 branch so Claude’s remaining packet is viewport proof, not a second tap rewrite.
- **D-104** (2026-08-24): Model-first stands. D-112 narrows FIGURES to grounded CAD so briefing totals cannot be swapped onto the asked account.
- **D-112 / D-113** (2026-08-24): Jonathan asked for usefulness 80. Cursor implemented the books-mouth, scorer, and first-tap-at-80; Codex still reviews CAD clamp vs member-scoped disclosure. Claude viewport matrix (320 / 390 / 720 / ~1100) remains. **Why-note:** live Workers AI echoed `GROUNDED JOURNAL` because every `$` in the dump was an allowed figure. Sanitize now falls back on prompt labels, `FIGURES` headings, and a Visa answer that only names Mastercard.
- **D-115 / D-116** (2026-08-24): Member-scoped AI disclosure projection. Briefing/notices/ledger/memories for the model rebuild from `householdForAiDisclosure`; independent post-merge review added grounded-answer and FIGURES reconstruction after reproducing a partner-personal P&L aggregate leak. Delayed responses are now rejected unless request, environment, household, and member still match.
- **D-119** (2026-08-24): Last-sync undo/reverse shipped in [PR #76](https://github.com/jonathanbeaulne123-blip/dual-ai-budget-app/pull/76). Renumbered from draft D-115 so D-114 stays cloud continuity on `main` and D-115/D-116 remain free for Hercules disclosure-scope rows referenced in `docs/ARCHITECTURE.md`. Claude authored the UX spec; Cursor Cloud Agent (GPT) implemented Slice A+B.
- **D-120** (2026-08-24): Jonathan’s chalkboard / Home theme / Hercules snippet brief. Desk control label is **Home theme**, not Look. Bought removed from Office chalkboard and legacy `DailyHearth` (unused in app shell).
- **D-121** (2026-08-24): PR #79's chat-host/meter salvage was originally recorded as D-120 before PR #80 independently merged the Office theme under that id. The limiter is renumbered D-121; code behavior is unchanged.
- **D-122** (2026-08-24): Client wires `publish_household_snapshot`; migration 002 remains unapplied until Jonathan approves. Deterministic two-client CAS/outbox proofs ship in-repo; live hosted RPC is a separate apply step. Draft id collided with D-121 meter, so this slice is D-122.
- **D-122** (2026-08-25): Jonathan applied `002_snapshot_cas.sql` to Development via SQL editor after the 12-arg REVOKE/GRANT signature fix. Live smoke `pnpm books:smoke:cas` passed create / duplicate / stale / advance on disposable household `HH-cas-smoke-mt7xsikl`. Production still unapplied.
- **D-123** (2026-08-24): Jonathan locked Q1 A, Q2 owner/member, Q3 email|QR, Q4 no anon household REST, Q5 Dev apply after review. Membership door stays `continuity_memberships` (not a fictional Auth `members` table). Phrase/Pass remain legacy aids, not PostgREST passwords. Packet is executable but unapplied; Production is a separate approval.
- **D-123** (2026-08-24): Jonathan approved the Development migration process and deletion of legacy Development households. Migrations 004/005 are live; the guarded cleanup removed 30 Development households and left the one Production household untouched. Migration 006 remains unapplied because its RLS/grant cutover is global to the shared Supabase project.
- **D-123 why-note (2026-08-25):** Jonathan chose path B (shared-project cutover). Trust review: applying 006 before a Production hosted write path and green preflight would abort or strand Production. Preflight packet shipped; apply deferred.
- **D-123 why-note (2026-08-25, follow-up):** Production continuity client ships behind `VITE_PRODUCTION_CONTINUITY=1` (off by default). SELECT-only `008_production_continuity_select.sql` and privileged seed template are in-repo, unapplied. 006 remains unapplied. Rollback packet id reserved as `009_rollback_006.sql`.
- **D-123 why-note (2026-08-25, preflight):** Live preflight showed migrations `2,4,5,7`; one empty Production household `HH-9465baf2ec6c9d9d` (Personal counts 0/0/0; zero memberships; zero `auth.users`). Jonathan: delete that Production household (no useful data); apply 008; configure Google Auth per `SUPABASE_GOOGLE_AUTH_SETUP.md`; postpone 006 Production-abort NOTICE revision until tasks 1–3 finish.
- **D-123 why-note (2026-08-25, cutover prep):** Tasks 1–3 done (Production gone; Google Auth live; `008` applied as schema id 8). Path B revises 006 Production guard to `RAISE NOTICE` with ceiling 1; `009_rollback_006.sql` shipped. **006 still not applied** pending green preflight re-run + Jonathan paste approval.
- **D-119 why-note (2026-08-25, quiet sync/undo):** Jonathan approved: sync immediately after ledger writes; undo only for write actions; Hercules/UX must not show an undo popup. Implemented as `classifyCommitWrite` (TXN/SHF = ledger-write). Kitchen commits enqueue and flush in the background without toast/Recent history. No fixed 10s heartbeat — launch/focus/reconnect remains the idle path.
- **D-119 / D-124 why-note (2026-08-25, combined undo engine):** Jonathan locked confirmation-scoped LIFO Undo (partner stays, auto CAS), owner-only Restore points, refuse Restore while conflicted (Q8 A), labels Undo vs Restore, Dev+Prod one engine. Whole-snapshot Dev undo retired for daily use.
- **D-149 why-note (2026-08-26, Realtime enablement):** Jonathan approved Migration **014** publication + `VITE_CONTINUITY_REALTIME=1` on Development builds. Client Realtime subscribe path was merged with T1-S3 (#165); enablement merged #175. **014 apply** confirmed live. Production Realtime stays off until 008 personal RLS OR-policy is reviewed.
- **D-149 why-note (2026-08-27, two-phone smoke):** Jonathan passed manual Tier 1 G4 Realtime latency smoke on two phones — A posts → B visible **≤500 ms p95** on live Development kitchen. Recorded in [`SYNC_REALTIME_SMOKE.md`](SYNC_REALTIME_SMOKE.md). Distinct from D-150 QR invite smoke. T2 remains gated on G6 trust review + T2-S1 rebase/merge.
- **D-149 why-note (2026-08-27, G6 trust review):** Independent `hearth-trust-auditor` + `books-auditor` reviewed Tier 1 slices T1-S1…T1-S5. Trust **FAIL** on P0: gzip personal envelope broke Migration 012 `payload_is_member_personal` above ~2 KB — remediated with `encodePersonalEnvelopePayload` (plain JSON) + Production Realtime guard matching 012. Books **PASS WITH NOTES** (P1 proof gaps: no live 012 smoke, harness still legacy CAS stub). G6 **CONDITIONAL PASS** — see [`worksessions/2026-08-27-g6-trust-review.md`](worksessions/2026-08-27-g6-trust-review.md).
- **D-149 why-note (2026-08-27, Tier 2 land):** Jonathan authorized same-day Tier 2 land + deploy. T2-S1…T2-S6 cherry-picked onto main (`cursor/t2-stack-deploy-12ce`). Hosted Migration **013** apply is the live gate; `VITE_CONTINUITY_COMMAND_LOG=1` flips after apply. See [`SYNC_COMMAND_LOG_APPLY.md`](SYNC_COMMAND_LOG_APPLY.md).
- **D-119 why-note (2026-08-26, T2-S6 command-log undo):** `undoLedgerConfirm` refuses when a reversal row references the posted id (journal integrity). Legacy whole-snapshot `undo()` stays test-only. Command classification table in `src/core/commandClassification.ts`. Tier 2 gate G4 proof: `test/continuity-command-undo-interleaving.test.ts`.
- **D-150 why-note (2026-08-26, live smoke):** Jonathan pasted migration **015**, cleaned disposable Development households via delete/leave UI, and passed two-device QR invite smoke on the live kitchen (redeem → immediate discovery → open → PGlite; no pending-invite loop). Recorded in [`AUTH_INVITE_SMOKE.md`](AUTH_INVITE_SMOKE.md). Broader Auth checklist (email invite, revoke UI, anon matrix) remains open on the roadmap.
- **D-149 why-note (2026-08-27, first-create stays on hearth_create_household):** Command-log flush (`VITE_CONTINUITY_COMMAND_LOG=1`) must not call `append_continuity_command` when `expectedRevision === 0`. The first hosted write stays on `hearth_create_household` so `continuity_memberships` gets `role='owner'`. Append requires an existing membership and never inserts the owner; invite then returns `not-owner` for the person who started the household. Later command-ref flushes (`expectedRevision > 0`) still append.
- **D-149 why-note (2026-08-27, first-create retry is not another phone):** After `hearth_create_household` returns `household-already-exists`, CAS from the hosted snapshot revision — never `expectedRevision === 0`. 012 treats expected 0 vs current ≥ 1 as `stale-revision`, whose default copy is “Another phone posted a newer household snapshot.” Client compare uses that line when the hosted tip is numerically newer. Unrecognized CAS reasons still fall through to the same default (expired Auth is a known follow-up). **October open:** this retry does not prove ancestry; local-ahead of a different lineage can still CAS. Use `canAbsorbDisjointSharedMoney` before Production cutover.
- **D-151 why-note (2026-08-27, start from scratch):** Jonathan asked to make deleting all households and starting over easy. Kitchen Confirm **Start from scratch** (picker + More) plus migration **016**; Production blocked; 015 loop is the fallback if the bulk RPC is missing.
- **D-151 why-note (2026-08-27, paste 016):** Jonathan ordered “paste 016.” Applied on project `tykhocwacaxwquhynkok` via Supabase MCP `apply_migration` (`reset_development_households`, MCP version `20260827072847`). `public.schema_migrations` id 16. Function grants: `authenticated` EXECUTE, `anon` denied. Production household count unchanged (0). Development household count unchanged (7) — the reset RPC was **not** called. Kitchen Start-from-scratch UI merged via #201.
- **D-151 why-note (2026-08-27, Preview history 016):** GitHub Supabase Preview failed with `Remote migration versions not found in local migrations directory` because MCP had recorded `supabase_migrations.schema_migrations` version `20260827072847` while the repo file is `016_reset_development_households.sql`. Jonathan ordered the check fixed. History row retagged to `016` (name unchanged). No function re-apply, no household rows, no Production. `009` stays `docs/sql/009_rollback_006.sql` only.
- **D-153 why-note (2026-08-27, Shift tab):** Jonathan locked the 390px Today / Report / Jobs mocks and asked the kitchen to match them. Nav is seven columns with Add centered. Home Timesheet stays. The tab never posts. Numbered D-153 after #208 claimed D-152 for tip covariates.
- **D-152 / D-153 why-note (2026-08-27, Shift Today camera):** Jonathan asked to put the #208 tip-sheet camera on the Shift page. Clock out / Already off stay on Today and host the same `documentHint: shift-report` draft + Confirm. Add keeps the same scan bar. Demo kitchen now has a Bianca job so welcome-demo can punch. OCR still never posts. Same-day Confirm retry stays on `postWorkShift` even if Add is still on the expense pad; the duplicate banner stays on Shift Today. Merged #217; kitchen Worker `c942e55b` live 2026-08-27.
- **D-152 why-note (2026-08-27, provider choice + clarity gate):** Jonathan asked for all three vision providers on the tip-sheet camera, and for capture to stay locked until the slip is clear/legible (QR-scanner style). Shift scan bar now offers Auto / Workers AI / OpenAI / Anthropic (device preference); forced provider is honored by `/documents/scan` without silent fall-through. Live `DocumentCamera` scores sharpness/contrast/brightness/textiness and refuses shutter until ready; Choose-photo applies the same clarity check. Confirm-only invent-nothing boundary unchanged.
- **D-152 why-note (2026-08-27, OpenAI tip-sheet 503):** Forcing OpenAI failed because `strict: true` structured outputs rejected `shiftDraft` (properties without a matching `required` list). Schema now null-unions every tip-sheet field under `required`, and `scanOpenAI` retries with `json_object` / plain JSON if strict mode is refused.
- **D-152 why-note (2026-08-28, tip-sheet transcript-first):** Blank/invalid Confirm drafts after OpenAI came back were from wrong `documentKind`, weak transcripts, and POS wholesale overwrite — not lack of PDF. Vision APIs here are image-only, so PDF wrapping is a dead end. Tip sheets now: (1) transcript-first prompt, (2) coerce to `shift-report` when the tip-sheet hint + Toast OCR agree, (3) field-level POS merge that keeps model-only keys, (4) contrast + higher-quality JPEG prep (no PDF). Confirm-only invent-nothing unchanged.
- **D-152 why-note (2026-08-28, free-first Auto):** Auto on tip sheets now tries **free Workers AI first** and only calls paid OpenAI/Anthropic when the sanitized draft still has fewer than two core totals. Forced OpenAI uses one `json_object` call (`auto` image detail, 2800 max tokens) — no strict-schema retry chain. Pick OpenAI manually only when Auto is not enough.
- **D-155 why-note (2026-08-27, 7shifts release):** Jonathan ordered the reviewed 7shifts inbox integrated into the new Shift tab, then pushed, merged, applied to Development D1, enabled, and deployed. Release is deliberately two-stage on the same day: inert code first, then D1 + secrets, then the minimal enablement diff. `SEVENSHIFTS_ALLOW_PRODUCTION=false` remains a hard lock.
- **D-158 / D-159 why-note (2026-08-28, PR #231):** Jonathan explicitly authorized push and merge after the inert Development vault, extraction, accounting, privacy, and Worker/UI review gates passed. The branch reconciled `origin/main@4a7ea7e` and tightened shared Toast OCR learning from wildcard CORS to exact Hearth/preview/local origins. Merge does not enable Evidence capture, email, 7shifts access, automation, or Production; all five activation flags remain false.
- **D-041 why-note (2026-08-28, queue handler):** Kitchen publish of `d067e56` failed Cloudflare Workers `33184620358` and Workers Builds `d6a7492e` with API **11001** (queue handler missing). `wrangler.jsonc` still has no `queues` consumers; Hearth does not use Cloudflare Queues. The Worker now exports a no-op `queue` that only `ack()`s leftover deliveries so `wrangler deploy` can publish. Do not add a consumer binding.
- **D-156 why-note (2026-08-28, live chalk; nav reverted):** Wide Home keeps the screenshot paper office: fat Draft C bottom nav with plus FAB, POST/DUE/HEALTH seals, Today's stories, and **More on this desk**. The mini-browser chip strip is withdrawn. Chalkboard still fills the notebook as a live paint surface: no typeset/Save chrome, tiny close, letter eraser, saved strokes shrink in place. Phone `<720px` stays Draft C.
- **D-156 why-note (2026-08-28, wide paper office):** Jonathan asked to port the phone UX to desktop without a 1:1 stretch. Default wide Home is composed paper (seals + mosaic + side notebook + infographics). Classic free-move desk stays behind Cabinets. Column cap moves 900 → 1120; 1280 lobby still refused. Phone C unchanged.
- **D-156 why-note (2026-08-28, posted tip spark):** Weekday tip bars on wide Home are rolling posted cents. They must not wear a Projection pill. Between 720–899px the paper office stacks to one column; two-column starts at 900px.
- **D-124** (2026-08-25): Jonathan locked R1 owners-only restore, R2 everyone can see, R3 last 30 days, R4 Dev+Production together, R5 hosted with household. D-123 remains reserved for the Auth/RLS packet on its branch. No D-124 implementation in the Recent-changes copy pass.
- **D-125** (2026-08-25): Add repeating on Calendar → Bills. Expense/income/transfer; daily cadence added; edit existing; Confirm optional post-first.
- **D-126** (2026-08-25): Jonathan locked Q1 A, Q2 C, Q3 A+B, Q4 C, Q5 A, Q6 C, Q7 B, Q8 A. Books civil dates stay Toronto; each phone keeps its own display zone and opt-in location stamps.
- **D-126 why-note (2026-08-25):** Hosted timezone packet renamed to `007_household_timezone_iana.sql` so it does not collide with applied Auth preparation id 4 (`004_auth_rls_prepare.sql`).
- **D-126** (2026-08-25): Jonathan applied `007_household_timezone_iana.sql` to the shared Supabase project. Hosted CHECK matches PGlite nonempty IANA; app kernel still requires Toronto for books civil (Q2 C).
- **D-090 / D-120 why-note (2026-08-25):** Desktop document scroll must work. The mobile viewport lock (`overflow-x: hidden`, `touch-action: manipulation` on `html`/`body`) is scoped to `<720px` only. Wide chalk draw surface is a fixed 140px board — `ChalkCanvas` tall mode sizes from width, never parent height (ResizeObserver growth loop while drawing). Books Wallet expands the tapped account tile in place; details no longer render as a separate panel under the strip.
- **D-127** (2026-08-25): Job-based work flow implemented locally in four slices: job rules, Timesheet, shift Confirm/history, then Calendar settlement/reporting. No hosted schema or provider integration is required because the existing continuity envelope carries the shaped work records.
- **D-127 review gate** (2026-08-25): Jonathan waived a separate slice review. Include D-127 in the single comprehensive project review immediately before September.
- **D-130 why-note (2026-08-25, import repair):** Automatic duplicate tier thresholds and defaults stay unchanged. Complete rows at `20%` or below are auto-kept and hidden from visual review; missing accounting details override hiding and remain fail-closed. The duplicate/details counts drive a focus queue onto the immediate decision or field, following a row across confidence tabs, advancing after **Keep imported / Keep both**, and centering final Confirm when the queue clears. A clean synthetic receipt reproduced live `503 Document detection is unavailable`; the Worker now falls back from OpenAI and Anthropic to its bound Cloudflare Workers AI vision model and deterministically redacts long account/card-like numbers from every model-controlled free-text field. Jonathan accepted the Meta terms and authorized Development deployments: run `32891016109` deployed receipt-repair commit `fc280f8`, and run `32893244966` deployed batch-review commit `fd70869` as Worker version `f49569d8-412d-428e-acd3-e5d07da968df`. The live batch bundle was verified; receipt diagnosis remains deferred. No schema, secret, or hosted-data mutation occurred.
- **D-130 why-note (2026-08-25, combined-batch acceptance):** Importing all statement batches at once exposed two `localStorage` quota multipliers: `saveHousehold` rejected before using IndexedDB, and persisted Undo history duplicated as many as 20 full household snapshots. Full accepted replicas may now persist through IndexedDB when the optional local backup is too large; failure remains closed when neither device store succeeds. Persisted ledger Undo keeps scoped confirmation metadata and posted ids, then hydrates against the current household because confirmation Undo removes those ids rather than restoring the old snapshot. Batch review surfaces the exact `CommandOutcome.userMessage` and remains staged on rejection.
- **D-134 why-note (2026-08-25):** Import descriptions are financial data, so the first context-coding pass uses local category/account names and member-visible ledger history rather than adding a Hercules model payload. Repeated-history inference requires two transfer pairs and a unique winner. Direct account evidence must also be unique. This reduces repetitive transfer/category choices without weakening the existing missing-detail or final Confirm gates.
- **D-134 unique-kind follow-up (2026-08-25):** `Everyday chequing` originally required both account-name tokens, so bank descriptions containing only `transfer from chequing` stayed incomplete and visible. A short kind now resolves only when the statement account is known, differs from that kind, and exactly one eligible active account has the mentioned kind. Multiple chequing/card/savings candidates and same-kind statement descriptions remain fail-closed. External e-transfer wording disables kind/history inference, preventing a complete 0% row from being silently hidden on weak evidence. The signed bank amount—not the words `from` or `to`—continues to control transfer direction.
- **D-036 / D-119 why-note (2026-08-25, App Store sync UX):** Undo history persists compact metadata for the last 20 ledger Confirms per env+household+member on this phone (`localStorage`); it never duplicates full household snapshots. Conflict sheet shows shared impact and does not autofocus Keep this phone. Sign out clears this phone only — cloud household remains; no wipe RPC. Pairing Invite vs Advanced; Retry force-flushes outbox; Restore tip hosts after sync; Personal stripped from restore tip payloads. Worksession: `docs/worksessions/2026-08-25-appstore-sync-ux.md`.
- **D-135** (2026-08-25): Phase 0 environment isolation — central `environmentIsolation.ts` binds join/pass/pull/persist/reconcile/outbox to environment + household + invite tuple; adversarial tests reject mismatches without persistence or fetch. Worksession: `docs/worksessions/2026-08-25-environment-isolation.md`.
- **D-123 why-note (2026-08-26, canon drift):** Living canon (`AGENTS.md`, `CLOUD_CONTINUITY.md`, `ENVIRONMENTS.md`, `ARCHITECTURE.md`, roadmap) incorrectly still claimed `USING (true)` / “006 unapplied” after Jonathan applied 006. Corrected to: 006 + 008 applied; anon household REST revoked; Create/invite smoke still open. Historical decision rows that described the open door remain history.
- **D-139 why-note (2026-08-26, companion load repair):** The first connected card proved the MCP HTML shell but stayed at `Waking Hercules…`: ChatGPT runs the resource under an OpenAI sandbox origin, while Hearth served its module and GLB without cross-origin permission. The v2 resource narrowly exposes only the public companion JS, GLB, and mark, and adds independent module/model deadlines so failure becomes a visible static Hercules rather than an endless wait. Books, OAuth, grounding, and write authority are unchanged.
- **D-139 why-note (2026-08-26, PiP auto-load):** “Near the start” was too optional and the widget waited for the model before requesting the host display. Resource v3 makes `summon_hercules` the mandatory first tool on the first user turn and requests PiP as soon as the bridge exists. A blank chat still cannot run tools before a message, and ChatGPT retains final display control; denied/unsupported PiP stays animated inline. No accounting, OAuth, or write authority changed.
- **D-097 why-note (2026-08-30, intelligent rig lifecycle):** Hercules remains autonomously alive through his part-level rig; he is not replaced with a static or CSS-only fallback. The unconditional animation-frame/React publication loop is replaced by mood-adaptive sleeping cadence (6/8/12 fps ambient, 24 fps bounded reactions), cached clip tracks, stable controls, unchanged-snapshot suppression, and hidden/reduced-motion suspension. Browser transitions only interpolate engine-issued targets. Remote rig polling stays dormant until `hearthRig().sessionId()` explicitly activates it. After ten seconds without human activity, desktop Hercules gets one quick geometric fly-pounce opportunity per idle period; only overlap at the capture frame adds the fly to the existing litter box. No money, ledger, Health, or household snapshot authority changes.
- **D-175 why-note (2026-08-30, swift local acceptance):** Demo entry acknowledges the tap before constructing six months of synthetic history or starting PGlite: the small member catalog paints first, then the existing full demo acceptance begins after two browser frames. Choosing a member cannot enter Home until the transactional PGlite projection, verification, and durable snapshot acceptance succeed. The projection remains a full `TRUNCATE`-and-rebuild transaction with the same SQL equation/hash checks, but bounded multi-row inserts replace one awaited WASM/IndexedDB query per row. This changes query count, not financial facts, rollback, persistence-before-ack, environment isolation, or Confirm authority.
- **D-174 why-note (2026-08-30, SF-01 reconciliation):** SF-01 records current capability truth in [`SHARED_MONEY_BASELINE.md`](SHARED_MONEY_BASELINE.md) and [`shared-money-baseline.json`](shared-money-baseline.json). Development Realtime is the proven primary path with poll fallback; the current build enables membership-scoped Production REST continuity but has no Production runtime proof and still refuses Production Realtime. D-141 reconciliation is on current main but its current served bundle is unverified. D-148 Development provider evidence exists; D-162 governs Fund-specific connected use and remains read-only/Release-gated. These are truth corrections only: no authority, runtime, schema, secret, hosted data, or Production state changed.
- **D-176 release continuation (2026-08-30):** Jonathan explicitly authorized disposable-Development apply, hosted smoke, and—if green—merge/deploy. Migration 017 was applied from the committed 33,216-byte SQL file (SHA-256 `6fd14ecde4755e346d8c46f510ea787d2f99a3a49bd98101f1ab66ce5b8839c1`). A two-principal transactional hosted smoke returned `SF02_HOSTED_SMOKE_PASS`, including invite replacement/replay, RLS isolation, device/member revoke, Personal-seat reuse denial, leave/last-owner, sanitized audit, and anon/private-schema denial, then rolled back. Postflight: 2 Development households, 0 Production households, 0 synthetic households, 0 registered sessions, and 0 audit events. Security Advisor: 0 errors and 18 expected guarded `SECURITY DEFINER` warnings. Merge/deploy and live-origin client proof remain the final release step.
- **D-143** (2026-08-26): Auth membership is the sole automatic continuity authority. `linked` alone no longer publishes.
- **D-145** (2026-08-26): Slim continuity outbox + selective gzip hosted payloads (#155). Shared CAS payloads stay plain for live 006 SQL guards.
- **D-146** (2026-08-26): Phase 0 Google membership tuple on automatic continuity boundaries; unified financial hash for PGlite/accept/CAS; Sheets-era tracker archive.
- **D-147** (2026-08-26): Demote optional Publish/`linked` transport; refuse legacy race when CAS expected; Personal fail after Shared CAS stays pending; Hercules KV + branch-protection runbooks; WORKING_MEMORY drift closed.
- **D-137 why-note (2026-08-26, read-only reconnect):** ChatGPT may request the union of every advertised OAuth scope even when a member left optional writing off. Rejecting that request also blocked the read-only companion. Hearth now narrows such a grant to `hearth.read`; it never enables Personal or Household writing. A later write still requires the member opt-in, a fresh `hearth.write` grant, sealed preview, and explicit confirmation.
- **D-130 why-note (2026-08-26, PGlite IndexedDB ceiling):** The active PGlite projection is a derived, transactional rebuild of the accepted household snapshot. Replacing it with `DELETE` retained the previous large relation pages, so the next household write could exceed Chromium's 127 MiB serialized IndexedDB-value limit even when creating an empty household. The rebuild now transactionally truncates the household graph plus its non-FK `audit_revisions`: rollback keeps the last accepted journal and audit hash, A→B→A replica switches cannot collide on stale audit ids, successful replacement releases the old relation files, and local/hosted household snapshots remain untouched.
- **D-043 / D-048 why-note (2026-08-28, kitchen notices):** Kitchen errors stay fail-closed (Google member mismatch still refuses bank connect; a missing PGlite receipt still does not ingest). The UI maps those engine strings to a compact closeable chip: one problem line, 1–2 fix steps, optional Open More / Reload, and ×. Same size language as the sync freshness chip. Engine/worker copy is unchanged.
- **D-167 why-note (2026-08-28, blank household boot):** Welcome worked; opening a household blanked `#root`. PR #235 already moved Evidence automation above early returns. This packet hardens remaining kitchen-only throws: GIS tokens without `identity`, missing `shiftSettings` on shape, and an App ErrorBoundary. Sign out of Google clears Auth/GIS keys **and** the member session so recovery cannot remount the same kitchen. Open welcome clears `hearth:session:v1:*` only. Household replicas and PGlite stay. Recovery never posts. Renumbered from local D-161 because `main` assigned D-161 to the Household Fund.
- **D-166 release (2026-08-28):** PR #245 merged as `7d74e527` and deployed to `hearth-books` as Worker version `2e63376d-4811-49f8-87e9-8aeb3553b6cd`. The release added private 7shifts roster identity and reviewed attendance context without schema, secret, or feature-flag changes.
- **D-168** (2026-08-28): Published coworker schedule windows remain member-Personal outlook. Shift review may preload coworkers scheduled on the selected job/date (or within the exact shift window plus one hour), starting present for explicit correction. Surprise helpers are added as private coworkers. The same visible Shift Confirm retains the non-financial attendance sidecar; attendance never supplies staffing count, sales, tips, hours, role authority, or money. Independent review hardened Undo to remove the shift and private sidecars together, resets reviewed attendance on a changed schedule revision, and permits schedule retirement only from an explicitly reviewed complete date range with Personal tombstones; partial captures remain additive. Renumbered from local D-167 after current `main` assigned D-167 to blank-household boot recovery.
- **D-169** (2026-08-28): The Development Chrome/Edge companion may capture the explicitly selected, currently visible 7shifts published-week grid through a fixed positive DOM projection when the page is server/page-state rendered and produces no fresh allowlisted response. Only displayed coworker name, role, date, station, and schedule window fields enter the existing encrypted one-use Evidence upload. Generic DOM, cookies, storage, request headers/bodies, credentials, other origins, and background navigation remain excluded. These facts are member-Personal outlook and never prove worked time, tips, sales, staffing, or money.
- **D-170** (2026-08-28): The Development companion may capture the explicitly selected, currently visible 7shifts **My Timesheets** pay-period table through a fixed positive projection. It retains only date, location, role, punch-in, punch-out, displayed breaks, approval, and decimal hours; it deliberately excludes the displayed wage column and unrelated page content. A real authenticated smoke produced four encrypted, approved worked-shift records with complete clock/worked-minute observations and zero wage observations. This is worked-time evidence only: Hearth job rules remain money authority, and posting still requires the ordinary Shift review/Confirm path.
- **D-170 why-note (2026-08-28, review placement):** `Review facts` already returned the correct member-scoped derived facts, but the panel rendered after the entire 100-item capture history, making the action appear broken. The derived panel now opens before the history and receives focus when loaded. Export, evidence content, money authority, and hosted scope are unchanged.
- **D-169 / D-170 why-note (2026-08-28, intake P1 hardening):** Evidence-backed work posting now requires explicit authority for worked and paid-break minutes and, for tipped roles, both tip sides plus required sales/covariates; a missing value cannot become zero. Job-specific requirements are checked before an automation job is eligible or claimed. My Timesheets is removed from generic response interception and remains available only through its fixed selected-table projection. Fictional stress fixtures are Development-only in both UI and the generator. OCR never calculates card tips from Total Tips, Cash Tips, or Merchant Owes Employee; only separately labeled cash/card fields survive deterministic parsing and model merge, while missing sides stay blank with a review warning. No hosted state, provider call, schema, secret, or Production data changed.
- **D-169 / D-170 release (2026-08-28):** Independent books, privacy, and Worker/UI reviewers reported no open P0/P1/P2. Exact commit `57c5537ee676c3c80c1d94340eb0662e34e462c1` was pushed to `codex/d169-visible-schedule-capture` and deployed to `hearth-books` as Worker version `f4876180-0819-4972-aab0-c99d269d007d`; the kitchen returned HTTP 200. No migration, secret change, provider call, or household-data mutation occurred.
- **D-171** (2026-08-28): A clear approved/final worked punch in the member Evidence review may prefill an editable Shift draft with date, exact clock window, worked hours, and only an explicitly observed paid-break value. It never copies tips, sales, covers, or staffing and never calls a writer; Shift → Today opens at Job & time and the ordinary four-step review plus visible Confirm remain mandatory. Break labels are bounded and classified only when they explicitly say no breaks/zero or label durations paid/unpaid. Unclassified break time stays unknown. The ordinary work command now distinguishes blank from explicit zero for paid breaks, required sales, and both tipped cash/card fields so missing values cannot cross Confirm as zero.
- **D-171 release** (2026-08-28): Independent books, privacy, and Worker/UI reviewers reported no open P0/P1/P2 after exact member/job/provider attribution was required. Application commit `021b61e8ea968ff490d52b24eca550b7a64ee089` was pushed to `codex/d171-approved-punch-shift-draft` and deployed to `hearth-books` as Worker version `3407ba4c-5e0c-4954-bbda-c215ceda8e07`; the kitchen returned HTTP 200. No migration, secret change, provider call, or household-data mutation occurred.
- **D-172** (2026-08-29): Near-autonomy belongs to collection and prefilling, not money authority. A revocable capture-only Chrome companion may project the signed-in employee's fixed published-schedule and My Timesheets shapes while Chrome is running; optional `gmail.readonly` incremental sync retains only exact-domain 7shifts mail. These sources reconcile into member-Personal Shift mail. Schedule disappearance is `cut` only from an explicitly complete captured week; explicit call-out, trade-away, and pickup notices retain their own status. Approved/final worked facts may prefill exact actual time and explicit breaks, but tips, sales, restaurant covers, and staffing remain blank until scanned or entered. The existing visible Shift Confirm atomically records books, reviewed attendance, and a versioned worked `ShiftBible`; non-work outcomes have their own visible nonfinancial confirmation. D-159 automatic posting is superseded and Worker automation routes fail closed. The Bible is the durable Tip Science record for new envelope-backed work. Job-place coordinates are rounded and historical weather is fail-soft context, never proof of patio/events. Contributing raw evidence is crypto-erased seven days after sealing; unresolved evidence, reusable jobs/coworkers, and revocable device registration follow separate lifecycles. Production, migrations, flags, extension distribution, and Gmail OAuth publication remain separately gated.
- **D-173 why-note (2026-08-29, kitchen desk):** Personal Books floor ≠ Personal `scopedHousehold`. Home seals use posted `incomeActualCents` / `expenseActualCents`. Leftover spend ≠ sit-down leftover. Shared Wallet is the pool plus Kitty Banks, not a room list. iPhone `OfficePhone` mosaic structure is unchanged except seal labels.
- **D-173 privacy repair (2026-08-29):** Books presentation is never the accepted household. Shared Audit/export receives household/both rooms only; Personal Audit/export receives household/both rooms plus the signed-in member's personal rooms. Journal, trial, statements, reconciliation history, import review, Ask, and SQL/CSV/close/sit-down downloads all use that presentation floor. The accepted snapshot remains write authority, including Batch Import Confirm; its visible review uses the floor but its command build uses accepted books so hidden reconciliation/history cannot be dropped. Raw device-wide Power SQL is unavailable on scoped floors because the on-device database also contains partner-personal rooms.
- **D-179 (2026-08-29):** Investor proof uses a dedicated, visibly synthetic Development household rather than replacing ordinary Development books. A fresh random master seed is always shown and exactly replayable; named domain streams independently generate accounts/cards/investments, income, expenses/imports/transfers, bills/budgets, shifts/schedules/settlements/Evidence, appointments/claims, goals/Fund, reconciliation/close, privacy canaries, and scale. The readiness report executes every Hercules read-tool contract, checks journal/equation/privacy/proposal-only gates, and hashes its non-sensitive report with SHA-256. Synthetic 7shifts/Evidence is hermetic and never calls Gmail, 7shifts, OCR, or another provider. Hercules Pro must disclose synthetic status. The investor finale may prepare one synthetic transaction and post it only after the ordinary explicit Confirm; D-172 remains unchanged.
- **D-179 hardening (2026-08-29):** Synthetic provenance is invalid outside Development at shape, accepted-write, sync assembly/merge, and Hercules identity boundaries. Conflict merge retains the newest deterministic fixture marker instead of silently dropping disclosure. Generated historical shifts carry sealed synthetic Shift Bibles so every Tip Science read exercises the current D-172 record, not a legacy raw shift. The validator plants named Bianca-private account, transaction, goal, schedule/Evidence, and Fund-reconciliation canaries and proves they stay out of Shared sync, Jonathan's Personal replica, Books Audit/Ask, Hercules, JSON, SQL, CSV, close, and sit-down exports. Personal activity metadata is filtered at sync/model boundaries because a hidden row id or account name in an activity summary is still disclosure.
- **D-179 replay attestation repair (2026-08-29):** **Ready** requires a SHA-256 match between the loaded generator-owned facts and a fresh regeneration from seed, date, profile, number style, generator version, coverage digest, and build contract. Household id, invite, link/revision, auth identity, sync receipts, and other continuity-only state are excluded; money, schedules, Evidence/envelopes, Shift Bibles, and private canaries are not. Any generated-fact change is **Not ready**. A later confirmed demo write intentionally diverges from the pristine seed, invalidates the displayed revision immediately, and remains **Not ready** until the pristine seed is replayed.
- **D-181 why-note (2026-08-31, Add slideshow):** Jonathan asked for unique cashpad prompt slideshows after the FAB speed dial, then ordered merge/push/deploy onto current `main`. Expense/income/transfer/shift each have their own titles. Category Add lives on the category slide. Account pickers use Books-floor wallet tiles. Picture/note is optional and local. Confirm remains the only post. Speed dial actions sit `22px` above `+`. Numbered D-181 because `main` D-174 is Shared Money.
- **D-050 why-note (2026-08-31, Add slideshow):** The giant CAD pad remains the amount primitive (digits are cents). Add now advances by Enter through prompts instead of one scrolling sheet. Amounts still submit as dollar strings through `postEntry` / `postShift` / `postTransfer` from the last Confirm screen.
- **D-164 why-note (2026-08-31, FAB speed dial):** Nav `+` opens a vertical paper speed dial (Shift, Income, Expense, Transfer) and then the Add slideshow. The dial never posts. Shared primary nav still has no Shift tab.
- **D-164 why-note (2026-08-29, leftover CAD fence):** Personal Hercules must not recite leftover CAD. Overlay chips, Plan overlay, typed Ask, `planHerculesTurn`, and Books Ask pass `view`. Leftover assignment and Confirm stay Shared. Parks-in copy is Kitty Banks. No new Fund formulas.
- **D-164 why-note (2026-08-29, kitchen notes confirmed):** Jonathan walked Shared and Personal on a laptop and confirmed the stacked kitchen notes. Plan Goals is replaced by Kitty Banks on Shared and Personal (existing goals, paper banks — Shared still D-161 Fund surplus; Personal is not Fund surplus). Shared Home is six tiles + notebook with no Month blotter hero; blotter stays in the desk drawer. Sit-down cycles paper charts; leftover assignment stays Shared. Calendar cells show titles. Personal nav keeps Books. Home can scroll. Hercules cream bubble must not cover the mosaic. Shift saucers expand into posted cash/card/wage totals. iPhone `OfficePhone` structure unchanged. No new Fund formulas.
- **D-164 why-note (2026-08-28, kitchen composition):** Jonathan’s laptop review: Shared and Personal are different jobs on the same paper. Shared Home folds story into the desk mosaic + notebook (no second stacked room) and uses laptop width. Shared primary nav is Home · Cal · + · Plan · More; Shift stays Personal; Books is a deep Audit/Fund room from More, not a Shared general-ledger landing. Calendar is the board. Plan left column holds sit-down and goals; categories add/adjust on-screen. Kitty Banks remains D-161 Fund surplus → existing shared goals — it does not replace the Goals vault until Jonathan confirms that Plan change. iPhone `OfficePhone` structure unchanged. No new Fund formulas.
- **D-164 why-note (2026-08-28, Shared Household table):** Jonathan reviewed the Shared Books landing (double-entry, household net worth, trial, Hercules unmodified, Assets/Liabilities/Income/Expenses) against the shared-account philosophy. Shared keeps the deep room because Fund setup, kitty, wallet rooms, and import have no other Shared home. It is no longer a Books/story page: opening is Fund or household cash; table tiles are Fund, chequing, goal savings, and cards; net worth, P&L, journal count, and statements live in a collapsed Audit office. Personal still opens on My books / position. No new Fund math.
